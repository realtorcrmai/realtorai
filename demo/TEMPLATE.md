# Demo: <feature-name>

<!-- This file is required for CODING:feature tasks. -->
<!-- It must contain AT LEAST 3 numbered steps and ONE expected-outcome per step. -->
<!-- Someone reproducing this demo should get the feature working with only these instructions. -->

## What this demonstrates

One paragraph describing what the feature does and who uses it.

## Prerequisites

- [ ] Which services must be running (CRM, newsletter, voice, etc.)
- [ ] Which test account to use (use demo@realestatecrm.com / demo1234 unless noted)
- [ ] Which data must exist (e.g., "at least 3 contacts in the dev DB")
- [ ] Any env vars that must be set beyond the standard `.env.local`

## Steps

1. **<action>** — e.g., "Navigate to /contacts"
   Expected: <what you should see>

2. **<action>** — e.g., "Click 'Bulk Export' in the toolbar"
   Expected: <what you should see>

3. **<action>** — e.g., "Select 'CSV' from the format dropdown, click Download"
   Expected: <what you should see; file name, file contents, etc.>

<!-- Add more steps as needed. Minimum 3. -->

## Failure modes

What could go wrong and what it would look like. At least one entry.

- If <condition>, you'll see <symptom>. Fix: <action>.

## Screenshots (optional)

If the feature is UI-heavy, drop screenshots here. Not required for API-only features.
