<!-- docs-audit: src/app/(dashboard)/newsletters/editorial/** -->
<!-- last-verified: 2026-04-15 -->
# QA Expansion — Section O, P, Q, R: Browser/Visual/Navigation Tests
## Test Range: TC-3610 to TC-3759
## Feature: Editorial Newsletter System

> **Module:** Editorial Newsletter (Edition-based email publishing)
> **Date:** 2026-04-15
> **Total Cases:** 150 (TC-3610 to TC-3759)
> **Sections:** O (Editor Browser — 50), P (Visual Regression — 40), Q (Toast/Notification/State Feedback — 30), R (Navigation & Routing — 30)

---

### Section O: Extended Editor Browser Tests (TC-3610 to TC-3659)

| TC | Title | Test Steps | Expected Result | Priority |
|----|-------|-----------|-----------------|----------|
| TC-3610 | Block drag-and-drop reorder — basic | 1. Navigate to `/newsletters/editorial/[id]/edit` with an edition containing at least 3 blocks. 2. Hover over the drag handle (⠿ icon) on the second block. 3. Press and hold the mouse button. 4. Drag the block above the first block. 5. Release the mouse. | The block list reorders in real time as the drag occurs; after release the dragged block occupies position 1; the new order persists in the editor state and saves to the database on the next auto-save cycle. | P0 |
| TC-3611 | Block drag-and-drop reorder — drag to last position | 1. Open an edition with 5 blocks in the editor. 2. Grab the drag handle on the first block. 3. Drag it below all remaining blocks. 4. Release. | Block moves from position 1 to position 5; all intermediate blocks shift up by one position; ordering is reflected immediately in the preview panel if open. | P0 |
| TC-3612 | Block drag-and-drop reorder — no change on same-position drop | 1. Open edition with 3 blocks. 2. Grab drag handle on block 2. 3. Drag it and release it back to the same position (between block 1 and block 3). | No reorder occurs; block remains at position 2; no save or dirty state is triggered. | P1 |
| TC-3613 | DnD keyboard accessibility — reorder via keyboard | 1. Tab to the drag handle of block 2 so it receives focus. 2. Press Space to activate drag mode. 3. Press Arrow Up once. 4. Press Space to drop. | Block 2 moves to position 1; keyboard-only interaction produces the same reorder result as mouse drag; focus returns to the drag handle after drop. | P0 |
| TC-3614 | DnD keyboard accessibility — Escape cancels drag | 1. Tab to drag handle of block 3; press Space to start drag. 2. Press Arrow Up once (block appears to move). 3. Press Escape. | Drag is cancelled; block returns to its original position (position 3); no database write occurs; focus returns to the drag handle. | P1 |
| TC-3615 | DnD keyboard accessibility — announce reorder to screen reader | 1. Use a screen reader (e.g. NVDA or VoiceOver). 2. Tab to drag handle of block 2; press Space. 3. Press Arrow Down once; press Space to confirm drop. | Screen reader announces something like "Block 2 moved to position 3"; live region (`aria-live="polite"`) updates with the new order description; WCAG 2.1 SC 1.3.1 satisfied. | P0 |
| TC-3616 | Drag handle visibility — appears on hover | 1. Open edition editor with at least 2 blocks. 2. Move mouse cursor away from any block (not hovering). 3. Slowly move cursor over block 1. | Drag handle icon (⠿ or equivalent gripper) becomes visible within 150 ms of the cursor entering the block boundary; handle is not visible when cursor is elsewhere. | P1 |
| TC-3617 | Drag handle visibility — always visible on touch devices | 1. Open editor in a browser with touch emulation enabled (DevTools device mode, iPhone 14). 2. Load an edition with 3 blocks. | Drag handles are persistently visible without requiring a hover; handles have a minimum tap target of 44×44 px per WCAG 2.5.5. | P1 |
| TC-3618 | Drop zone highlighting — active drop zone shown during drag | 1. Start dragging a block (mouse held down). 2. Slowly move the cursor over the gap between two other blocks. | A highlighted drop zone indicator (e.g. blue bar or shaded region) appears in the gap between blocks as the cursor enters that zone; the indicator moves as the cursor moves to different gaps. | P1 |
| TC-3619 | Drop zone highlighting — highlight disappears on drop | 1. Drag a block over a valid drop zone (highlighted). 2. Release the mouse to drop. | The drop zone highlight disappears immediately on mouse release; block renders in its new position without any lingering highlight artefact. | P1 |
| TC-3620 | Block collapse — clicking chevron collapses block | 1. Open editor; all blocks expanded by default. 2. Click the collapse/chevron button (▾) on the hero block. | The hero block body collapses to show only its type label and a one-line summary; the chevron rotates to ▸; vertical space is reclaimed in the editor. | P1 |
| TC-3621 | Block expand — clicking chevron on collapsed block expands it | 1. Collapse a `market_commentary` block per TC-3620. 2. Click the chevron (▸) on the collapsed block. | Block expands to full editing view; all fields are visible and editable; chevron returns to ▾. | P1 |
| TC-3622 | Block collapse/expand — state persists across editor scroll | 1. Collapse block 1. 2. Scroll the editor to the bottom (block 10 visible). 3. Scroll back to the top (block 1 visible). | Block 1 remains collapsed after scrolling; collapse state is stored in component state, not lost on scroll. | P2 |
| TC-3623 | Inline editing — hero block fields | 1. Click on the hero block body text in the editor (not a separate modal). 2. Edit the headline field inline (change text from "Market Update" to "April Market Update"). 3. Click outside the field. | Hero block headline updates in place; preview panel (if open) reflects the change; dirty/unsaved indicator appears. | P0 |
| TC-3624 | Inline editing — just_sold block fields | 1. Click on the address field inside a `just_sold` block. 2. Edit the address to "1234 Elm Street, Vancouver". 3. Tab to the sale price field and enter "$1,250,000". 4. Click outside. | Both fields update inline; block shows the new address and price; no modal required; unsaved-changes indicator activates. | P0 |
| TC-3625 | Inline editing — market_commentary block | 1. Click on the body text area of a `market_commentary` block. 2. Edit the commentary text to a 200-word paragraph. 3. Click outside the textarea. | Text updates in the block; character count indicator (if present) updates; auto-save triggers within the configured debounce window (≤3 s). | P0 |
| TC-3626 | Inline editing — rate_watch block | 1. Click the "Prime Rate" value field inside a `rate_watch` block. 2. Change the value from "6.95%" to "6.70%". 3. Tab to the "5-Year Fixed" field and change it to "5.14%". 4. Click elsewhere. | Both rate values update inline; preview panel reflects new rates; no page reload required. | P0 |
| TC-3627 | Inline editing — local_intel block | 1. Click the headline field of a `local_intel` block. 2. Change headline to "Surrey Market Heats Up". 3. Tab to the body field; enter 3 sentences of body copy. | Headline and body update inline; block preview shows new content; dirty state flagged. | P0 |
| TC-3628 | Inline editing — neighborhood_spotlight block | 1. Click the neighbourhood name field in the `neighborhood_spotlight` block. 2. Change it to "Kitsilano". 3. Click the description field and type a 100-word description. | Both fields update without any modal; auto-save fires after debounce; block displays updated neighbourhood info. | P0 |
| TC-3629 | Inline editing — quick_tip block | 1. Click the tip text field in the `quick_tip` block. 2. Clear existing text and type "Always get a pre-inspection before making an offer — it saves negotiation stress." | Text updates inline; character limit indicator (if any) reflects new count; unsaved indicator shows. | P1 |
| TC-3630 | Inline editing — agent_note block | 1. Click the note text area in the `agent_note` block. 2. Type "Hi [First Name], I wanted to personally share this update with you." including the merge tag. | Text updates inline; merge tag displayed as-is in the editor (not resolved); auto-save fires. | P1 |
| TC-3631 | Inline editing — cta block | 1. Click the CTA button label field in the `cta` block. 2. Change the label from "View Listings" to "Book a Free Consultation". 3. Click the URL field and update it to `https://calendly.com/agent`. | Both fields update inline; preview panel reflects new button label and URL; no save required immediately. | P0 |
| TC-3632 | Inline editing — divider block | 1. Click on a `divider` block. 2. If the divider has configurable options (style: solid/dashed/dotted, spacing: small/medium/large), change style to "dashed" and spacing to "large". | Divider style and spacing update; the preview panel shows the dashed divider with increased spacing immediately. | P2 |
| TC-3633 | Undo — Ctrl+Z reverts last inline edit | 1. Edit the headline of the hero block from "Spring Update" to "Summer Update". 2. Press Ctrl+Z (Cmd+Z on Mac). | The headline reverts to "Spring Update"; the undo stack decrements by one; the unsaved indicator remains active if other unsaved changes exist. | P0 |
| TC-3634 | Undo — multiple Ctrl+Z steps | 1. Make 5 sequential edits to different block fields. 2. Press Ctrl+Z five times. | Each Ctrl+Z reverses one edit in reverse chronological order; after 5 presses all edits are reverted; the editor shows original content. | P1 |
| TC-3635 | Undo — redo (Ctrl+Y) after undo | 1. Make 3 edits. 2. Press Ctrl+Z twice to undo 2 edits. 3. Press Ctrl+Y (or Ctrl+Shift+Z) once. | One undone edit is re-applied; the editor state moves forward by one step in the undo stack. | P1 |
| TC-3636 | Undo — undo stack cleared after save | 1. Make 3 edits. 2. Click "Save" or let auto-save fire. 3. Press Ctrl+Z once. | After a confirmed save, the undo stack is cleared; Ctrl+Z has no effect (or shows a "nothing to undo" state); saved content remains. | P2 |
| TC-3637 | Auto-save indicator — shows "Saving…" during save | 1. Open editor and make any inline edit to a block field. 2. Wait for the auto-save debounce window (≤3 s) without clicking save manually. 3. Observe the save indicator in the editor toolbar or header. | A "Saving…" or spinner indicator appears while the PATCH/PUT request is in-flight; the indicator text or icon changes to "Saved" or a checkmark within 2 s of a successful response. | P0 |
| TC-3638 | Auto-save indicator — shows "Saved" after success | 1. Edit a block field. 2. Wait for auto-save to complete successfully (API returns 200). | Indicator changes from "Saving…" to "Saved ✓" (or equivalent); the indicator auto-hides after 3–5 s; no error state shown. | P0 |
| TC-3639 | Auto-save indicator — shows error state on failure | 1. Edit a block field. 2. In DevTools Network tab, block the auto-save endpoint (e.g. pause or return 500). 3. Wait for auto-save attempt. | Indicator changes to "Save failed" or "⚠ Unsaved" in red/amber; a toast or inline message prompts the user to retry; block content is not lost from the editor. | P0 |
| TC-3640 | Optimistic UI — block content visible before save confirms | 1. Edit the `rate_watch` block prime rate field to "6.70%". 2. Immediately scroll down without waiting for save confirmation. | The editor and preview panel both show "6.70%" immediately after the keystroke; the user does not need to wait for the API round-trip to see their change. | P1 |
| TC-3641 | Concurrent editing prevention — edition lock UI | 1. Open the same edition in two separate browser tabs (Tab A and Tab B) as the same user. 2. Begin editing in Tab A. | Tab B shows a "This edition is being edited in another window" banner or modal; the editor in Tab B is in read-only mode or prompts the user before allowing edits; no silent data overwrite occurs. | P0 |
| TC-3642 | Concurrent editing — lock released when Tab A is closed | 1. Follow TC-3641 — Tab A has the lock, Tab B is read-only. 2. Close Tab A. 3. Within 30 s, observe Tab B. | Tab B detects the lock release (via polling or websocket) and removes the read-only banner; the editor in Tab B becomes fully editable. | P1 |
| TC-3643 | Add block — clicking + button opens block type picker | 1. Open edition editor. 2. Scroll to the bottom of the block list. 3. Click the "+ Add Block" button. | A picker panel, dropdown, or modal appears listing all available block types: hero, just_sold, market_commentary, rate_watch, local_intel, neighborhood_spotlight, quick_tip, agent_note, cta, divider. | P0 |
| TC-3644 | Add block — selecting a type inserts block at bottom | 1. Open block type picker per TC-3643. 2. Click "market_commentary" from the picker. | A new `market_commentary` block with empty/placeholder content is appended at the end of the block list; the editor scrolls to make the new block visible; the unsaved indicator activates. | P0 |
| TC-3645 | Add block — inserting block between existing blocks | 1. In the editor with 3 blocks, click the "+ Add Block" insertion point between block 2 and block 3 (if per-gap insertion is supported). 2. Select "quick_tip" from the picker. | The new `quick_tip` block is inserted at position 3; the previous block 3 shifts to position 4; editor scrolls to the new block. | P1 |
| TC-3646 | Delete block — clicking delete shows confirmation | 1. Hover over any block (e.g. `local_intel`). 2. Click the delete (🗑 or ✕) icon on the block. | A confirmation dialog appears: "Delete this block? This cannot be undone." with "Delete" (destructive) and "Cancel" buttons; the block is NOT removed until "Delete" is clicked. | P0 |
| TC-3647 | Delete block — confirming deletion removes block | 1. Follow TC-3646 through to the confirmation dialog. 2. Click "Delete". | The block is removed from the list; remaining blocks reorder to fill the gap; the unsaved indicator activates; the editor scrolls to keep the nearest block in view. | P0 |
| TC-3648 | Delete block — cancelling confirmation keeps block | 1. Follow TC-3646 to show the confirmation dialog. 2. Click "Cancel". | The dialog closes; the block remains in the list unchanged; no dirty state is triggered. | P1 |
| TC-3649 | Duplicate block — clicking duplicate creates copy | 1. Hover over a `just_sold` block with filled content. 2. Click the duplicate (⧉ or "Duplicate") button on the block. | A new `just_sold` block with identical content is inserted immediately below the original; the new block is auto-focused for editing; unsaved indicator activates. | P1 |
| TC-3650 | Block type switcher — change hero to market_commentary | 1. Click the block type label or "Change Type" button on a `hero` block. 2. In the type switcher dropdown, select `market_commentary`. | The block type changes to `market_commentary`; the field layout updates to match the new type; previously entered content is either cleared with a warning or mapped to compatible fields; dirty state flagged. | P1 |
| TC-3651 | Editor scroll position — preserved on re-render | 1. Open editor with 8 blocks and scroll to block 7. 2. Make an inline edit to block 7 that triggers a React state update and re-render. | The editor's scroll position remains at block 7 after the re-render; the user is not snapped back to the top of the block list. | P1 |
| TC-3652 | Unsaved changes warning — triggered on navigation | 1. Make an inline edit to a block field (creating unsaved state). 2. Click the sidebar nav item "Contacts" to navigate away. | A browser confirmation dialog or custom modal appears: "You have unsaved changes. Leave page?" with "Leave" and "Stay" options; clicking "Leave" navigates away; clicking "Stay" cancels navigation and returns focus to the editor. | P0 |
| TC-3653 | Unsaved changes warning — not triggered when no changes | 1. Open the editor without making any changes. 2. Click the sidebar nav item "Listings". | Navigation proceeds immediately without any unsaved-changes prompt. | P1 |
| TC-3654 | Keyboard shortcut — Ctrl+S saves immediately | 1. Make an inline edit to a block field. 2. Press Ctrl+S (Cmd+S on Mac) immediately without waiting for auto-save. | The save is triggered immediately (API request fires); "Saving…" then "Saved ✓" indicator cycles; the edition is persisted to the database; the undo stack is cleared. | P0 |
| TC-3655 | Keyboard shortcut — Ctrl+Z undoes last edit | 1. Change the hero block headline. 2. Press Ctrl+Z. | The headline reverts to its pre-edit value; confirmed via TC-3633 expectations at keyboard level. | P0 |
| TC-3656 | Focus management — focus moves to new block after add | 1. Click "+ Add Block" and select `quick_tip`. | The first editable field of the newly inserted `quick_tip` block receives keyboard focus automatically; the user can begin typing without clicking. | P1 |
| TC-3657 | Focus management — focus returns to delete button area after block deletion | 1. Delete a block (confirm deletion). | After the block is removed, focus is moved to the delete button of the next block or to the "+ Add Block" button if no blocks remain; focus is not lost to the document body. | P1 |
| TC-3658 | Focus management — focus on first field after block type switch | 1. Switch a `hero` block to `cta` via the block type switcher. | The first field of the new `cta` block (e.g. button label) receives focus automatically; tab order is logical. | P2 |
| TC-3659 | Undo — undo block add removes the newly added block | 1. Add a new `divider` block via "+ Add Block". 2. Press Ctrl+Z. | The newly added `divider` block is removed from the list; the editor returns to its pre-add state; block count decrements by 1. | P1 |

---

### Section P: Visual Regression Tests (TC-3660 to TC-3699)

| TC | Title | Test Steps | Expected Result | Priority |
|----|-------|-----------|-----------------|----------|
| TC-3660 | Screenshot — edition dashboard empty state | 1. Log in as a new user with zero editions created. 2. Navigate to `/newsletters/editorial`. 3. Take a full-page screenshot. | Screenshot shows an empty-state illustration, a heading like "No editions yet", a subheading with guidance, and a prominent "Create Edition" CTA button; no table or list renders; layout matches the Realtors360 design system (navy sidebar, white content area). | P1 |
| TC-3661 | Screenshot — edition dashboard with 3+ editions | 1. Ensure at least 3 editions exist with statuses: draft, generating, ready. 2. Navigate to `/newsletters/editorial`. 3. Take a full-page screenshot. | Edition list renders with all 3 rows visible; each row shows edition number, subject line, status badge (colour-coded), scheduled date, and action buttons; no overlapping text or broken layout. | P1 |
| TC-3662 | Screenshot — editor with all 10 block types visible | 1. Open an edition that contains one block of each type: hero, just_sold, market_commentary, rate_watch, local_intel, neighborhood_spotlight, quick_tip, agent_note, cta, divider. 2. Expand all blocks. 3. Take a full-page screenshot. | All 10 block types render with correct colour-coded left-border indicators, appropriate field layouts, and consistent spacing; no block overlaps another; block type labels are clearly legible. | P1 |
| TC-3663 | Screenshot — preview panel desktop viewport | 1. Open `/newsletters/editorial/[id]/preview`. 2. Ensure the desktop viewport toggle is active (or default). 3. Take a screenshot of the preview panel at 1280px viewport width. | Preview renders at a simulated email width (~600 px centred); fonts load correctly; images have fallback alt text visible if images are missing; no horizontal scroll. | P0 |
| TC-3664 | Screenshot — preview panel mobile viewport toggle | 1. Open the edition preview. 2. Click the mobile viewport toggle button (e.g. 📱 icon). 3. Take a screenshot. | Preview re-renders at ~375 px simulated width; single-column layout; font sizes scale up; CTA buttons span full width; no content is cropped or hidden. | P0 |
| TC-3665 | Screenshot — preview panel dark mode enabled | 1. Open edition preview. 2. Enable "Dark Mode Preview" toggle (if present) or switch OS to dark mode. 3. Take a screenshot. | Preview background changes to dark (#1a1535 or equivalent); text switches to light; brand colours adapt; no invisible white-on-white or black-on-black text areas. | P1 |
| TC-3666 | Screenshot — brand colour injection in preview | 1. Set the agent's brand primary colour to #E63946 (red) in settings. 2. Open edition preview containing a `cta` block. 3. Take a screenshot. | The CTA button background uses #E63946; heading underlines or accent lines (if any) also reflect the brand colour; no hardcoded indigo (#4F35D2) visible in brand-injected elements. | P1 |
| TC-3667 | Email client rendering — Gmail paste test | 1. Render the edition HTML (via "Copy HTML" or preview source). 2. Open Gmail compose window. 3. Paste the HTML into the body. 4. Take a screenshot of the Gmail compose preview. | Layout is intact; headings render in the correct font stack (or closest Gmail-supported fallback); CTA button renders as a styled link; images load or show alt text; no broken table cells or misaligned sections. | P0 |
| TC-3668 | Email client rendering — Outlook table layout preservation | 1. Open the edition HTML in Outlook (or use an email preview tool such as Litmus or Email on Acid simulation). 2. Take a screenshot at 600 px wide. | All table-based layout is intact; no sections collapse into a single column unexpectedly; MSO conditionals (if implemented) activate correctly; no stray table borders visible. | P0 |
| TC-3669 | Email client rendering — Apple Mail rendering | 1. Send the edition to a test Apple Mail account (macOS Mail app) OR use a rendering simulation. 2. Take a screenshot in Apple Mail. | Fonts render cleanly (Apple Mail supports web fonts via style tags); the email is not treated as plain text; images load; correct padding and spacing preserved. | P1 |
| TC-3670 | Screenshot — font stack fallback rendering | 1. Open edition preview in a browser with Google Fonts blocked (use DevTools to block fonts.googleapis.com). 2. Take a screenshot. | Text still renders in a legible system sans-serif fallback (Arial, Helvetica, or system-ui); no invisible text (FOIT); layout is not broken by font substitution. | P1 |
| TC-3671 | Screenshot — divider block spacing variants | 1. Create an edition with 3 `divider` blocks configured as: small spacing, medium spacing, large spacing. 2. Open preview. 3. Take a screenshot. | Each divider block shows visually distinct vertical spacing; the small variant has the narrowest gap, large the widest; spacing is proportional and consistent across email clients. | P2 |
| TC-3672 | Screenshot — CTA button visual state: default | 1. Open edition preview containing a `cta` block with label "Book a Consultation". 2. Take a screenshot (no interaction). | Button renders with brand background colour, white text, appropriate border-radius (matching `--lf-r` / design tokens), and a subtle box shadow; corners are not cut off. | P0 |
| TC-3673 | Screenshot — CTA button visual state: hover | 1. Open edition preview in browser. 2. Hover the cursor over the CTA button. 3. Take a screenshot or use DevTools :hover pseudo-state. | Button shows a hover state: slightly darker background (e.g. 10% darker than brand colour), or subtle border change; cursor changes to pointer; no layout shift. | P1 |
| TC-3674 | Screenshot — CTA button visual state: active (pressed) | 1. Open edition preview. 2. Click and hold the CTA button (do not release). 3. Take a screenshot. | Button shows an active/pressed state: depressed appearance (scale transform or further darkened background); visually distinguishable from hover state. | P2 |
| TC-3675 | Screenshot — badge state: draft | 1. Open edition dashboard showing a draft edition. 2. Screenshot the status badge. | Badge renders with grey background (#9CA3AF or equivalent), white or dark text "Draft"; correct border-radius; text is legible at 12–14 px. | P1 |
| TC-3676 | Screenshot — badge state: generating | 1. Trigger AI generation on an edition to put it in "generating" status. 2. Screenshot the status badge within 5 s. | Badge renders with amber/yellow background (#F59E0B or equivalent), text "Generating"; optionally includes an animated spinner or pulse ring. | P1 |
| TC-3677 | Screenshot — badge state: ready | 1. View an edition with "ready" status. 2. Screenshot the status badge. | Badge renders with teal/green background (#00BDA5 or equivalent), text "Ready"; colour-coded appropriately for positive state. | P1 |
| TC-3678 | Screenshot — badge state: sent | 1. View a sent edition. 2. Screenshot the status badge. | Badge renders with navy/blue background (#2D3E50 or equivalent), text "Sent"; visually distinct from "ready" badge. | P1 |
| TC-3679 | Screenshot — badge state: failed | 1. View an edition in a "failed" state (simulate via direct DB update or API mock). 2. Screenshot the status badge. | Badge renders with red background (#D94F57 or equivalent), text "Failed"; accompanied by an error icon (⚠) inline or adjacent to the badge. | P0 |
| TC-3680 | Screenshot — generation progress bar at 0% | 1. Trigger AI generation. 2. Immediately (within 1 s) screenshot the edition card or editor banner showing the progress bar. | Progress bar renders at 0% (fully empty fill); progress bar is visible with a container track; percentage label "0%" (or equivalent) is shown. | P1 |
| TC-3681 | Screenshot — generation progress bar at 25% | 1. Mock or observe AI generation at the 25% milestone. 2. Screenshot. | Progress bar fill covers 25% of the total width; fill colour matches the brand palette (e.g. coral or indigo); label shows "25%". | P1 |
| TC-3682 | Screenshot — generation progress bar at 50% | 1. Mock or observe AI generation at 50% milestone. 2. Screenshot. | Progress bar at 50%; fill is exactly half the container width; label shows "50%". | P1 |
| TC-3683 | Screenshot — generation progress bar at 75% | 1. Mock or observe AI generation at 75% milestone. 2. Screenshot. | Progress bar at 75%; fill covers three-quarters of container; label shows "75%". | P1 |
| TC-3684 | Screenshot — generation progress bar at 100% | 1. Mock or observe AI generation at 100% / completion. 2. Screenshot immediately before the status transitions to "ready". | Progress bar completely filled; label shows "100%" or "Complete"; fill colour may transition to green to signal success; no visual glitch or overflow. | P1 |
| TC-3685 | Screenshot — toast notification: success style | 1. Trigger any action that produces a success toast (e.g. save edition). 2. Screenshot the toast while it is visible. | Toast renders in the bottom-right corner; green left-border or background (#00BDA5); white body; bold title "Success" or action-specific title; "✓" icon; dismiss (✕) button; correct border-radius. | P0 |
| TC-3686 | Screenshot — toast notification: error style | 1. Trigger an action that produces an error toast (e.g. failed send). 2. Screenshot. | Toast renders with red left-border or background (#D94F57); bold title "Error"; descriptive message; "⚠" or "✕" icon; correct styling consistent with design system. | P0 |
| TC-3687 | Screenshot — toast notification: warning style | 1. Trigger a warning condition (e.g. CASL consent warning). 2. Screenshot. | Toast renders with amber/yellow left-border (#F59E0B); title "Warning"; message text; "⚠" icon; distinct from error style. | P1 |
| TC-3688 | Screenshot — toast notification: info style | 1. Trigger an info toast (e.g. auto-save confirmation). 2. Screenshot. | Toast renders with blue/indigo left-border; title "Info" or contextual title; message text; "ℹ" icon; correct style. | P1 |
| TC-3689 | Screenshot — skeleton loading: edition list | 1. In DevTools, throttle network to Slow 3G. 2. Navigate to `/newsletters/editorial`. 3. Screenshot during the loading state before data arrives. | A skeleton loader renders in place of the edition list rows; skeleton items are grey animated bars of approximate row height; no raw "Loading…" text; page title area already rendered. | P1 |
| TC-3690 | Screenshot — skeleton loading: editor | 1. Throttle network to Slow 3G. 2. Navigate to `/newsletters/editorial/[id]/edit`. 3. Screenshot during editor loading. | Block list area shows skeleton placeholders (grey animated bars matching approximate block heights); toolbar/header is already rendered; no layout shift on data arrival. | P1 |
| TC-3691 | Screenshot — voice profile completeness: 0% | 1. Ensure agent has no voice profile data. 2. Open voice profile section in editorial settings. 3. Screenshot the completeness indicator. | Completeness indicator shows 0% (empty progress ring or bar); accompanied by text "Add your writing samples to personalise AI generation"; indicator colour is grey. | P1 |
| TC-3692 | Screenshot — voice profile completeness: 50% | 1. Add one of two required writing sample categories. 2. Screenshot completeness indicator. | Indicator shows 50%; half-filled ring or bar; colour transitions toward amber; prompt text updated to reflect remaining steps. | P1 |
| TC-3693 | Screenshot — voice profile completeness: 100% | 1. Complete all voice profile fields. 2. Screenshot the completeness indicator. | Indicator shows 100%; fully filled ring or bar in green; "Voice profile complete ✓" message shown; no further action prompts. | P1 |
| TC-3694 | Screenshot — A/B subject variant chips: two variants | 1. Create an edition with A/B subject testing enabled (Subject A: "April Market Update"; Subject B: "What's Happening in Your Neighbourhood?"). 2. Screenshot the edition card or editor header showing both variant chips. | Two chips are displayed side by side; Chip A and Chip B are labelled; each shows the subject line text (possibly truncated); chips are colour-differentiated (e.g. indigo for A, coral for B). | P1 |
| TC-3695 | Screenshot — edition number badge display | 1. Create at least 5 editions. 2. Navigate to edition dashboard. 3. Screenshot the edition number badges. | Each edition row or card displays a numeric badge (e.g. "#1", "#2" … "#5") in the correct sequence; badges are formatted consistently (same font size, colour, padding); no two editions share the same number. | P2 |
| TC-3696 | Screenshot — preview panel responsive breakpoint: 480px | 1. Open edition preview. 2. Resize the preview panel to 480 px wide. 3. Screenshot. | Layout renders in single-column at 480 px; no horizontal overflow; all content visible; touch-friendly button sizes. | P1 |
| TC-3697 | Screenshot — block drag ghost image during drag | 1. Begin dragging a block (mouse held). 2. Screenshot while the block is mid-drag. | A semi-transparent drag ghost image of the block follows the cursor; the original block position shows a placeholder; ghost image does not include interactive elements (inputs remain non-interactive during drag). | P2 |
| TC-3698 | Screenshot — edition dashboard with mixed status badges | 1. Ensure editions with statuses draft, generating, ready, sent, failed all exist. 2. Navigate to dashboard. 3. Screenshot the full list. | All 5 badge colours render correctly on the same screen without colour bleed or overlap; row heights are consistent regardless of badge. | P1 |
| TC-3699 | Screenshot — editor toolbar (save, undo, redo, preview buttons) | 1. Open edition editor. 2. Make an edit to enable undo button. 3. Screenshot the editor toolbar area. | Toolbar renders with correctly labelled buttons: "Save" (active), "Undo" (enabled), "Redo" (disabled until undo is used), "Preview"; button states are visually distinct (enabled vs disabled); consistent with design system. | P1 |

---

### Section Q: Toast/Notification/State Feedback Tests (TC-3700 to TC-3729)

| TC | Title | Test Steps | Expected Result | Priority |
|----|-------|-----------|-----------------|----------|
| TC-3700 | Success toast — edition created | 1. Navigate to `/newsletters/editorial/new`. 2. Fill in edition subject "May Market Roundup". 3. Click "Create Edition". | A green success toast appears in the bottom-right: title "Edition Created", message "May Market Roundup has been added to your pipeline." Toast is visible for at least 4 s; edition appears in the dashboard list. | P0 |
| TC-3701 | Error toast — edition creation failure | 1. Mock the edition create API to return HTTP 500 (via DevTools request blocking or test environment). 2. Attempt to create a new edition. | A red error toast appears: title "Creation Failed", message "Unable to create edition. Please try again." Specific error code or details are included if available; the form remains populated for retry. | P0 |
| TC-3702 | Success toast — AI generation started | 1. Open a draft edition in the editor. 2. Click "Generate with AI" button. | An info or success toast appears: title "Generation Started", message "AI is writing your edition. This usually takes 20–40 seconds." A progress indicator also appears on the edition card. | P0 |
| TC-3703 | Progress notification — generation in progress | 1. Trigger AI generation per TC-3702. 2. Observe the UI during the generation window (20–40 s). | A persistent progress notification or banner remains visible with a live percentage counter or animated spinner; it does not auto-dismiss until generation completes or fails; the user can continue using the CRM while it runs. | P0 |
| TC-3704 | Success toast — generation complete | 1. Trigger AI generation; wait for it to complete successfully. | A green success toast fires: title "Generation Complete", message "Your edition is ready for review." A "Review Now" link inside the toast navigates to the editor; status badge updates to "ready". | P0 |
| TC-3705 | Error toast — generation failed with retry button | 1. Mock the AI generation endpoint to return an error after 5 s. 2. Trigger generation and wait. | A red error toast appears: title "Generation Failed", message explaining the failure (e.g. "AI service unavailable"); the toast includes a "Retry" button; clicking "Retry" re-triggers the generation without requiring page reload. | P0 |
| TC-3706 | Success toast — edition sent | 1. Open a "ready" edition. 2. Click "Send Now" and confirm in the confirmation dialog. 3. Wait for the send to complete. | A green success toast appears: title "Edition Sent", message "Your edition was delivered to [N] contacts." Status badge updates to "sent"; send count on the edition card updates. | P0 |
| TC-3707 | Error toast — send failure with details | 1. Mock the send endpoint to return HTTP 422 (e.g. no valid recipients). 2. Attempt to send a ready edition. | A red error toast appears: title "Send Failed", message with specific reason (e.g. "No contacts match the selected segment" or "Resend API error: [code]"); the edition status reverts to "ready" (not "sent"); user can fix and retry. | P0 |
| TC-3708 | Success toast — block saved manually | 1. Open edition editor. 2. Edit any block field. 3. Click the manual "Save" button (not auto-save). | A green success toast appears: title "Saved", message "Your changes have been saved." The auto-save indicator also updates to "Saved ✓"; dirty state clears. | P1 |
| TC-3709 | Error toast — block save conflict | 1. Open the same edition in two tabs. 2. In Tab A, edit block 2 headline and save. 3. In Tab B (which loaded before Tab A's save), edit the same block 2 headline to a different value and save. | A red or amber toast appears in Tab B: title "Save Conflict", message "This edition was updated by another session. Please reload to get the latest version."; a "Reload" button is present in the toast. | P1 |
| TC-3710 | Warning toast — unsaved changes on block operation | 1. Edit a block field (creating dirty state). 2. Click "Duplicate" on a different block before saving. | An amber warning toast appears: "You have unsaved changes. Save before duplicating?" with "Save First" and "Duplicate Anyway" actions; or the duplicate operation first auto-saves. | P2 |
| TC-3711 | Info toast — auto-save fired | 1. Edit any block field in the editor. 2. Wait for the auto-save debounce (≤3 s) without pressing Save. | An info toast or subtle indicator appears: "Auto-saved" or "Changes saved automatically" — this is low-prominence (grey/info style), not a bold success colour, to avoid distracting the user during active editing. | P1 |
| TC-3712 | Toast auto-dismiss — dismisses after 4 s | 1. Trigger a success toast (e.g. edition created). 2. Do not click the dismiss button. 3. Measure time with a stopwatch. | The toast automatically disappears (fade-out or slide-out animation) approximately 4 s after it appears; the DOM node is removed or hidden after dismissal. | P0 |
| TC-3713 | Toast manual dismiss — clicking ✕ closes toast immediately | 1. Trigger a success toast. 2. Within 1 s of the toast appearing, click the dismiss (✕) button on the toast. | The toast closes immediately with a smooth animation; it does not wait for the 4 s timer; no duplicate toasts are created. | P0 |
| TC-3714 | Toast stack — maximum 3 visible at once | 1. Trigger 5 consecutive toast-generating actions in rapid succession (e.g. save 5 blocks quickly via a script or rapid clicking). | A maximum of 3 toasts are visible simultaneously; older toasts are dismissed or queued as new ones arrive; toasts are stacked vertically in the bottom-right corner without overlapping. | P1 |
| TC-3715 | Toast position — bottom-right placement | 1. Trigger any toast (e.g. edition created). 2. Observe the toast position relative to the viewport. | Toast appears in the bottom-right corner of the viewport with consistent margin (e.g. 16–24 px from right edge and bottom edge); it does not appear at top, centre, or bottom-left. | P1 |
| TC-3716 | Edition status badge — updates in real time via polling | 1. Open the edition dashboard. 2. In a second tab, manually update an edition's status from "generating" to "ready" (e.g. via direct Supabase update or API call). 3. Wait up to 15 s without refreshing the first tab. | The edition's status badge on the dashboard updates from "generating" to "ready" automatically, driven by polling (or websocket); no manual page refresh required. | P0 |
| TC-3717 | Generation progress % — updates in real time | 1. Open the edition editor while AI generation is in progress. 2. Observe the progress indicator without refreshing. | The progress percentage increments (e.g. 0% → 25% → 50% → 75% → 100%) as the generation advances; updates arrive at least every 10 s; the UI does not freeze or require manual refresh. | P0 |
| TC-3718 | Send count — updates after confirmed send | 1. Open an edition with "send_count: 0" displayed. 2. Send the edition to 50 contacts. | After the send completes, the edition card or details panel shows "send_count: 50"; the count updates without a page refresh if polling is active, or is correct on next page load. | P1 |
| TC-3719 | Failed send count — UI update on partial failure | 1. Mock Resend to partially fail (e.g. 5 of 50 sends fail with bounce). 2. Send the edition. | The UI shows both sent and failed counts (e.g. "45 sent, 5 failed"); the edition status reflects the partial failure; a banner or toast details the failure count; failed addresses are accessible via a "View Failed" link. | P1 |
| TC-3720 | Consent warning banner — contacts missing CASL consent | 1. Create an edition with a segment that includes contacts who have `casl_consent_given = false`. 2. Navigate to the send step or edition detail. | A persistent amber warning banner is visible: "X contacts in this segment are missing CASL consent and will be excluded from the send." The banner includes a count and a "Review Contacts" link; it is not dismissible while the issue persists. | P0 |
| TC-3721 | CASL expiry warning toast | 1. Have at least 5 contacts with `casl_consent_date` older than 2 years (CASL implied consent expiry). 2. Open edition dashboard or newsletters section. | An amber toast or banner appears: "5 contacts have expired CASL consent. Their consent is over 2 years old." Includes a "Review" link to the contacts list filtered by expiry; appears once per session, not on every page load. | P0 |
| TC-3722 | Rate limit warning toast | 1. Simulate exceeding the Resend API rate limit (e.g. mock the API to return HTTP 429). 2. Attempt to send an edition. | An amber warning toast appears: "Send rate limit reached. Your edition will be sent in batches. Estimated completion: [time]." The edition is queued rather than failing; status shows "sending" or "queued". | P1 |
| TC-3723 | Network offline indicator | 1. Open the edition editor. 2. In DevTools, set the network to "Offline". 3. Observe the editor UI. | A persistent banner or badge appears: "You are offline. Changes will not be saved until your connection is restored." Auto-save is suspended; the save indicator shows "Offline" or a warning state. | P0 |
| TC-3724 | Reconnect success toast | 1. Set network to "Offline" per TC-3723 and wait for the offline indicator. 2. Restore the network (set back to "Online"). | A green success toast appears: "Back online. Saving your changes…"; auto-save resumes immediately; the offline indicator disappears; any queued saves are flushed. | P0 |
| TC-3725 | Toast — stale-while-revalidate data warning | 1. Open the edition list. 2. Mock the API to return a stale response (cache hit) and then an updated response 2 s later. | If the data changes between the stale and fresh response, an info toast appears: "Edition list updated." The list re-renders with the latest data; no page flash or blank state during revalidation. | P2 |
| TC-3726 | Edition locked toast — when another session holds edit lock | 1. Open edition editor in Tab A (takes lock). 2. Open the same edition in Tab B. | Tab B shows a toast or inline alert: "This edition is currently being edited in another window. View-only mode." Toast persists (no auto-dismiss) until lock is released or user navigates away. | P1 |
| TC-3727 | Toast — duplicate block success | 1. Click "Duplicate" on any block. | A subtle info or success toast appears: "Block duplicated." The new block appears below the original; the toast auto-dismisses after 3 s. | P2 |
| TC-3728 | Toast — block type switched warning | 1. Switch a fully-filled block from `hero` to `cta` using the type switcher (per TC-3650). | An amber warning toast appears before the switch is committed: "Switching block types will clear the current content. Continue?" with "Switch" and "Cancel" actions; if the user clicks "Switch", a separate "Block type changed" toast confirms the action. | P1 |
| TC-3729 | Toast — edition deleted confirmation | 1. Delete an edition from the dashboard (e.g. via a dropdown action → "Delete Edition"). 2. Confirm the deletion in the modal. | A success toast appears: "Edition deleted." The edition is removed from the dashboard list immediately; an "Undo" action is available in the toast for 5 s; clicking "Undo" within the window restores the edition. | P1 |

---

### Section R: Navigation and Routing Tests (TC-3730 to TC-3759)

| TC | Title | Test Steps | Expected Result | Priority |
|----|-------|-----------|-----------------|----------|
| TC-3730 | Route `/newsletters/editorial` loads correctly | 1. Ensure user is authenticated. 2. Navigate to `/newsletters/editorial`. | HTTP 200 response; page title includes "Editorial" or "Editions"; edition list (or empty state) renders within 3 s; no console errors; sidebar highlights the "Editorial" or "Newsletters" nav item. | P0 |
| TC-3731 | Route `/newsletters/editorial/new` loads create form | 1. Navigate to `/newsletters/editorial/new`. | Create edition form or modal renders; fields for edition subject, target segment, and scheduled date are visible; "Create Edition" and "Cancel" buttons present; page title is "New Edition". | P0 |
| TC-3732 | Route `/newsletters/editorial/[id]/edit` loads editor | 1. Note an existing edition ID from the dashboard. 2. Navigate to `/newsletters/editorial/[id]/edit` replacing `[id]` with the real UUID. | The edition editor loads with all blocks rendered; edition subject is displayed in the header; editor toolbar is visible; no 404 or redirect. | P0 |
| TC-3733 | Route `/newsletters/editorial/[id]/preview` loads preview | 1. Navigate to `/newsletters/editorial/[id]/preview` with a valid edition ID. | The read-only preview panel renders showing the full email layout; desktop/mobile viewport toggle is present; "Back to Editor" button navigates to the edit route; no 404. | P0 |
| TC-3734 | Back button from editor returns to dashboard | 1. Navigate to `/newsletters/editorial/[id]/edit`. 2. Click the browser's back button (or the in-app "← Back" button in the editor header). | User is returned to `/newsletters/editorial` (the edition dashboard); the edition list is visible; scroll position is restored if the user had scrolled before navigating to the editor. | P1 |
| TC-3735 | Breadcrumb navigation in editor | 1. Navigate to `/newsletters/editorial/[id]/edit`. 2. Locate the breadcrumb trail (e.g. "Newsletters > Editorial > [Edition Subject]"). 3. Click the "Editorial" breadcrumb segment. | Clicking the "Editorial" breadcrumb navigates to `/newsletters/editorial`; the unsaved-changes prompt fires if there are dirty edits (per TC-3652). | P1 |
| TC-3736 | Sidebar active state for editorial nav item | 1. Navigate to `/newsletters/editorial`. 2. Observe the sidebar navigation. | The "Newsletters" or "Editorial" sidebar nav item is in its active state: coral left-border indicator, slightly bolder text, or active background; no other nav item is simultaneously active. | P1 |
| TC-3737 | Deep link to specific edition opens correctly | 1. Copy the full URL from the browser while on `/newsletters/editorial/[id]/edit`. 2. Open a new browser tab and paste the URL. | The edition editor opens directly for the correct edition without requiring navigation from the dashboard; all blocks are populated with the edition's content; authentication is verified. | P0 |
| TC-3738 | 404 for non-existent edition ID | 1. Navigate to `/newsletters/editorial/00000000-0000-0000-0000-000000000000/edit` (a UUID that does not exist in the database). | A 404 error page renders (Next.js `not-found.tsx`) with a helpful message "Edition not found" and a link back to `/newsletters/editorial`; HTTP status is 404; no unhandled exception in the console. | P0 |
| TC-3739 | 403 redirect when accessing another tenant's edition | 1. Obtain the UUID of an edition belonging to a different `realtor_id` (multi-tenant test). 2. Attempt to navigate to `/newsletters/editorial/[other-tenant-id]/edit`. | User is redirected to `/newsletters/editorial` or shown a 403/404 error page; the other tenant's edition content is never rendered; no data leak; console shows no 200 response from the edition API for that ID. | P0 |
| TC-3740 | Redirect to login when session expires mid-edit | 1. Open edition editor and begin editing. 2. In another tab, invalidate the session (sign out or manually expire the NextAuth token/cookie). 3. Return to the editor tab and trigger an auto-save or API call. | The API call returns 401; the editor shows a "Session expired" banner or toast with a "Sign In Again" button; clicking the button redirects to `/login` with a `?callbackUrl=/newsletters/editorial/[id]/edit` parameter so the user returns after re-authenticating. | P0 |
| TC-3741 | Preserve editor scroll position on browser back/forward | 1. Navigate to edition editor and scroll to block 8 (near the bottom). 2. Navigate to the dashboard (browser forward). 3. Click back. | The editor re-opens with the scroll position preserved at block 8 (using `scrollRestoration: 'manual'` or Next.js scroll restoration); user does not have to scroll from the top again. | P1 |
| TC-3742 | router.push after send redirects to dashboard | 1. Send an edition (confirm in dialog). 2. After the send API returns success. | The application programmatically navigates to `/newsletters/editorial`; the dashboard shows the sent edition with updated status "sent"; no manual navigation needed by the user. | P1 |
| TC-3743 | router.push after edition created goes to editor | 1. Complete the "Create Edition" form at `/newsletters/editorial/new`. 2. Click "Create Edition". | After successful creation, the application navigates to `/newsletters/editorial/[new-id]/edit`; the new edition's editor opens with empty blocks or template blocks pre-populated; no return to the dashboard. | P0 |
| TC-3744 | Modal URL state — `?modal=voice-profile` opens modal on load | 1. Directly navigate to `/newsletters/editorial?modal=voice-profile` in the browser address bar. | The voice profile modal opens automatically when the page loads; no extra clicks required; the modal is fully rendered and interactive; the URL parameter is preserved in the address bar while the modal is open. | P1 |
| TC-3745 | Close modal updates URL — removes `?modal=` param | 1. Open the voice-profile modal per TC-3744. 2. Close the modal by clicking the ✕ button or pressing Escape. | The URL updates to `/newsletters/editorial` (without `?modal=voice-profile`); the modal closes with an animation; no page reload occurs; the edition list remains intact. | P1 |
| TC-3746 | Shareable preview link — public token generates unique URL | 1. Open an edition in the editor. 2. Click "Share Preview" or "Generate Preview Link" action. | A unique, token-signed URL is generated (e.g. `/newsletters/editorial/preview/[token]`); the URL is displayed or copied to clipboard; the link is accessible without authentication (public preview). | P1 |
| TC-3747 | Preview link expiry — expired token shows appropriate message | 1. Generate a preview token and manually expire it in the database (set `expires_at` to a past timestamp). 2. Navigate to the preview URL. | A message is displayed: "This preview link has expired. Please request a new link." HTTP status is 404 or 410 (Gone); no edition content is shown; the login page is not shown. | P1 |
| TC-3748 | Edition list pagination — `?page=2` loads second page | 1. Create more than the default page size of editions (e.g. 11 if page size = 10). 2. Navigate to `/newsletters/editorial?page=2`. | The second page of editions loads; the URL contains `?page=2`; pagination controls show page 2 as active; first-page editions are not visible; "Prev" link is active. | P1 |
| TC-3749 | Filter state in URL — `?status=draft` filters list | 1. Navigate to `/newsletters/editorial?status=draft`. | Only editions with `status = draft` are shown in the list; the status filter control in the UI reflects "Draft" as the active filter; navigating to this URL directly applies the filter without manual interaction. | P1 |
| TC-3750 | Filter state in URL — `?type=market_update` filters by edition type | 1. Navigate to `/newsletters/editorial?type=market_update`. | Only editions of type `market_update` are displayed; the type filter UI shows "Market Update" selected; the URL parameter persists on pagination (`?type=market_update&page=2`). | P2 |
| TC-3751 | Sort state in URL — `?sort=created_at&order=desc` | 1. Navigate to `/newsletters/editorial?sort=created_at&order=desc`. | Editions are sorted by creation date, newest first; the sort control reflects "Created (Newest)" as active; clicking a different column header updates both the sort and order URL parameters. | P1 |
| TC-3752 | Sort state in URL — `?sort=scheduled_at&order=asc` | 1. Navigate to `/newsletters/editorial?sort=scheduled_at&order=asc`. | Editions are sorted by scheduled send date, ascending (earliest first); sort control reflects this; URL updates when changed via the UI. | P2 |
| TC-3753 | Browser tab title — updates per route | 1. Navigate to `/newsletters/editorial` — note browser tab title. 2. Navigate to `/newsletters/editorial/[id]/edit` — note tab title. 3. Navigate to `/newsletters/editorial/[id]/preview` — note tab title. | Each route has a unique tab title: (1) "Editorial — Realtors360", (2) "[Edition Subject] — Editor — Realtors360", (3) "[Edition Subject] — Preview — Realtors360"; titles are set via Next.js `<title>` metadata. | P1 |
| TC-3754 | Canonical URL in metadata | 1. Navigate to `/newsletters/editorial/[id]/edit`. 2. Inspect the `<head>` tag in browser DevTools. | A `<link rel="canonical" href="https://[domain]/newsletters/editorial/[id]/edit" />` tag is present in the `<head>`; no duplicate canonical tags. | P2 |
| TC-3755 | 404 route for `/newsletters/editorial/[id]/unknown-sub-route` | 1. Navigate to `/newsletters/editorial/[valid-id]/something-invalid`. | A 404 error page renders; HTTP 404 status; message "Page not found"; link back to the edition dashboard. | P1 |
| TC-3756 | URL parameter validation — non-UUID edition ID | 1. Navigate to `/newsletters/editorial/not-a-valid-uuid/edit`. | The app renders a 404 or 400 error page; no database query is executed with the invalid ID; no unhandled exception; the error is handled gracefully in the Next.js route handler. | P1 |
| TC-3757 | Redirect — unauthenticated user accessing `/newsletters/editorial` | 1. Clear all session cookies. 2. Navigate to `/newsletters/editorial` directly in the browser. | User is redirected to `/login?callbackUrl=%2Fnewsletters%2Feditorial`; no CRM content is rendered; the NextAuth middleware protects the route. | P0 |
| TC-3758 | Navigation — sidebar "New Edition" quick-action button | 1. Navigate to any CRM page (e.g. `/`). 2. Locate and click the "New Edition" shortcut in the sidebar (if implemented as a pinned action under Newsletters). | User is navigated to `/newsletters/editorial/new`; the create edition form loads; the URL reflects the new route; the sidebar newsletters group is expanded/active. | P2 |
| TC-3759 | Route transition — no flash of unauthenticated content | 1. While authenticated, navigate between `/newsletters/editorial` and `/newsletters/editorial/[id]/edit` multiple times rapidly (e.g. click Back and Forward repeatedly 5 times). | No blank page, unauthenticated state flash, or "loading" flicker that reveals unstyled HTML; the Next.js App Router handles the transitions with appropriate loading states; no race-condition rendering of two different editions simultaneously. | P1 |
