<!-- docs-audit: none --># QA Expansion — Sections AC through AH: Accessibility, Personalization, Analytics, Settings, Load, Regression
## Test Range: TC-4115 to TC-4414
## Feature: Editorial Newsletter System

---

### Section AC: Accessibility Deep Tests (TC-4115 to TC-4164)

| TC | Test Title | Test Steps | Expected Result | Priority |
|----|-----------|-----------|----------------|----------|
| TC-4115 | Screen reader announces edition list with role="list" | 1. Open NVDA (Windows) or VoiceOver (macOS). 2. Navigate to /newsletters/editorial. 3. Focus the editions list container. 4. Listen to the screen reader announcement. | Screen reader announces "list" role and item count (e.g., "list, 12 items") when entering the editions list container. The DOM element has role="list" explicitly set. | High |
| TC-4116 | Screen reader reads edition status badge as "Status: Generating" | 1. Enable VoiceOver. 2. Navigate to an edition with status "generating". 3. Tab to or hover over the status badge. 4. Listen to announcement. | Screen reader announces "Status: Generating" — the badge renders visible text "Generating" and an accessible prefix "Status:" via aria-label or visually-hidden span, not just a colored dot. | High |
| TC-4117 | Screen reader announces generation progress percentage and block name | 1. Enable NVDA. 2. Trigger AI generation on a 10-block edition. 3. Wait for the progress bar/indicator to update. 4. Listen for live region announcement. | Screen reader announces "40% complete, generating market commentary block" (or current block name) via aria-live="polite" region updating in real time as generation progresses. | High |
| TC-4118 | All drag-and-drop reordering has keyboard alternative move-up/down buttons | 1. Open edition editor with 5 blocks. 2. Disable mouse (keyboard-only mode or test with Tab only). 3. Attempt to reorder a block using keyboard. 4. Verify move-up/move-down buttons are present and functional. | Each block has visible "Move Up" and "Move Down" buttons operable via Tab+Enter. Block order changes correctly when activated. No drag-and-drop required for reordering. | High |
| TC-4119 | Move up/down buttons visible and operable via Tab+Enter | 1. Use keyboard only (Tab to navigate). 2. Navigate to a block in the edition editor. 3. Press Tab until "Move Up" or "Move Down" button is focused. 4. Press Enter. | Button receives focus with visible focus ring. Pressing Enter moves the block one position. Focus follows the moved block to its new position. | High |
| TC-4120 | Drag handles have aria-label="Drag to reorder [block type]" | 1. Inspect DOM for drag handle elements in the edition editor block list. 2. Check aria-label attribute on each drag handle icon/button. | Each drag handle has aria-label="Drag to reorder [block_type]" where [block_type] is the specific type (e.g., "Drag to reorder hero block", "Drag to reorder market commentary block"). | Medium |
| TC-4121 | Block editor form fields have associated label elements | 1. Open edition editor and select any block for editing. 2. Inspect DOM of the editor panel. 3. Check each input/textarea/select for a corresponding <label> element with matching htmlFor/id. | Every form control in the block editor panel has an associated <label> element linked via htmlFor/id. No inputs rely solely on placeholder text for identification. | High |
| TC-4122 | Hero headline input has aria-label and aria-required="true" | 1. Open a hero block in the edition editor. 2. Inspect the headline input field in the DOM. 3. Check aria-label and aria-required attributes. | Hero headline input has aria-label="Hero block headline" and aria-required="true" set on the input element. | High |
| TC-4123 | All required fields marked with aria-required="true" | 1. Open the edition editor. 2. Inspect all mandatory fields (title, from-name, subject line, etc.). 3. Verify aria-required attribute on each. | All required fields have aria-required="true" set. Optional fields do not have aria-required or have aria-required="false". | High |
| TC-4124 | Error messages linked via aria-describedby to their input | 1. Submit the edition save form with an empty required field. 2. Inspect the DOM after validation error appears. 3. Check aria-describedby on the input and the id of the error message element. | The input's aria-describedby value matches the id of the visible error message element. NVDA/VoiceOver reads the error message when the invalid input is focused. | High |
| TC-4125 | Focus trapped in delete-edition confirmation modal | 1. Click "Delete Edition" for any edition. 2. Confirmation modal appears. 3. Press Tab repeatedly. 4. Verify focus cycles only within the modal. | Focus cycles only between focusable elements inside the modal (Cancel button, Confirm button). Tab from the last element returns to the first. Focus does not escape to elements behind the modal. | High |
| TC-4126 | Focus trapped in delete-block confirmation modal | 1. Open edition editor. 2. Click delete on any block. 3. Confirmation modal appears. 4. Press Tab repeatedly. | Focus cycles only within the delete-block confirmation modal. No focus escapes to the block list or editor panel behind the modal. | High |
| TC-4127 | Escape key closes all modals and returns focus to trigger element | 1. Open any modal (delete edition, delete block, send confirmation). 2. Press Escape key. | Modal closes immediately. Focus returns to the exact element that triggered the modal (e.g., the Delete button). Page state is unchanged. | High |
| TC-4128 | Focus moves to first block after "Add Block" action | 1. Open the edition editor. 2. Click "Add Block" and select a block type. 3. Observe focus position after block is inserted. | After the new block is added to the list, keyboard focus moves to the first interactive element of the newly added block (e.g., its edit button or block title). | Medium |
| TC-4129 | Focus moves to next block after deleting a block | 1. Open edition editor with 3+ blocks. 2. Delete the second block via keyboard (Tab to delete, Enter to confirm). 3. Observe focus position after deletion. | After deletion, focus moves to the next block in the list (what was block 3 becomes block 2). If the last block is deleted, focus moves to the previous block. Focus does not return to document body. | High |
| TC-4130 | Edition list table has aria-label="Editorial newsletter editions" | 1. Navigate to /newsletters/editorial. 2. Inspect the DOM of the editions list/table element. 3. Check the aria-label attribute. | The table or list element has aria-label="Editorial newsletter editions" (or equivalent). Screen reader announces this label when entering the table. | Medium |
| TC-4131 | Table column headers use scope="col" | 1. Inspect DOM of the editions table at /newsletters/editorial. 2. Check all <th> elements in the header row. | Every column header <th> element has scope="col" attribute. Row headers (if any) have scope="row". | Medium |
| TC-4132 | Table rows have aria-label describing edition title and status | 1. Inspect DOM of the editions table rows. 2. Check aria-label on each <tr> or the row's primary cell. | Each row has aria-label="[Edition title], status [status]" (e.g., "April Market Update, status Draft"). Screen reader announces this when navigating rows. | Medium |
| TC-4133 | Sort buttons announce current sort direction | 1. Enable screen reader. 2. Click "Sort by Date" column header. 3. Focus the sort button. 4. Listen to announcement. | Sort button announces "Sort by date, currently ascending" (or descending). aria-sort="ascending" or aria-sort="descending" is set on the active sort column header. | Medium |
| TC-4134 | Preview iframe has title="Email preview" | 1. Open any edition in the editor. 2. Navigate to the preview tab or panel. 3. Inspect the iframe element in DOM. | The iframe element has title="Email preview" attribute. Screen reader announces "Email preview, frame" when entering the iframe. | High |
| TC-4135 | Preview iframe does not trap keyboard focus | 1. Navigate to the preview panel. 2. Tab into the preview iframe. 3. Continue pressing Tab. | After entering the preview iframe, Tab eventually moves focus out of the iframe to the next element in the page (the close/back button). Focus is not permanently trapped in the iframe. | High |
| TC-4136 | Generation status not conveyed by color alone | 1. Inspect all status badge variants (Draft, Generating, Ready, Sent, Failed). 2. View in grayscale simulation. 3. Verify each status has both a text label and optional icon. | Each status badge displays both a text label (e.g., "Generating") and optionally a distinguishing icon. Removing color does not eliminate the ability to distinguish statuses. | High |
| TC-4137 | CASL warning not conveyed by color alone | 1. Trigger a CASL warning (e.g., attempt to send to contact with no consent). 2. Inspect the warning element. | CASL warning displays a warning icon (⚠ or equivalent) plus descriptive text. Removing color from the display still makes the warning identifiable. | High |
| TC-4138 | All interactive elements have minimum 44x44px touch target | 1. Open the edition editor on a mobile viewport (375px). 2. Inspect clickable elements (buttons, drag handles, delete icons) using DevTools. 3. Measure computed width and height. | Every interactive element (buttons, icon buttons, drag handles) has a touch target of at least 44x44 CSS pixels as per WCAG 2.5.5 (AAA target). Minimum AA: 24x24px with offset. | High |
| TC-4139 | Voice control "Click Generate Content" activates the generate button | 1. Use Dragon NaturallySpeaking (or simulate accessible name lookup). 2. Say "Click Generate Content". 3. Observe which button activates. | The "Generate Content" button has a visible label or accessible name exactly matching "Generate Content". Voice control successfully activates it. | Medium |
| TC-4140 | Voice control "Click Save Edition" activates save button | 1. Use Dragon NaturallySpeaking or verify button accessible name. 2. Say "Click Save Edition". | The save button has accessible name "Save Edition". Voice control activates it correctly. The edition save action fires. | Medium |
| TC-4141 | Voice control "Click Delete Block" activates delete with confirmation | 1. Focus a block in the editor. 2. Say "Click Delete Block" via Dragon NaturallySpeaking. 3. Observe confirmation dialog appearance. | The delete button has accessible name "Delete Block". Voice control activates it. Confirmation dialog appears (not silent deletion). | Medium |
| TC-4142 | High contrast mode: all text remains readable | 1. Enable Windows High Contrast Mode (or forced-colors: active CSS). 2. Navigate through /newsletters/editorial and the edition editor. 3. Inspect all text elements. | All text is readable in high contrast mode. No white-on-white, black-on-black, or same-as-background text occurs. Custom CSS colors fall back correctly to system colors. | High |
| TC-4143 | High contrast mode: CTA button remains visible | 1. Enable Windows High Contrast Mode. 2. Locate CTA buttons (Generate, Save, Send). | CTA buttons are clearly visible with legible text in high contrast mode. Border or background adapts to system highlight color. Button text does not disappear. | High |
| TC-4144 | Reduced motion: drag animation disabled | 1. Set OS to "Reduce Motion" (macOS: Accessibility > Display > Reduce Motion). 2. Open edition editor. 3. Attempt to drag-and-drop a block. | CSS transition/animation for drag movement is disabled or significantly reduced when prefers-reduced-motion: reduce is active. Block snaps to position without sliding animation. | High |
| TC-4145 | Reduced motion: generation progress bar has no CSS animation | 1. Set OS to Reduce Motion. 2. Trigger AI generation on an edition. 3. Inspect the progress bar CSS while generation is in progress. | Progress bar does not use a pulsing, spinning, or looping CSS animation when prefers-reduced-motion: reduce is active. The progress value updates as a plain value change without animated fill. | High |
| TC-4146 | WCAG 2.1 AA: all badge variant text meets 4.5:1 contrast ratio | 1. Use browser DevTools accessibility panel or a contrast checker. 2. Check each badge variant: Draft (gray), Generating (blue), Ready (green), Sent (teal), Failed (red). | Every badge variant's text color against its background color meets a minimum contrast ratio of 4.5:1. Document any failures with exact hex values. | High |
| TC-4147 | WCAG 2.1 AA: large text (18px+) meets 3:1 contrast ratio | 1. Identify all text elements 18px or larger (or 14px bold). 2. Check contrast ratio for each. | All large text meets minimum 3:1 contrast ratio against its background. This includes page headings, edition titles, and block type labels. | High |
| TC-4148 | Skip-to-content link present and navigates to main content | 1. Load /newsletters/editorial. 2. Press Tab as the first keyboard action after page load. 3. Observe the first focusable element. | A "Skip to main content" link appears as the first focusable element. Activating it moves focus to the main content area (bypassing navigation). | High |
| TC-4149 | Tab order in editor follows logical reading order | 1. Open edition editor. 2. Start from the page top and press Tab repeatedly. 3. Record focus sequence. | Tab order goes: page header → block list (top to bottom) → block editor panel → footer action buttons. Order matches visual layout. No focus jumps illogically across the page. | High |
| TC-4150 | Reverse-tab order mirrors forward tab order | 1. Open edition editor. 2. Start from footer actions and press Shift+Tab repeatedly. 3. Record focus sequence. | Shift+Tab traverses elements in exact reverse of Tab order. No element is only reachable in one direction. | Medium |
| TC-4151 | No keyboard trap on block type dropdown | 1. Open "Add Block" dropdown in the editor. 2. Use keyboard to navigate options. 3. Press Escape. | Pressing Escape closes the dropdown and returns focus to the "Add Block" trigger button. Tab from the dropdown does not trap focus. Focus escapes naturally. | High |
| TC-4152 | Autocomplete off on block content fields | 1. Inspect block content textarea/input elements in the DOM. 2. Check autocomplete attribute. | Block content fields have autocomplete="off" set to prevent browser autocomplete from overlaying AI-generated content suggestions. | Low |
| TC-4153 | Voice profile wizard all 5 steps are keyboard-navigable | 1. Open voice profile setup wizard at /newsletters/editorial/voice-profile. 2. Complete all 5 steps using keyboard only (Tab, Enter, arrow keys). | All 5 steps are fully operable via keyboard. Each step's controls (text inputs, radio buttons, checkboxes, next/back buttons) are reachable and activatable with Tab+Enter/Space. | High |
| TC-4154 | Voice profile wizard progress indicator reads "Step 3 of 5" to screen reader | 1. Enable VoiceOver. 2. Navigate to step 3 of the voice profile wizard. 3. Focus the progress indicator (stepper). | Screen reader announces "Step 3 of 5" or equivalent. aria-label or visually-hidden text provides this information. The visual stepper dots/numbers alone are insufficient. | High |
| TC-4155 | Send confirmation modal has role="alertdialog" and aria-modal="true" | 1. Click "Send Edition" to trigger the send confirmation modal. 2. Inspect the modal container element in DOM. | Modal element has role="alertdialog" and aria-modal="true". aria-labelledby points to the modal's heading. aria-describedby points to the modal's body text. | High |
| TC-4156 | CASL gate modal announced as alert when shown | 1. Attempt to send to a contact list that has expired CASL consent. 2. CASL gate modal appears. 3. Enable screen reader. | Screen reader immediately announces the CASL warning text when the modal appears, using role="alert" or aria-live="assertive" on the modal. No user action required to trigger the announcement. | High |
| TC-4157 | Edition title field minimum 200px width on mobile | 1. Open edition editor on 375px viewport (mobile). 2. Locate the edition title input field. 3. Measure its rendered width in DevTools. | Edition title input field renders at a minimum of 200px wide on mobile viewport. Text is not clipped or overflowing its container. | Medium |
| TC-4158 | Mobile: pinch-zoom not disabled | 1. Load /newsletters/editorial on mobile device or emulated mobile viewport. 2. Inspect the <meta name="viewport"> tag. | The viewport meta tag does not include user-scalable=no or maximum-scale=1. Users can pinch-zoom freely as required by WCAG 1.4.4. | High |
| TC-4159 | Print stylesheet: removes navigation, shows only edition content | 1. Open an edition in the editor. 2. Use browser print preview (Cmd+P or Ctrl+P). 3. Inspect the rendered print view. | Print view hides the sidebar navigation, top header, and action buttons. Only the edition title, metadata, and block content are printed. A @media print stylesheet accomplishes this. | Low |
| TC-4160 | Print view: blocks print with borders instead of background colors | 1. Open a multi-block edition. 2. Use browser print preview. 3. Check block visual separation. | In print view, blocks are separated by borders rather than background colors (which may not print). Edition content is clearly structured and readable when printed in black and white. | Low |
| TC-4161 | PDF export produces accessible tagged PDF | 1. If PDF export feature is implemented, export an edition as PDF. 2. Open PDF in Adobe Acrobat. 3. Run Accessibility Checker. | PDF passes Acrobat accessibility check: tagged headings, reading order defined, images have alt text, document language set. If PDF export is not implemented, TC is marked N/A. | Low |
| TC-4162 | HTML lang attribute set correctly per locale | 1. Load /newsletters/editorial. 2. Inspect the <html> element in DOM. 3. Check lang attribute value. | <html> element has lang="en" (or "fr" if French locale active). Screen readers use this to select correct pronunciation rules. | High |
| TC-4163 | Time elements use datetime attribute with ISO-8601 format | 1. Inspect all date displays in the edition list and editor (created_at, sent_at, scheduled_for). 2. Check DOM for <time> elements. | All dates rendered in the UI use <time datetime="2026-04-15T14:30:00Z"> format. The datetime attribute contains a machine-readable ISO-8601 timestamp. Display text may be human-readable ("April 15, 2026"). | Medium |
| TC-4164 | Focus ring visible on all interactive elements | 1. Using keyboard only, Tab through all interactive elements on /newsletters/editorial and the edition editor. 2. Verify focus ring is visible at each stop. | Every focused element displays a visible focus ring or focus indicator with sufficient contrast (at least 3:1 against adjacent colors, WCAG 2.4.11). No outline: none without a replacement focus style. | High |

---

### Section AD: Content Personalization Engine Tests (TC-4165 to TC-4214)

| TC | Test Title | Test Steps | Expected Result | Priority |
|----|-----------|-----------|----------------|----------|
| TC-4165 | AI reads contact.newsletter_intelligence before generating edition | 1. Create a contact with newsletter_intelligence = {"interests": ["condos"], "engagement_score": 85}. 2. Create an edition targeting that contact. 3. Trigger AI generation. 4. Inspect the Anthropic API request payload in server logs. | The generation system prompt includes the contact's newsletter_intelligence data. The log entry or debug output shows the JSONB data was retrieved and injected into the prompt before the Anthropic API call. | High |
| TC-4166 | High-engagement contacts get longer hero copy | 1. Create a contact with newsletter_intelligence.engagement_score = 85. 2. Generate an edition for a segment containing only this contact. 3. Inspect the generated hero block content field. | The hero block copy is 120 words or more. Generation prompt instructs Claude to write longer, more detailed copy for high-engagement (80+) contacts. | High |
| TC-4167 | Low-engagement contacts get shorter punchier hero copy | 1. Create a contact with newsletter_intelligence.engagement_score = 20. 2. Generate an edition targeting only this contact. 3. Inspect the generated hero block content field. | The hero block copy is between 60 and 80 words. Generation prompt instructs Claude to write concise, punchy copy for low-engagement (under 30) contacts. | High |
| TC-4168 | Contact interest "condos" causes hero to focus on condo market stats | 1. Create a contact with newsletter_intelligence.interests = ["condos"]. 2. Generate edition. 3. Inspect hero block body text. | The generated hero block references condo-specific market data (e.g., "Vancouver condo benchmark rose 2.3%"). The word "condo" or "condominium" appears in the hero copy. | High |
| TC-4169 | Contact interest "detached homes" causes market commentary to skip condo data | 1. Create contact with newsletter_intelligence.interests = ["detached homes"]. 2. Generate edition. 3. Inspect market_commentary block text. | Market commentary block text focuses on detached home statistics. Condo-specific data is absent or deprioritized. Generation prompt includes conditional instruction based on interest type. | High |
| TC-4170 | Contact location "Kitsilano" causes neighbourhood spotlight to default to Kitsilano | 1. Create contact with newsletter_intelligence.preferred_neighbourhood = "Kitsilano". 2. Generate edition with a neighbourhood_spotlight block type. 3. Inspect block content. | Neighbourhood spotlight block content is about Kitsilano. Location name "Kitsilano" appears in block text. The AI used the contact's preferred neighbourhood as the spotlight location. | High |
| TC-4171 | Contact price range "$600K–$900K" causes just_sold examples in that range | 1. Create contact with newsletter_intelligence.price_range = {"min": 600000, "max": 900000}. 2. Generate edition with just_sold block. 3. Inspect just_sold block content. | The just_sold block examples feature properties sold in the $600K–$900K range. Properties far outside this range (e.g., $2M+) are not included. | High |
| TC-4172 | Active buyer journey stage gets "Book a Showing" CTA | 1. Create contact with newsletter_intelligence.buyer_journey_stage = "active". 2. Generate edition with CTA block. 3. Inspect CTA block button_text field. | CTA block button_text is "Book a Showing" (not "Start Your Search" or generic CTA). CTA link points to booking flow. | High |
| TC-4173 | Lead buyer journey stage gets "Get Your Free Market Report" CTA | 1. Create contact with newsletter_intelligence.buyer_journey_stage = "lead". 2. Generate edition with CTA block. 3. Inspect CTA block button_text. | CTA block button_text is "Get Your Free Market Report". CTA link directs to market report request form or equivalent. | High |
| TC-4174 | Past client contact type gets relationship-acknowledging hero copy | 1. Create contact with type = "past_client". 2. Generate edition for this contact. 3. Inspect hero block copy. | Hero block includes relationship acknowledgment language such as "As one of our valued clients…" or "As someone we've had the pleasure of working with…". Generic first-time language is absent. | High |
| TC-4175 | Agent note block content differs for buyer vs seller contact | 1. Generate edition for a buyer contact. 2. Generate edition for a seller contact. 3. Compare the agent_note block text in each. | Agent note block text for buyers focuses on buying opportunities, market timing, or neighbourhood insights. For sellers, it focuses on listing performance, equity, or seller market conditions. Content is not identical. | High |
| TC-4176 | Market commentary adjusts tone for buyer-heavy vs seller-heavy segment | 1. Create segment A with 10 buyer contacts. 2. Create segment B with 10 seller contacts. 3. Generate edition for each. 4. Compare market_commentary block tone. | Segment A (buyers) market commentary uses buyer-friendly framing ("inventory is increasing, giving buyers more options"). Segment B (sellers) uses seller-friendly framing ("demand remains strong, sellers seeing multiple offers"). | Medium |
| TC-4177 | Rate watch block uses "good time to buy" framing for buyer contacts | 1. Create buyer contact. 2. Generate edition with rate_watch block. 3. Inspect block content. | Rate watch block text includes buyer-positive framing such as "current rates present an opportunity for buyers to lock in" or equivalent. Language encourages purchase action. | Medium |
| TC-4178 | Rate watch block uses "lock in equity" framing for seller contacts | 1. Create seller contact. 2. Generate edition with rate_watch block. 3. Inspect block content. | Rate watch block text includes seller-positive framing such as "now is the time to leverage your equity before rates shift further" or equivalent. | Medium |
| TC-4179 | A/B subject variant A uses property-specific headline | 1. Enable A/B testing. 2. Generate an edition with A/B subject test. 3. Inspect ab_variants.subject_a in the DB or UI. | Subject variant A contains a property-specific headline referencing an address, price, or neighbourhood (e.g., "3 new listings under $800K in Kitsilano this week"). | High |
| TC-4180 | A/B subject variant B uses question format | 1. Enable A/B testing. 2. Generate edition with A/B subject test. 3. Inspect ab_variants.subject_b in DB or UI. | Subject variant B is phrased as a question (e.g., "Is now the right time to buy in Kitsilano?" or "Ready to find your next home?"). Ends with a question mark. | High |
| TC-4181 | A/B winner auto-selected after 100 opens in 48-hour window | 1. Send A/B edition. 2. Simulate 60 opens on variant A, 40 opens on variant B within 48 hours. 3. Wait for winner selection cron. 4. Check newsletters row. | newsletters.active_variant_id is updated to variant A's ID. The winner is the variant with the higher open rate after minimum 100 total opens in the 48-hour window. | High |
| TC-4182 | Winner selection updates active_variant_id on newsletters row | 1. After winner is selected (TC-4181). 2. Query newsletters table for the edition row. | newsletters.active_variant_id = ID of winning variant. newsletters.ab_winner_selected_at timestamp is set. | High |
| TC-4183 | Contacts who opened variant A have variant A tracked in newsletter_events | 1. Send A/B edition. 2. Contact opens variant A email. 3. Resend open webhook fires. 4. Query newsletter_events. | newsletter_events row has edition_id, contact_id, event_type = "open", variant = "a". Variant A contacts are never tagged as variant B openers. | High |
| TC-4184 | Contacts who opened variant B have variant B tracked in newsletter_events | 1. Send A/B edition. 2. Contact opens variant B email. 3. Resend open webhook fires. 4. Query newsletter_events. | newsletter_events row has variant = "b". Contacts sent variant B are tracked under variant B. Cross-variant tracking does not occur. | High |
| TC-4185 | Personalization variables interpolated correctly in preview | 1. Open an edition that includes {{first_name}}, {{neighbourhood}}, and {{price_range}} variables. 2. Open the preview panel for a contact with all three values set. | Preview renders "Hi Jane," not "Hi {{first_name}}," — all three variables are replaced with the contact's actual values in the preview panel. | High |
| TC-4186 | Missing {{first_name}} falls back to "there" | 1. Create a contact with no first name. 2. Generate and preview an edition for this contact. 3. Find the greeting line in the preview. | "Hi {{first_name}}," renders as "Hi there," in the preview and in the actual sent email. No raw template variable is visible in the output. | High |
| TC-4187 | Missing {{neighbourhood}} falls back to "your area" | 1. Create a contact with no preferred_neighbourhood. 2. Preview edition. 3. Find neighbourhood reference in preview. | {{neighbourhood}} renders as "your area" (e.g., "Market trends in your area"). No raw variable text visible. | High |
| TC-4188 | Personalization applied per-recipient at send time, not at draft time | 1. Create edition with {{first_name}} variable. 2. Save draft (do not send). 3. Change contact's first name. 4. Send edition. 5. Check received email. | The sent email uses the contact's first name as it existed at send time, not at draft-save time. Template variables are resolved at the moment of send, not earlier. | High |
| TC-4189 | Contact with no newsletter_intelligence uses generic market defaults | 1. Create contact with newsletter_intelligence = null or {}. 2. Generate edition. 3. Inspect all blocks. | Generation completes without errors. All blocks use generic market defaults (no personalized tone, standard CTA, citywide market data). No null reference errors in generation logs. | High |
| TC-4190 | Contact interest updated from click updates next edition content | 1. Contact clicks "condo listings" link in an edition. 2. Click webhook updates newsletter_intelligence.interests to include "condos". 3. Generate next edition for this contact. 4. Inspect hero block. | Next edition's hero block references condo market data. The interest update from the previous click is reflected in the new edition's content. | High |
| TC-4191 | High click-through rate on "Open Houses" triggers open_house_invite block suggestion | 1. Set a contact's newsletter_intelligence.click_history to show 5+ clicks on open house links. 2. Generate new edition. 3. Inspect block composition suggestion. | The AI generation suggests or automatically includes an open_house_invite block type. The suggestion is visible in the edition editor as an added block or a recommendation. | Medium |
| TC-4192 | Zero opens after 3 editions flags contact for re-engagement | 1. Send 3 editions to a contact. 2. Simulate zero opens for all 3 (no open webhook fires). 3. Check send advisor output or dashboard recommendations. | Send advisor flags the contact as "consider re-engagement" or equivalent warning. This flag is visible in the contact detail page or the send advisor recommendations widget. | Medium |
| TC-4193 | Realtor manual edits to blocks lock the block from AI regeneration | 1. Open edition editor. 2. Manually edit the text of the hero block. 3. Save. 4. Click "Re-generate" for the edition. | The manually edited hero block is NOT regenerated. All other blocks are regenerated. The hero block retains the realtor's manual text. A lock icon is visible on the block. | High |
| TC-4194 | Locked blocks survive edition re-generate cycle | 1. Lock a block (manual edit). 2. Click "Re-generate All" on the edition. 3. After regeneration completes, inspect the locked block content. | Locked block content is identical to what the realtor manually entered. The re-generate action skipped this block. Lock icon still present after regeneration. | High |
| TC-4195 | Lock icon visible when block is manually edited | 1. Open edition editor. 2. Edit hero block text. 3. Save the block. 4. Return to edition block list. | A lock icon (🔒 or padlock SVG) is visible on the hero block card in the block list, indicating it is manually locked and will not be regenerated. | Medium |
| TC-4196 | Voice profile tone "luxury" generates formal vocabulary in market commentary | 1. Create voice profile with tone = "luxury". 2. Generate market_commentary block. 3. Inspect generated text. | Market commentary uses formal, elevated vocabulary (e.g., "prestige properties", "discerning buyers", "prime real estate") rather than casual language. No slang or informal contractions. | High |
| TC-4197 | Voice profile tone "friendly" generates casual language in agent note | 1. Create voice profile with tone = "friendly". 2. Generate agent_note block. 3. Inspect text. | Agent note uses casual, warm language (e.g., "Hey!", "It's been a great month!", "Let's grab a coffee and chat"). Contractions and first names used freely. | High |
| TC-4198 | Voice profile signature phrase injected verbatim in agent note | 1. Set voice profile signature_phrase = "Talk soon and happy hunting! — Dave". 2. Generate edition. 3. Inspect end of agent_note block. | The exact phrase "Talk soon and happy hunting! — Dave" appears at the end of the agent_note block content, character-for-character. It is not paraphrased or modified. | High |
| TC-4199 | Voice profile sample email extracts 5+ writing rules via Claude Haiku | 1. Paste a 500-word sample email into the voice profile wizard. 2. Trigger voice rule extraction. 3. Inspect editorial_voice_profiles.voice_rules in DB. | voice_rules JSONB array contains at least 5 distinct writing rules extracted from the sample (e.g., "uses short sentences", "always opens with a question", "avoids jargon"). Extraction uses Claude Haiku model. | High |
| TC-4200 | Voice rules stored in editorial_voice_profiles.voice_rules JSONB array | 1. After extraction (TC-4199). 2. Query editorial_voice_profiles WHERE realtor_id = [current realtor]. | Row exists with voice_rules as a JSONB array of string rules. Each element is a short, actionable writing instruction (not the full sample text). | High |
| TC-4201 | Voice rules injected into system prompt for all block generation | 1. Enable verbose logging or inspect generation server logs. 2. Trigger any block generation. 3. Check the system prompt sent to Anthropic API. | System prompt includes a section like "Writing rules:" followed by all rules from the realtor's voice_rules array. Rules are present before the generation instruction. | High |
| TC-4202 | Voice rule "avoid passive voice" results in active constructions | 1. Add voice rule "avoid passive voice" to profile. 2. Generate market_commentary block. 3. Inspect generated text grammatically. | Generated market commentary uses active voice constructions (e.g., "Buyers snapped up 47 homes" not "47 homes were snapped up by buyers"). Passive voice constructions are rare or absent. | Medium |
| TC-4203 | Voice rule "always include a statistic" results in at least one number per market block | 1. Add voice rule "always include a statistic". 2. Generate 3 market_commentary blocks. 3. Verify each for numeric content. | All 3 generated market_commentary blocks contain at least one numeric statistic (e.g., "up 3.2%", "$847,000 benchmark", "23 days on market"). | Medium |
| TC-4204 | Voice profile update triggers fresh rule extraction | 1. Load existing voice profile. 2. Replace sample email with new sample. 3. Save. 4. Query DB for voice_rules. | voice_rules JSONB array is updated to reflect rules extracted from the new sample. Old rules are replaced. version column increments by 1. updated_at timestamp is fresh. | High |
| TC-4205 | Voice profile version increments on update | 1. Save voice profile version 1. 2. Update profile. 3. Query version column. | version column value is 2 (incremented by 1 from 1 to 2). Each save increments. version is never decremented. | Medium |
| TC-4206 | Rollback to previous voice profile version possible via API | 1. Update voice profile to version 2. 2. Call POST /api/editorial/voice-profile/rollback with {"version": 1}. 3. Query DB. | voice_rules reverts to version 1 content. Current version is set back to 1 (or a new version 3 is created with version 1 content for auditability). | Low |
| TC-4207 | CTA block can have per-block voice tone override | 1. Set global voice tone to "luxury". 2. Edit CTA block and set per-block tone override to "friendly". 3. Regenerate CTA block only. | CTA block regenerates with friendly/casual language regardless of the global luxury tone setting. Other blocks regenerate with the global luxury tone. Per-block override takes precedence. | Medium |
| TC-4208 | Accepted realtor edit triggers voice learning | 1. AI generates hero block. 2. Realtor edits the hero block and saves (without reverting). 3. After save, check voice learning pipeline output. | System detects the edit delta between AI-generated and final content. Positive example is stored. New writing rules are extracted from the delta and appended to voice_rules. | High |
| TC-4209 | Reverted realtor edit does not update voice rules | 1. AI generates hero block. 2. Realtor edits block then immediately reverts to AI-generated version. 3. Check voice_rules after revert. | Reverted edits are flagged as rejected. voice_rules array is NOT updated. A negative_examples array stores the rejected edit for future model fine-tuning. No new rules appended. | High |
| TC-4210 | Content library reuses saved blocks for matching context | 1. Save a neighbourhood_spotlight block for "Kitsilano" from a previous edition to content library. 2. Generate new edition targeting Kitsilano, same month. 3. Check if content library block is suggested. | Editor displays a suggestion: "We found a similar block from [previous edition]" with an "Use This Block" option. Suggestion only appears when similarity_score exceeds 0.85 threshold. | Medium |
| TC-4211 | Content library match requires similarity score > 0.85 | 1. Save a "Burnaby" neighbourhood block to library. 2. Generate "Kitsilano" edition. 3. Check if content library block is suggested. | No suggestion appears for the Burnaby block because neighbourhood context differs significantly (similarity_score < 0.85 for different neighbourhood names). Only highly relevant blocks are surfaced. | Medium |
| TC-4212 | Personalization does not bleed between contacts in same batch send | 1. Send edition to segment with contacts "Alice (buyer, Kitsilano)" and "Bob (seller, Burnaby)". 2. Inspect emails received by both. | Alice's email contains buyer-focused Kitsilano content. Bob's email contains seller-focused Burnaby content. Alice's personalization does not appear in Bob's email and vice versa. | High |
| TC-4213 | A/B winner promotion emails remaining contacts with winning variant | 1. Send A/B edition to 200 contacts (20% test = 40 contacts). 2. Winner is selected after 48h window. 3. Inspect the send to remaining 160 contacts. | Remaining 160 contacts receive only the winning variant subject line. The losing variant subject is not used for the hold group. active_variant_id drives the hold-group send. | High |
| TC-4214 | Personalization engine handles Unicode characters in contact names | 1. Create contact with first_name = "Renée". 2. Generate edition with {{first_name}} greeting. 3. Inspect generated text and sent email. | "Hi Renée," renders correctly with the accent character. No Unicode encoding errors or garbled text. The é character displays correctly in both preview and sent email. | Medium |

---

### Section AE: Analytics and Reporting Tests (TC-4215 to TC-4264)

| TC | Test Title | Test Steps | Expected Result | Priority |
|----|-----------|-----------|----------------|----------|
| TC-4215 | Newsletter analytics row created on first send | 1. Create a new edition. 2. Send it to 10 contacts. 3. Query newsletter_analytics (or newsletters row stats fields). | A tracking record is created for the edition at send time with recipient_count = 10, sent_at = current timestamp, open_count = 0, click_count = 0, bounce_count = 0. | High |
| TC-4216 | open_count increments by 1 on first open event | 1. Send edition to 5 contacts. 2. Simulate open webhook for contact A (first time opening). 3. Query open_count for this edition. | open_count = 1 after the first unique open event. The Resend webhook handler increments the counter in the DB. | High |
| TC-4217 | open_count not incremented by repeat opens from same contact | 1. Send edition to contacts. 2. Simulate open webhook for contact A three times (same email, same edition). 3. Query open_count. | open_count = 1 (not 3). Repeat opens from the same contact_id and edition_id are deduplicated. Only the first open per contact is counted. | High |
| TC-4218 | click_count increments on first click from a contact | 1. Send edition. 2. Simulate click webhook for contact A clicking the hero link. 3. Query click_count. | click_count = 1 after the first click event from contact A. Counter is correctly incremented by the webhook handler. | High |
| TC-4219 | click_count increments again on second click from same contact on different link | 1. Send edition. 2. Simulate click webhook for contact A on hero link, then on CTA link. 3. Query click_count. | click_count = 2. Clicks on different URLs from the same contact within the same edition each count. Only same-link duplicates are deduplicated. | High |
| TC-4220 | bounce_count increments on hard bounce event | 1. Send edition. 2. Simulate hard bounce webhook from Resend for contact A. 3. Query bounce_count. | bounce_count = 1. Hard bounce event is processed by webhook handler. Contact A's email is also flagged as bounced in the contacts table. | High |
| TC-4221 | unsubscribe_count increments on unsubscribe event | 1. Send edition. 2. Contact A clicks unsubscribe link. 3. Unsubscribe event webhook fires. 4. Query unsubscribe_count. | unsubscribe_count = 1. Contact A's newsletter_consent field is updated to false. unsubscribe_count on the edition row increments atomically. | High |
| TC-4222 | open_rate calculated correctly to 2 decimal places | 1. Send to 200 contacts. 2. Record 47 unique opens. 3. Check open_rate displayed on analytics page. | open_rate displayed = 23.50% (47/200 * 100 = 23.5). Value is rounded to 2 decimal places. Formula: (unique_opens / recipient_count) * 100. | High |
| TC-4223 | click_rate calculated correctly to 2 decimal places | 1. Send to 200 contacts. 2. Record 18 unique clicks. 3. Check click_rate on analytics page. | click_rate = 9.00% (18/200 * 100). Displayed with 2 decimal places. Formula: (unique_clicks / recipient_count) * 100. | High |
| TC-4224 | Analytics page shows all 4 key metrics for each edition | 1. Navigate to /newsletters/analytics (or /newsletters/editorial/analytics). 2. Find any sent edition row. | Each edition row displays: open_rate (%), click_rate (%), bounce_rate (%), and unsubscribe_rate (%). All four metrics visible without horizontal scrolling. | High |
| TC-4225 | Analytics page shows total sent editions count | 1. Send 5 editions. 2. Navigate to analytics page. 3. Find summary stat. | A summary metric at the top of the analytics page shows "Total Editions Sent: 5" (or equivalent). Count is accurate to the number of editions with status = "sent". | Medium |
| TC-4226 | Analytics page shows total unique contacts reached | 1. Send 3 editions: to 50, 60, 70 contacts. With 20 contacts overlapping across all 3. 2. Check "Unique contacts reached" metric. | Total unique contacts reached = 140 (not 180). Deduplication applied so same contact counted once regardless of how many editions they received. | Medium |
| TC-4227 | Lifetime open rate calculated across all sent editions | 1. Send 5 editions with varying open rates. 2. Navigate to analytics summary. | Lifetime open rate = (total unique opens across all editions) / (total recipients across all editions) * 100. Displayed as a weighted average, not simple average of open rates. | Medium |
| TC-4228 | Best performing edition highlighted on analytics page | 1. Send 5+ editions with varying open rates. 2. Navigate to analytics page. | The edition with the highest open_rate is visually highlighted or marked as "Best Performer". The highlight/badge is accurate (matches the edition with max open_rate). | Medium |
| TC-4229 | Best performing subject line shown as copywriting tip | 1. Send multiple editions. 2. Navigate to analytics page. 3. Look for subject line tip. | A "Copywriting Tip" section shows the subject line of the best-performing edition by open_rate with the message "This subject line achieved X% open rate". | Low |
| TC-4230 | Click map shows which block got the most clicks | 1. Send edition with hero, market_commentary, and CTA blocks. 2. Record clicks on each block's link. 3. View edition analytics drilldown. | A click map or block-level breakdown shows relative click counts per block. The highest-clicked block is visually indicated (e.g., bar chart, heat percentage). | Medium |
| TC-4231 | Click map shows hero_url, market_url, and cta_url click counts separately | 1. Send edition. 2. Record 10 clicks on hero_url, 5 on market_url, 15 on cta_url. 3. View analytics drilldown. | Analytics shows three separate rows or bars: "Hero link: 10 clicks", "Market link: 5 clicks", "CTA link: 15 clicks". Counts match actual webhook events. | Medium |
| TC-4232 | Time-of-day analytics shows open distribution by hour | 1. Send edition. 2. Simulate opens at hours 8, 8, 9, 12, 12, 12, 18. 3. View analytics time-of-day chart. | Time-of-day chart shows bar chart or table with 24 hour slots (0-23). Hour 12 has the highest bar (3 opens), hours 8 and 18 have 2 and 1 respectively. | Medium |
| TC-4233 | Best send time recommendation displayed based on analytics | 1. Accumulate opens data over 5+ editions. 2. Navigate to analytics send time section. | A "Best Send Time" recommendation shows the hour with highest historical open rate (e.g., "Tuesday at 10 AM has your highest open rate: 34%"). Labeled "based on your data" or similar. | Low |
| TC-4234 | Edition analytics drilldown shows per-contact detail | 1. Click any edition on the analytics page. 2. View the drilldown/detail view. | Per-contact table shows: contact name, open_time (or "Not opened"), last_click_time (or "No clicks"), last_clicked_url. Pagination applied if more than 50 contacts. | High |
| TC-4235 | Per-contact detail shows correct open and click timestamps | 1. Send edition to contact A. 2. Simulate open at 14:30:00 UTC. 3. Simulate click at 14:32:00 UTC. 4. View drilldown. | Contact A row shows open_time = "2:30 PM" (localized), click_time = "2:32 PM", last_clicked_url = "[the clicked URL]". Timestamps match webhook event data. | High |
| TC-4236 | Analytics CSV export has correct columns | 1. Navigate to analytics page. 2. Click "Export CSV". 3. Open downloaded file. | CSV header row: edition_title, sent_at, recipient_count, open_rate, click_rate, bounce_rate, unsubscribe_rate. Column order matches display. All sent editions are included as rows. | Medium |
| TC-4237 | CSV export handles accented characters correctly | 1. Create contact named "Renée Côté". 2. Send edition. 3. Export per-contact CSV from drilldown. 4. Open in text editor (UTF-8). | Contact name "Renée Côté" appears correctly with accents. File is UTF-8 encoded (or UTF-8 with BOM for Excel compatibility). No garbled characters (Ren√© or similar). | Medium |
| TC-4238 | Analytics line chart shows open_rate trend over last 12 editions | 1. Send 12+ editions over time. 2. Navigate to analytics page. 3. View the trend chart. | Line graph renders with up to 12 data points (one per sent edition). X-axis shows edition dates, Y-axis shows open_rate percentage. Chart is legible and correctly ordered by sent_at. | Medium |
| TC-4239 | Analytics chart renders on mobile viewport | 1. Open analytics page on 375px viewport. 2. Inspect the line chart. | Chart is visible without horizontal overflow. Touch-pinch zoom is enabled. Chart is readable (labels not overlapping) at mobile size. No broken layout or clipped axes. | High |
| TC-4240 | Analytics shows consent opt-out rate | 1. Send to 100 contacts. 2. Record 3 unsubscribes. 3. View analytics summary. | Analytics shows "Opt-out rate: 3.00%" (unsubscribes / sent * 100). Label is clearly "Opt-out rate" or "Unsubscribe rate" to distinguish from bounce rate. | High |
| TC-4241 | CASL compliance metric shows % of sends with valid consent | 1. Send to 98 consented contacts and 2 with expired consent (via test data). 2. View analytics CASL section. | "CASL Compliance" metric shows "98.00% of sends had valid consent at time of send". A warning appears if this drops below 99%. | High |
| TC-4242 | Journey vs manual send attribution tracked separately | 1. Send 3 editions via journey auto-trigger. 2. Send 2 editions manually. 3. View analytics. | Analytics shows two attribution categories: "Journey-triggered: 3 sends" and "Manual sends: 2 sends". Editions tagged with origin = "journey" or origin = "manual" in the DB. | Medium |
| TC-4243 | Analytics not generated for draft editions | 1. Create a draft edition. Do not send. 2. Navigate to analytics page. | Draft edition does not appear in analytics view. Analytics only shows editions with status = "sent" or status = "delivered". | High |
| TC-4244 | Bounce rate above 2% triggers analytics warning banner | 1. Send edition to 100 contacts. 2. Simulate 3 hard bounces. 3. View analytics page. | A warning banner appears: "Bounce rate is 3.00%, above the 2% threshold. Consider cleaning your contact list." Banner is dismissible but reappears on next analytics page load if rate remains high. | High |
| TC-4245 | Unsubscribe rate above 0.5% triggers CASL review reminder | 1. Send to 200 contacts. 2. Record 2 unsubscribes (1.00% rate). 3. View analytics. | A CASL review reminder appears: "Unsubscribe rate is 1.00%. Review your contact consent records." Threshold is 0.5%. | High |
| TC-4246 | Engagement score recalculated on each new open or click event | 1. Contact A has engagement_score = 50. 2. Contact A opens a new edition. 3. Check newsletter_intelligence.engagement_score. | engagement_score is recalculated and updated. Score increases (e.g., to 53). Recalculation is triggered by the open webhook handler. | High |
| TC-4247 | Updated engagement score stored in contacts.newsletter_intelligence | 1. After TC-4246. 2. Query contacts.newsletter_intelligence WHERE id = contact_A. | newsletter_intelligence JSONB contains updated "engagement_score" key with new value. The update is written to the contacts table (not just in memory). | High |
| TC-4248 | Edition analytics visible to realtor only (multi-tenant isolation) | 1. Log in as realtor A. Send edition. 2. Log in as realtor B. 3. Try to access /newsletters/editorial/analytics for realtor A's edition. | Realtor B cannot see realtor A's edition analytics. The analytics query filters by realtor_id = current user's ID. Returns 403 or empty result for cross-tenant access. | High |
| TC-4249 | Analytics API requires authentication | 1. Send unauthenticated GET request to /api/editorial/analytics. | API returns HTTP 401 Unauthorized. No analytics data is returned. Authentication check runs before any DB query. | High |
| TC-4250 | Analytics aggregation correct for contact with multiple email addresses | 1. Contact has email_1 = "a@test.com" and email_2 = "b@test.com". 2. Both addresses receive the same edition and open it. 3. View analytics. | open_count and unique_opens attribute both opens to the same contact_id. One unique open is counted (not two). Deduplication is by contact_id, not by email address. | Medium |
| TC-4251 | Delivery rate shown on analytics page | 1. Send to 100 contacts. 2. Record 3 hard bounces. 3. View analytics. | Analytics shows "Delivery rate: 97.00%" — calculated as (sent - bounced) / sent * 100. This is a distinct metric from open_rate. | Medium |
| TC-4252 | Click-to-open rate (CTOR) shown as secondary metric | 1. Send edition. 2. Record 50 opens and 12 clicks. 3. View analytics. | CTOR = 24.00% (clicks/opens * 100) is shown as a secondary metric, labeled "Click-to-Open Rate" or "CTOR". Positioned near click_rate on the analytics display. | Medium |
| TC-4253 | Spam complaint count tracked when Resend sends complaint event | 1. Simulate Resend webhook with event type = "complaint". 2. Query analytics data. | Spam complaint is recorded. analytics row has spam_complaint_count (or equivalent field) incremented. The contact is flagged in the contacts table. | Medium |
| TC-4254 | Analytics date range filter: last 7 days | 1. Navigate to analytics page. 2. Select "Last 7 days" filter. 3. Verify displayed editions. | Only editions sent within the last 7 days are shown. Editions sent 8+ days ago are excluded. Date filter applies to sent_at timestamp. | Medium |
| TC-4255 | Analytics date range filter: last 30 days | 1. Select "Last 30 days" filter. | Only editions sent within the last 30 days shown. Correct cutoff date calculated from current date. | Medium |
| TC-4256 | Analytics date range filter: last 90 days | 1. Select "Last 90 days" filter. | Only editions sent within the last 90 days shown. | Medium |
| TC-4257 | Analytics date range filter: all time | 1. Select "All time" filter. | All sent editions shown regardless of sent_at date. No date range restriction applied. | Medium |
| TC-4258 | Analytics page loads within 2 seconds with large dataset | 1. Seed DB with 100 editions and 10,000 newsletter_events. 2. Load /newsletters/editorial/analytics. 3. Measure page load time. | Page loads within 2 seconds (Time to Interactive). Aggregation queries use indexed columns. No timeout errors. | High |
| TC-4259 | Analytics data cached for 5 minutes with webhook invalidation | 1. Load analytics page. 2. Note open_count. 3. Immediately reload (within 5 minutes, no new webhook). 4. Simulate open webhook. 5. Reload page. | Reload within 5 minutes returns cached data (same open_count). After webhook event fires, cache is invalidated and the next page load returns updated count. | Medium |
| TC-4260 | Revenue attribution links sold listings to last-opened edition | 1. Contact opens edition A on March 1. 2. Contact is linked to a listing that closes on March 15. 3. View revenue attribution section (if implemented). | Edition A is tagged with a "Conversion: 1 sale" attribution. Attribution window is configurable (default 30 days). If not implemented, TC is marked N/A with a note. | Low |
| TC-4261 | Analytics shows recipient_count matching actual sent count | 1. Send edition to exactly 37 contacts. 2. View analytics for that edition. | recipient_count = 37. Count matches the number of contacts in the segment at send time, not the current segment count (which may have changed). | High |
| TC-4262 | Per-contact analytics not shown to contacts (not publicly accessible) | 1. Retrieve edition analytics URL. 2. Access it without authentication. | Returns 401. The per-contact detail (who opened what) is never accessible without realtor authentication. No information leakage about individual contact behavior. | High |
| TC-4263 | Analytics persists after edition is archived | 1. Send edition. 2. Archive the edition. 3. Navigate to analytics page. | Archived edition's analytics data is still accessible. Archiving does not delete analytics records. Analytics page may show archived editions in a separate section or with an "Archived" tag. | Medium |
| TC-4264 | Analytics open_count cannot be manually edited (read-only) | 1. As a realtor, attempt to call PUT /api/editorial/analytics with modified open_count. 2. Check if update is accepted. | API rejects manual updates to analytics counters. open_count, click_count, etc. can only be incremented via the webhook handler. Direct update returns 403 or 405 Method Not Allowed. | High |

---

### Section AF: Settings and Configuration Tests (TC-4265 to TC-4304)

| TC | Test Title | Test Steps | Expected Result | Priority |
|----|-----------|-----------|----------------|----------|
| TC-4265 | Editorial settings page accessible at correct route | 1. Log in as an authenticated realtor. 2. Navigate to /newsletters/editorial/settings. | Page loads without 404 or 500 error. Settings form renders with all configuration fields. Page title is "Editorial Settings" or equivalent. | High |
| TC-4266 | Settings page redirects unauthenticated users to login | 1. Clear session/cookies. 2. Navigate directly to /newsletters/editorial/settings. | Redirected to /login with a returnTo=/newsletters/editorial/settings parameter (or equivalent). Settings page content is not shown to unauthenticated users. | High |
| TC-4267 | Default edition type dropdown saves correctly | 1. Navigate to settings page. 2. Change "Default Edition Type" dropdown to "just_sold". 3. Trigger save (blur or Save button). 4. Reload page. | Setting persists after reload. New editions created after this change default to type = "just_sold". DB value in editorial_settings.default_edition_type = "just_sold". | High |
| TC-4268 | Default send time picker saves HH:MM value | 1. Set default send time to 09:30. 2. Save. 3. Reload page. 3. Check saved value. | Default send time = "09:30" persists. Stored in editorial_settings or realtor_agent_config as a 24-hour HH:MM string. New edition scheduled_for defaults use this time. | High |
| TC-4269 | Default from-name saves to realtor_agent_config | 1. Set from-name to "Jane Smith — 24K Realty". 2. Save. 3. Query realtor_agent_config.from_name. | realtor_agent_config.from_name = "Jane Smith — 24K Realty". All subsequent editions use this from-name unless overridden at edition level. | High |
| TC-4270 | Default from-email saves to realtor_agent_config | 1. Set from-email to "jane@realtors360.ai". 2. Save. 3. Query realtor_agent_config.from_email. | realtor_agent_config.from_email = "jane@realtors360.ai". Stored and used as the default sender address for new editions. | High |
| TC-4271 | From-email must be a verified Resend sender domain | 1. Set from-email to "jane@gmail.com" (unverified domain). 2. Save. | Save rejected. Error message: "Must be a verified sender address". Resend API or local domain allowlist is checked. DB update does not occur. | High |
| TC-4272 | Invalid from-email shows field-level error | 1. Enter "not-an-email" in the from-email field. 2. Trigger save. | Field-level validation error appears directly below the from-email input: "Must be a valid email address". Error disappears when a valid email is entered. | High |
| TC-4273 | Unsubscribe footer toggle is enabled by default and cannot be disabled | 1. Navigate to settings. 2. Find the unsubscribe footer toggle. 3. Attempt to disable it. | Toggle is on by default. Attempting to turn it off is blocked — toggle springs back or is disabled/read-only with a tooltip: "Required by CASL — cannot be disabled." | High |
| TC-4274 | Physical address field required; save blocked if empty | 1. Clear the physical address field in settings. 2. Attempt to save. | Save is blocked. Validation error: "Physical address is required for CASL/CAN-SPAM compliance". Field is highlighted. DB is not updated. | High |
| TC-4275 | Feature flag editorial_newsletter = off makes settings page return 404 | 1. Set feature flag editorial_newsletter = false in DB or environment. 2. Navigate to /newsletters/editorial/settings. | Page returns 404. No settings content rendered. Other newsletter pages (/newsletters) are unaffected. | Medium |
| TC-4276 | Feature flag editorial_mls_data = off hides MLS data source fields | 1. Set editorial_mls_data = false. 2. Navigate to settings page. | MLS API key field, MLS connection type, and related MLS fields are not rendered in the settings form. Rest of settings page renders normally. | Medium |
| TC-4277 | MLS API key field shows masked display with reveal button | 1. Enter and save an MLS API key. 2. Return to settings page. | API key field displays "••••••••" (masked). A "Reveal" button (eye icon) is present. Clicking Reveal shows the actual key temporarily. | High |
| TC-4278 | MLS API key saved encrypted, not plaintext | 1. Save an MLS API key via settings. 2. Query the realtor_agent_config or editorial_settings table directly. | The stored value is encrypted (not the plaintext key). Application decrypts on read. No plaintext key in DB or server logs. | High |
| TC-4279 | MLS API key test connection button calls mls-test endpoint | 1. Enter a valid MLS API key in settings. 2. Click "Test Connection" button. 3. Monitor network requests. | Browser sends request to /api/editorial/mls-test. The endpoint is called with the current API key (from settings state, not yet saved). Network tab confirms the correct endpoint. | High |
| TC-4280 | MLS API test failure shows informative error | 1. Enter an invalid MLS API key. 2. Click "Test Connection". | Error message appears: "API key invalid or quota exceeded". HTTP error code from Repliers/Spark is surfaced in the message. No generic "Something went wrong" message. | High |
| TC-4281 | MLS API test success shows listing count | 1. Enter a valid MLS API key. 2. Click "Test Connection". | Success message: "Connected, [N] active listings available" (e.g., "Connected, 247 active listings available"). Number comes from the MLS API response. | High |
| TC-4282 | Rate API key optional; uses public endpoint by default | 1. Leave Rate API key field empty. 2. Trigger edition generation with a rate_watch block. | Generation succeeds using the public Bank of Canada or FRED rate endpoint. No error about missing rate API key. Rate data populates correctly. | Medium |
| TC-4283 | AI generation model defaults to claude-sonnet-4-6 with no UI to change | 1. Inspect settings page HTML for model selection field. 2. Check the generation API calls in server logs. | No model selection dropdown exists on the settings page. Server logs show model = "claude-sonnet-4-6" in all Anthropic API calls. Model is hardcoded server-side. | Medium |
| TC-4284 | Voice profile section links to voice profile page | 1. Navigate to settings. 2. Find the "Voice Profile" section. 3. Click the link/button. | Browser navigates to /newsletters/editorial/voice-profile. The link uses Next.js <Link> component (client-side navigation, no full page reload). | Medium |
| TC-4285 | A/B testing global toggle enables and disables A/B features | 1. Navigate to settings. 2. Toggle "A/B Subject Testing" off. 3. Create a new edition. | A/B subject variant fields are hidden in the edition editor when A/B testing is globally disabled. The toggle state persists after page reload. | High |
| TC-4286 | A/B sample size setting: 20% test group configurable | 1. Set A/B sample size to 20% in settings. 2. Send A/B edition to 200 contacts. 3. Check send distribution. | 40 contacts receive variant A (10%), 40 receive variant B (10%), 120 are in hold group (80%). Total test group = 40 contacts = 20% of 200. | Medium |
| TC-4287 | A/B winner threshold: minimum opens before auto-select configurable | 1. Set winner selection threshold to 50 opens in settings. 2. Send A/B edition. 3. Simulate 49 total opens within 48h. | Winner is NOT selected at 49 opens. No active_variant_id update occurs. System waits for the 50th open or the 48h window expiry (whichever triggers the fallback). | Medium |
| TC-4288 | BCC self on sends toggle saves to config | 1. Enable "BCC Self" toggle. 2. Save settings. 3. Send an edition. 4. Check the demo email inbox. | realtor_agent_config.bcc_self = true. Each edition send includes the realtor's email as BCC. Realtor receives a copy of every email sent. | Low |
| TC-4289 | Sandbox mode routes all sends to DEMO_EMAIL | 1. Enable sandbox mode in settings. 2. Send edition to 50 contacts. 3. Check who received the email. | Only the DEMO_EMAIL address (e.g., demo@realtors360.ai) receives the email — not the 50 real contacts. Sandbox intercepts all outbound sends. | High |
| TC-4290 | Sandbox mode indicator shows orange banner | 1. Enable sandbox mode. 2. Navigate to any editorial page. | Orange banner visible at top of all editorial pages: "Sandbox Mode Active — emails go to demo@realtors360.ai". Banner persists on all page navigations within /newsletters/editorial. | High |
| TC-4291 | Settings autosave on blur (no manual Save button needed) | 1. Change the "Default from-name" field. 2. Click elsewhere on the page (blur the field). 3. Reload the page. | Setting is saved automatically on blur. No Save button press needed. After reload, the new from-name is shown. A brief "Saved" toast appears within 1 second of blur. | Medium |
| TC-4292 | Settings save success shows "Settings saved" toast within 1 second | 1. Change any setting. 2. Trigger save. 3. Watch for toast notification. | "Settings saved" toast appears within 1 second of the save action. Toast auto-dismisses after 3 seconds. | Medium |
| TC-4293 | Settings save failure shows error toast | 1. Simulate DB failure (network disconnect or test DB error). 2. Attempt to save settings. | Error toast appears: "Failed to save, please try again". The failed value is reverted to the previous saved value in the UI. | High |
| TC-4294 | Settings data isolated by realtor_id | 1. Log in as realtor A. Set from-name to "Alice". 2. Log in as realtor B. View settings. | Realtor B sees their own from-name (not "Alice"). Settings queries filter by realtor_id = auth.uid(). No cross-tenant data visible. | High |
| TC-4295 | Reset to defaults button shows confirmation dialog | 1. Click "Reset to Defaults" button in settings. | Confirmation dialog appears: "This will reset all editorial settings. Are you sure?" with Cancel and Confirm buttons. Settings are NOT reset yet at this point. | High |
| TC-4296 | Reset confirmed reverts all settings to factory defaults | 1. Change multiple settings. 2. Click "Reset to Defaults". 3. Confirm. 4. Reload page. | All settings revert to factory defaults (documented default values). The voice profile is NOT reset — it remains unchanged. | High |
| TC-4297 | Reset does not affect voice profile | 1. Set up voice profile with custom rules. 2. Reset settings to defaults. 3. Navigate to voice profile page. | Voice profile (editorial_voice_profiles row) is unchanged after settings reset. Voice rules, tone, and signature phrase persist. | High |
| TC-4298 | Settings export downloads valid JSON file | 1. Click "Export Settings" button. 2. Download the JSON file. 3. Open and inspect. | JSON file is valid, well-formed, and contains all settings fields. File is named "editorial-settings-export-[date].json". No credentials or API keys in the export. | Low |
| TC-4299 | Settings import validates schema before applying | 1. Modify export JSON to include an invalid field type (e.g., default_send_time = 12345). 2. Import via settings upload. | Import is rejected with error: "Invalid settings file. Please check the file format." Settings are not partially applied. Original settings remain unchanged. | Medium |
| TC-4300 | Audit log records all settings changes | 1. Change default_edition_type from "market_update" to "just_sold". 2. Check audit log or realtor_agent_config.updated_at. | realtor_agent_config.updated_at timestamp is updated. An audit log entry exists recording: realtor_id, field = "default_edition_type", old_value, new_value, changed_at. | Medium |
| TC-4301 | Trial plan shows upgrade warning in settings | 1. Set realtor's plan to "trial" in DB. 2. Navigate to settings page. | Banner or inline warning visible: "Upgrade to send to more than 25 contacts per edition" or equivalent. The warning links to the upgrade/billing page. | Medium |
| TC-4302 | Free plan: MLS data toggle disabled with upgrade tooltip | 1. Set realtor's plan to "free". 2. Navigate to settings. 3. Hover over MLS data toggle. | MLS data toggle is visually disabled (grayed out, pointer-events: none). Hover shows tooltip: "Upgrade Required — MLS data is a Pro feature". Clicking does not activate the toggle. | Medium |
| TC-4303 | Settings page labels all form fields correctly | 1. Inspect all form fields on the settings page. 2. Verify each has a visible <label> element. | Every form control has a visible, descriptive label. No placeholder-only labeling. Labels are associated via htmlFor/id. | High |
| TC-4304 | Settings page keyboard-navigable in full | 1. Open settings page. 2. Complete all fields and save using keyboard only (Tab, Enter, Space, arrow keys). | All settings fields are reachable via Tab. Toggles activate with Space or Enter. Dropdowns open with Enter, navigate with arrow keys. Form can be fully configured without a mouse. | High |

---

### Section AG: Load Testing and Stress Edge Cases (TC-4305 to TC-4354)

| TC | Test Title | Test Steps | Expected Result | Priority |
|----|-----------|-----------|----------------|----------|
| TC-4305 | Edition list loads within 1.5s with 500 editions | 1. Seed DB with 500 editorial_editions for one realtor. 2. Load /newsletters/editorial. 3. Measure page TTI. | Page reaches Time to Interactive within 1.5 seconds. Pagination is applied (e.g., 25 per page). Database query uses limit/offset. No full-table scan. | High |
| TC-4306 | Edition editor loads within 1s with 20-block edition | 1. Create edition with 20 editorial_blocks. 2. Open edition in editor. 3. Measure load time. | Edition editor loads and renders all 20 blocks within 1 second. Block list is virtualized if needed. No UI freeze during initial render. | High |
| TC-4307 | AI generation completes within 90s for 10-block edition at p95 | 1. Create 10-block edition. 2. Trigger AI generation. 3. Measure time from start to generation_complete status. 4. Repeat 20 times. | 95th percentile (19/20 runs) completes within 90 seconds. p50 target < 45 seconds. Timeout at 120s with graceful failure. | High |
| TC-4308 | AI generation completes within 45s for 5-block edition at p95 | 1. Create 5-block edition. 2. Trigger generation. 3. Measure 20 times. | p95 completion time ≤ 45 seconds. Smaller editions complete proportionally faster than 10-block editions. | High |
| TC-4309 | Preview HTML renders within 500ms for 20-block edition | 1. Create 20-block edition with full content. 2. GET /api/editorial/preview/[id]. 3. Measure server response time. | Server returns rendered HTML within 500ms. Template rendering (react-email or custom renderer) is server-side. No database roundtrips during render (data pre-fetched). | High |
| TC-4310 | Send to 500 contacts completes without timeout via async queue | 1. Create segment with 500 contacts. 2. Send edition. 3. Monitor completion. | Send action returns immediately (within 2s) after enqueueing the job. All 500 contacts receive the email within 10 minutes. No Next.js API timeout occurs. Async queue (BullMQ) handles the actual sending. | High |
| TC-4311 | Send to 5000 contacts enqueues within 5 seconds | 1. Create segment with 5000 contacts. 2. Click "Send Edition". 3. Measure time for success response. | API responds with "Send queued" within 5 seconds. The BullMQ job is enqueued and all 5000 contacts are batched for sending. The UI does not block waiting for completion. | High |
| TC-4312 | Webhook handler processes 100 events per minute without queue backup | 1. Simulate 100 Resend webhook events arriving within 60 seconds. 2. Monitor BullMQ queue depth. | Queue depth does not grow unbounded. All 100 events are processed within 90 seconds (60s arrival + 30s processing). No events dropped or left unprocessed. | High |
| TC-4313 | Webhook handler is idempotent (duplicate event not double-counted) | 1. Send the same Resend webhook event twice with identical event_id. 2. Query newsletter_events and analytics counters. | newsletter_events has exactly one row for the event. open_count or click_count incremented once (not twice). Idempotency check uses event_id unique constraint or upsert. | High |
| TC-4314 | External data cache prevents more than 10 API calls per hour | 1. Trigger 15 edition generations for the same neighbourhood within 1 hour. 2. Monitor outbound API call logs for Repliers/Bank of Canada. | External API is called at most 10 times in 60 minutes. Subsequent calls return cached data from external_data_cache table. Cache TTL = 60 minutes. | High |
| TC-4315 | 10 concurrent saves by different realtors have no cross-tenant data leak | 1. Simulate 10 concurrent POST /api/editorial/editions/save requests from 10 different realtor sessions. 2. Verify each save updates only the correct realtor's data. | Each save modifies only the row owned by the requesting realtor. No row is updated with another realtor's data. DB queries include WHERE realtor_id = auth.uid(). | High |
| TC-4316 | 10 concurrent generation requests from same realtor deduplicated to 1 job | 1. Send 10 simultaneous POST /api/editorial/generate requests for the same edition. 2. Check BullMQ job queue. | Only 1 BullMQ job is created (deduplication by edition_id). 9 of the 10 requests return "Generation already in progress". No duplicate generation jobs run simultaneously. | High |
| TC-4317 | DB connection pool survives 50 concurrent API requests | 1. Simulate 50 concurrent requests to various editorial API endpoints. 2. Monitor DB connection pool utilization. | All 50 requests complete successfully. No "connection pool exhausted" or timeout errors. Pool size is sufficient (typically 10-20 connections for a single worker). | High |
| TC-4318 | Editor open 8 hours with polling: no memory growth above 50MB | 1. Open edition editor. 2. Leave it open with generation polling active for 8 hours (simulate with 480 status polls). 3. Measure JS heap size before and after. | JS heap size does not increase by more than 50MB over 8 hours. Event listeners are properly cleaned up. No memory leak from polling interval accumulation. | Medium |
| TC-4319 | Edition with 30 blocks renders without browser freeze | 1. Create edition with 30 editorial_blocks (stress test). 2. Open edition editor. 3. Interact with the block list (scroll, click). | Editor renders within 2 seconds. Scrolling and clicking are responsive (no 500ms+ input lag). If virtualization is used, only visible blocks render to DOM. | High |
| TC-4320 | Drag-and-drop reorder on 30-block edition completes within 200ms | 1. Open 30-block edition. 2. Drag block from position 1 to position 15. 3. Measure drag completion time. | Block reorder operation (DOM update + state update) completes within 200ms after drop. No jank or freeze. List rerender is optimized (keys stable, no full re-render). | Medium |
| TC-4321 | Block content with 2000 characters saves without truncation | 1. Enter 2000 characters in a block's content field. 2. Save. 3. Reload and inspect saved content. | All 2000 characters are saved and returned intact. No truncation at 255, 1000, or any other limit. DB column uses TEXT type (not VARCHAR with short limit). | High |
| TC-4322 | Block content with emoji characters saves and renders correctly | 1. Enter "🏠 Market is heating up! 💰 Prices up 📈 5.2%" in a block content field. 2. Save. 3. View in preview. | Emoji characters save correctly to DB (UTF-8 multibyte). Preview HTML renders emojis visually. No garbled characters, HTML entities, or question marks. | Medium |
| TC-4323 | Block content with HTML injection attempt is sanitized | 1. Enter "<script>alert('XSS')</script>" in a block content field. 2. Save. 3. View in preview. | The script tag is sanitized before rendering. Preview renders the text as literal characters or strips the script tag. No JavaScript executes. Script tags escaped as &lt;script&gt; or removed. | High |
| TC-4324 | Block content with SQL injection attempt stored as literal text | 1. Enter "'; DROP TABLE editorial_editions; --" in a block content field. 2. Save. | Content saves as literal text without executing. DB query uses parameterized statements (not string interpolation). No tables are dropped. Saved value = "'; DROP TABLE editorial_editions; --". | High |
| TC-4325 | Voice profile with 50 rules keeps generation prompt within 8K tokens | 1. Create voice profile with 50 writing rules. 2. Trigger generation. 3. Inspect system prompt token count in server logs. | Total system prompt (including 50 rules) is 8,000 tokens or fewer. If rules exceed limit, oldest or least-specific rules are truncated. Generation completes without Anthropic context length error. | Medium |
| TC-4326 | Voice Haiku extraction with 50 rules completes within 15 seconds | 1. Provide a long sample email (1000 words). 2. Trigger voice rule extraction with Haiku model. 3. Measure extraction time. | Claude Haiku extraction returns within 15 seconds (wall clock). Result contains 5-50 rules. No timeout error. | Medium |
| TC-4327 | Large external data response (50KB JSON) parsed without timeout | 1. Mock Repliers API to return a 50KB JSON payload. 2. Trigger generation that fetches external data. 3. Monitor parsing time. | Server parses the 50KB JSON within the API route timeout (< 10s). No JSON.parse timeout or memory allocation error. Data is correctly mapped to edition blocks. | Medium |
| TC-4328 | 100 editions in generating state polled within 2 seconds | 1. Set 100 editions to status = "generating". 2. Call GET /api/editorial/status/batch. 3. Measure response time. | Batch status endpoint returns statuses for all 100 editions within 2 seconds. Uses a single DB query (IN clause or batch fetch), not 100 individual queries. | High |
| TC-4329 | 10KB webhook payload processed within 500ms | 1. Send a Resend webhook with a 10KB JSON body. 2. Measure webhook handler processing time. | Webhook handler returns 200 within 500ms of receiving the request. Payload parsing and DB write complete within the time limit. | Medium |
| TC-4330 | 1000 newsletter_events for one edition: analytics loads within 2s | 1. Seed 1000 newsletter_events for a single edition. 2. Load /newsletters/editorial/analytics/[id]. 3. Measure load time. | Analytics page loads within 2 seconds. Aggregation query uses GROUP BY with index on newsletter_events(newsletter_id, event_type). No full table scan. | High |
| TC-4331 | Edition list query uses index scan | 1. Run EXPLAIN ANALYZE on the edition list query (SELECT * FROM editorial_editions WHERE realtor_id = ? ORDER BY created_at DESC LIMIT 25). 2. Inspect query plan. | Query plan shows "Index Scan" on realtor_id or a composite index (realtor_id, created_at). No "Seq Scan" on the full table. | High |
| TC-4332 | Analytics query uses index on newsletter_events(newsletter_id) | 1. Run EXPLAIN ANALYZE on analytics aggregation query for a given newsletter_id. | Query plan shows index scan on newsletter_events(newsletter_id) index. Aggregation is fast (< 50ms for 10K events). | High |
| TC-4333 | Voice profile lookup uses index on realtor_id | 1. Run EXPLAIN ANALYZE on SELECT * FROM editorial_voice_profiles WHERE realtor_id = ?. | Query plan shows index scan using an index on editorial_voice_profiles(realtor_id). Not a sequential scan. | Medium |
| TC-4334 | External cache lookup uses index on cache_key | 1. Run EXPLAIN ANALYZE on SELECT * FROM external_data_cache WHERE cache_key = ? AND expires_at > NOW(). | Query plan shows index scan on external_data_cache(cache_key). Not a sequential scan. Lookup is O(log n) or O(1). | Medium |
| TC-4335 | Resend API 429 triggers exponential backoff | 1. Mock Resend API to return 429 on first 3 attempts. 2. Trigger an email send. 3. Monitor retry timing in logs. | System retries with delays of 1s, 2s, 4s (exponential backoff). 4th attempt succeeds. Total delay ≈ 7 seconds. Error is not surfaced to the realtor if final attempt succeeds. | High |
| TC-4336 | Resend API 30-second timeout cancels request gracefully | 1. Mock Resend API to hang for 35 seconds. 2. Trigger send. 3. Monitor outcome. | Request is cancelled after 30 seconds. Error returned to caller: "Email send timed out". Edition status updated to "send_failed". No zombie requests left open. | High |
| TC-4337 | Anthropic API 60-second timeout marks generation as failed | 1. Mock Anthropic API to hang for 65 seconds. 2. Trigger generation. 3. Check edition status after timeout. | Generation request cancelled after 60 seconds. editorial_editions.generation_status = "failed". Error message stored: "Generation timed out. Please try again." No hanging process. | High |
| TC-4338 | BullMQ worker crash causes jobs to re-queue on restart | 1. Start a generation job. 2. Kill the BullMQ worker mid-execution (SIGKILL). 3. Restart worker. 4. Check job status. | Job is re-queued by BullMQ's stall detection. Worker restarts and picks up the stalled job. Job is re-executed from the beginning (generation is idempotent). | High |
| TC-4339 | BullMQ stalled job re-queued after 30-second stall timeout | 1. Mock worker to stall (not heartbeat) for 35 seconds without completing. 2. Monitor queue. | BullMQ detects the stall after 30s and moves the job back to the waiting state. A new worker instance picks it up. Job is not permanently lost. | High |
| TC-4340 | Redis connection loss causes BullMQ to fail gracefully with error log | 1. Disconnect Redis (kill Redis process or block port). 2. Attempt to enqueue a generation job. | Error is logged: "Redis connection failed — unable to queue job". The API returns a 503 error to the realtor with a user-friendly message. No unhandled promise rejection crash. | High |
| TC-4341 | Redis reconnection allows BullMQ to resume job processing | 1. Disconnect Redis. 2. Re-connect Redis. 3. Attempt to enqueue generation job after reconnection. | BullMQ auto-reconnects to Redis. Job enqueues successfully after connection is restored. Previously queued but unprocessed jobs resume processing. | High |
| TC-4342 | Next.js API route timeout returns 408 with retry instruction | 1. Mock a generation status check to take longer than 30 seconds on Vercel. 2. Monitor the response. | API route returns HTTP 408 Request Timeout with body: {"error": "Request timed out", "retry": true}. Client can use retry flag to retry with exponential backoff. | Medium |
| TC-4343 | DB deadlock on edition save retries up to 3 times | 1. Simulate a DB row-level lock contention scenario during edition save. 2. Monitor retry behavior. | Server retries the save up to 3 times with a brief delay (100ms between retries). If all 3 retries fail, returns a 503 error. Does not throw an unhandled exception on first deadlock. | Medium |
| TC-4344 | 500-contact CASL filter query completes within 2 seconds | 1. Create segment with 500 contacts. 2. Run CASL filter (WHERE newsletter_consent = true AND consent_expires_at > NOW()). 3. Measure query time. | Query completes within 2 seconds. Index on contacts(newsletter_consent, realtor_id) is used. Result contains only consented contacts. | High |
| TC-4345 | Recipient calculation for edition with 5000-contact DB completes within 5s | 1. Seed contacts table with 5000 contacts. 2. Calculate recipients for an edition targeting a segment. 3. Measure time. | recipient_count calculation returns within 5 seconds. Query is optimized with proper indexes. Count is accurate. | High |
| TC-4346 | Generation worker Node.js heap stays below 512MB under normal load | 1. Run 10 consecutive generation jobs. 2. Monitor Node.js process heap using --inspect or metrics. | Peak heap usage does not exceed 512MB during normal operation. No heap growth between jobs (garbage collection working). | Medium |
| TC-4347 | Preview HTML endpoint returns correct Cache-Control headers | 1. GET /api/editorial/preview/[id] for a non-draft edition. 2. Inspect response headers. | Response includes Cache-Control: public, max-age=300, s-maxage=300 (or similar 5-minute CDN cache). Preview of sent editions is cacheable. | Medium |
| TC-4348 | Status polling endpoint returns Cache-Control: no-store | 1. GET /api/editorial/status/[id]. 2. Inspect response Cache-Control header. | Response includes Cache-Control: no-store. Status polling endpoints must not be cached (stale status would block generation progress display). | High |
| TC-4349 | Edition editor does not serve stale block data on browser refresh | 1. Edit edition in tab A. 2. Open same edition in tab B. 3. Edit in tab B. 4. Refresh tab A. | After refresh, tab A shows the latest data from tab B's edits. Browser cache does not serve stale edition state. next/cache or fetch cache correctly invalidated. | High |
| TC-4350 | DB row count check before full-list query (guard against massive tables) | 1. Seed 10,000 editorial_blocks (stress). 2. Load edition editor for a 30-block edition. 3. Verify only 30 blocks are fetched. | Editor query includes WHERE edition_id = ? — fetches only blocks for the specific edition. Does not load all 10,000 blocks into memory. | High |
| TC-4351 | Concurrent generation and save operations do not corrupt edition state | 1. Start generation on edition X. 2. Simultaneously save a manual block edit on edition X. 3. Wait for generation to complete. 4. Inspect all blocks. | Manual edits are preserved (locked blocks). Generated blocks are updated correctly. No block content is lost or corrupted due to race condition. Optimistic locking or row-level locking prevents corruption. | High |
| TC-4352 | Preview rendering does not call external APIs (fully offline) | 1. Disable all outbound network connections on the server. 2. Render a preview for a complete edition. | Preview HTML renders successfully without any external API calls. All data for preview comes from the DB (edition content). No calls to Anthropic, Repliers, or any CDN. | Medium |
| TC-4353 | Batch send job splits into chunks of 100 to avoid Resend rate limits | 1. Send to 500 contacts. 2. Monitor BullMQ job logs and Resend API call frequency. | Send job creates 5 batches of 100 contacts. Resend API called with batch payloads of ≤100 at a time. Delay between batches prevents rate limit (429) errors. | High |
| TC-4354 | Polling interval backs off if server returns 503 | 1. Mock /api/editorial/status to return 503. 2. Observe frontend polling behavior. | Frontend polling uses exponential backoff on 503 responses (starts at 5s, doubles to 10s, 20s, max 60s). Does not hammer server with rapid retries. Error is shown to user after 3 consecutive 503s. | Medium |

---

### Section AH: Regression Guard Tests (TC-4355 to TC-4414)

| TC | Test Title | Test Steps | Expected Result | Priority |
|----|-----------|-----------|----------------|----------|
| TC-4355 | Existing newsletter create action still works | 1. Call createNewsletter() from newsletters.ts with a standard payload. 2. Check DB for new row. | A new row is created in the newsletters table with correct fields. No import conflicts or function signature changes from editorial code. Returns the new newsletter ID. | High |
| TC-4356 | Existing newsletter send action still works | 1. Call sendNewsletter() from newsletters.ts for an existing newsletter. 2. Verify Resend API call fires. | Newsletter send completes successfully. Resend API called with correct from address, subject, and HTML. newsletter.sent_at updated. No regression from editorial send code. | High |
| TC-4357 | Newsletter approval queue page still loads | 1. Navigate to /newsletters/queue. | Page loads without 404 or 500 error. Approval queue renders existing draft newsletters. No import errors from editorial modules affect this page. | High |
| TC-4358 | Newsletter analytics tab still loads | 1. Navigate to /newsletters/analytics. | Page loads without error. Existing newsletter analytics (non-editorial) display correctly. Editorial analytics additions do not break the existing analytics tab. | High |
| TC-4359 | Journey enrollment action still works | 1. Call enrollContact() from journeys.ts with a valid contact_id and journey_type. 2. Check contact_journeys table. | New contact_journeys row created. Journey enrollment works identically to before editorial tables were added. No FK conflicts. | High |
| TC-4360 | Journey phase advancement still works | 1. Enroll a contact in a journey. 2. Call advanceJourneyPhase() to move to next phase. 3. Check contact_journeys row. | current_phase incremented correctly. Phase advancement triggers correct email sends. No interference from editorial_editions or editorial_blocks tables. | High |
| TC-4361 | Contact journey status visible on contact detail page | 1. Navigate to /contacts/[id] for a contact enrolled in a journey. 2. Find the journey status section. | Journey phase, enrollment date, and next scheduled email are visible on the contact detail page. No rendering errors caused by editorial module imports. | High |
| TC-4362 | Workflow engine step execution not affected by editorial tables | 1. Create a workflow with an ai_email step. 2. Execute the workflow via workflow-engine.ts. 3. Verify step executes correctly. | ai_email step generates and sends email via workflow engine. No schema conflicts with new editorial tables. Workflow step table names do not clash with editorial table names. | High |
| TC-4363 | Send governor frequency cap still enforced for non-editorial sends | 1. Attempt to send a standard newsletter to a contact who was sent one 2 hours ago (within frequency cap window). | Send is blocked by frequency cap. send-governor.ts returns "frequency cap exceeded" for the contact. Editorial newsletter sends do not bypass this check for non-editorial sends. | High |
| TC-4364 | Editorial sends have their own frequency cap (not double-counted) | 1. Send an editorial edition to contact A. 2. Immediately attempt to send a standard newsletter to contact A. | Standard newsletter send may be blocked by standard frequency cap, but the editorial send does not incorrectly consume the standard send quota. The two caps are evaluated independently. | High |
| TC-4365 | Existing React Email templates render correctly (NewListingAlert) | 1. Render NewListingAlert template with test data using react-email render(). 2. Check output HTML. | HTML is valid and contains listing details, photo, CTA button. No import errors. Template renders identically to pre-editorial-feature state. | High |
| TC-4366 | Existing React Email templates render correctly (MarketUpdate) | 1. Render MarketUpdate template with test data. | HTML valid. Market stats and chart data rendered. No runtime errors. | High |
| TC-4367 | Existing React Email templates render correctly (JustSold) | 1. Render JustSold template with test data. | HTML valid. Sale price and address rendered. No errors. | High |
| TC-4368 | BaseLayout.tsx unsubscribe link present in all existing templates | 1. Render each of the 6 existing email templates (NewListingAlert, MarketUpdate, JustSold, OpenHouseInvite, NeighbourhoodGuide, HomeAnniversary). 2. Search output HTML for unsubscribe link. | All 6 rendered HTML outputs contain an unsubscribe link. Link URL is not broken or empty. BaseLayout.tsx unsubscribe implementation is unchanged. | High |
| TC-4369 | Resend webhook handler processes existing newsletter events | 1. Send a Resend open webhook with event data for a standard newsletter (non-editorial). 2. Check newsletter_events table. | Standard newsletter open event is recorded. newsletters.open_count incremented. No errors caused by editorial webhook handling code being added to the same handler. | High |
| TC-4370 | Resend webhook handler processes editorial events without interfering | 1. Send open webhook for an editorial edition. 2. Send open webhook for a standard newsletter. Both in rapid succession. 3. Check both event tables. | Both events recorded in their respective tables (newsletter_events for standard, and editorial_newsletter_events or the shared table for editorial). Neither event modifies the wrong counter. | High |
| TC-4371 | CASL consent check in newsletters.ts still gates sends | 1. Create contact with newsletter_consent = false. 2. Attempt to include in a standard newsletter send via newsletters.ts. | Contact is excluded from the send. CASL gate in newsletters.ts still runs its check independently of editorial CASL logic. Contact receives no email. | High |
| TC-4372 | Realtor agent config changes do not break existing send config | 1. Update a realtor_agent_config field added by editorial (e.g., default_edition_type). 2. Send a standard newsletter. 3. Verify from_name and from_email still correct. | Standard newsletter send uses the correct from_name and from_email from realtor_agent_config. New editorial config fields do not overwrite or shadow existing fields. | High |
| TC-4373 | Contact newsletter_intelligence JSONB: editorial clicks don't overwrite existing keys | 1. Contact has newsletter_intelligence = {"engagement_score": 70, "interests": ["detached"]}. 2. Editorial edition sends a click event for "condos". 3. Check newsletter_intelligence. | newsletter_intelligence is updated using JSONB merge (jsonb_set or || operator), not full replace. engagement_score and existing interests keys are preserved. New interests appended, not replacing. | High |
| TC-4374 | contact_journeys table unaffected by editorial_editions inserts | 1. Insert 10 editorial_editions rows. 2. Query contact_journeys for an enrolled contact. | contact_journeys rows are unchanged. No FK cascade or trigger from editorial_editions affects contact_journeys. Query returns expected journey data. | High |
| TC-4375 | AI newsletter generation (newsletter-ai.ts) works independently | 1. Call generateNewsletterContent() from newsletter-ai.ts. 2. Verify Anthropic API called and content returned. | Generation succeeds with correct content. newsletter-ai.ts is not affected by editorial AI generation code. No shared mutable state between the two generation systems. | High |
| TC-4376 | Email block system (email-blocks.ts) unaffected by editorial imports | 1. Import email-blocks.ts in isolation. 2. Call createBlock() and renderBlock(). | Both functions work correctly. No circular import or name collision from editorial block types added to the system. | High |
| TC-4377 | Text pipeline (text-pipeline.ts) unaffected by editorial imports | 1. Call runTextPipeline() from text-pipeline.ts with test content. 2. Verify 7-step pipeline runs. | All 7 pipeline steps execute correctly. No import path collision with editorial text processing code. Pipeline output unchanged from pre-editorial state. | High |
| TC-4378 | Quality pipeline (quality-pipeline.ts) still runs on existing newsletters | 1. Trigger quality pipeline on a standard newsletter draft. 2. Verify quality score returned. | Quality score calculated and returned. Pipeline runs on standard newsletters without being accidentally re-routed through editorial quality checks. | High |
| TC-4379 | Validated send (validated-send.ts) still works for existing newsletters | 1. Call validatedSend() for a standard newsletter. 2. Verify all validations pass and send fires. | CASL check, frequency cap, and address validation all run. Send fires if validations pass. No function signature changes break the call. | High |
| TC-4380 | Contacts page loads without error | 1. Navigate to /contacts. | Page loads. Contact list renders. No console errors. Editorial module additions have not introduced any import-level side effects affecting the contacts page. | High |
| TC-4381 | Listings page loads without error | 1. Navigate to /listings. | Listings page renders correctly. Workflow phase indicators work. No regression. | High |
| TC-4382 | Showings page loads without error | 1. Navigate to /showings. | Showings list renders. Status badges, filters, and actions all work. No regression. | High |
| TC-4383 | Dashboard widgets all load correctly | 1. Navigate to / (dashboard home). 2. Verify all widgets render. | ActivityFeed, TodaysPriorities, PipelineWidget, AIRecommendations all render without errors. No new editorial API calls block dashboard widget loading. | High |
| TC-4384 | Notification center still works | 1. Navigate to /api/notifications. 2. Check for correct JSON response. | API returns notifications array. Notification center UI (bell icon) shows correct unread count. No regression from editorial notification additions. | High |
| TC-4385 | Command palette Cmd+K searches contacts and listings | 1. Press Cmd+K (or Ctrl+K). 2. Type a contact name. 3. Type a listing address. | Both contacts and listings appear in search results. Cmd+K overlay opens. Search works correctly. No regression from editorial search additions. | High |
| TC-4386 | Segment builder still works | 1. Navigate to segment builder. 2. Create a segment with 2 filter conditions. 3. Preview recipients. | Segment builder loads. Filters apply. Preview count accurate. contact_segments table unaffected by editorial tables. | High |
| TC-4387 | Template editor still loads | 1. Navigate to /newsletters/templates. | Template editor loads. Existing templates render in list. Email builder opens without error. No regression. | High |
| TC-4388 | Workflow builder (React Flow) still loads | 1. Navigate to a workflow builder page (e.g., /listings/[id]/workflow or /newsletters/templates/[id]). | React Flow canvas renders. Nodes and edges display correctly. Workflow builder is unaffected by editorial code additions. | High |
| TC-4389 | Feature flags for existing features not overwritten by editorial flags | 1. Query feature flags table (or editorial_settings). 2. Verify existing flags (e.g., ai_newsletter, journey_engine) retain their values. | Editorial flags (editorial_newsletter, editorial_mls_data) are new additions. Existing flag values are unchanged. No migration accidentally resets existing flag values. | High |
| TC-4390 | Migration 113 is idempotent (safe to run twice) | 1. Run migration 113 (CREATE TABLE editorial_editions, etc.). 2. Run it again. | Second run does not error. All CREATE TABLE statements use IF NOT EXISTS. All CREATE INDEX statements use IF NOT EXISTS. No duplicate data or constraint violations. | High |
| TC-4391 | Editorial tables have correct FK to users table | 1. Inspect migration 113 DDL for editorial_editions.realtor_id and editorial_voice_profiles.realtor_id FK definitions. | Both columns reference the correct parent table (e.g., REFERENCES auth.users(id) ON DELETE CASCADE). FK target is the users table, not listings or contacts. | High |
| TC-4392 | No circular dependency between editorial actions and newsletter actions | 1. Analyze import graph of src/actions/editorial.ts and src/actions/newsletters.ts. | editorial.ts does not import from newsletters.ts AND newsletters.ts does not import from editorial.ts at module level. Data sharing uses DB only. No circular module dependency. | High |
| TC-4393 | TypeScript compiles without errors after editorial files added | 1. Run npx tsc --noEmit from realestate-crm/. | TypeScript compilation reports 0 errors. All editorial type definitions are correct. No `any` type escapes cause downstream type errors. | High |
| TC-4394 | No new ESLint errors introduced by editorial files | 1. Run npm run lint from realestate-crm/. | ESLint reports 0 new errors. Any warnings introduced are pre-existing or acknowledged. Editorial files follow project ESLint config (no-unused-vars, react-hooks/rules-of-hooks, etc.). | High |
| TC-4395 | No unused imports in editorial action files | 1. Run ESLint with no-unused-vars rule on src/actions/editorial.ts and all editorial files. | Zero unused import warnings. All imported symbols are referenced in the file. Dead code cleanup completed before merge. | Medium |
| TC-4396 | No console.log statements in editorial production code | 1. Grep editorial source files for console.log statements. | Zero console.log calls in src/actions/editorial.ts, src/app/api/editorial/**, and src/components/editorial/**. Debug logging uses a logger utility or is removed. | Medium |
| TC-4397 | Server action return types correctly typed (no `any`) | 1. Inspect return type annotations on all functions in src/actions/editorial.ts. | All server action functions have explicit return types (e.g., Promise<{ edition: EditorialEdition } | { error: string }>). No return type is `any` or implicitly `any`. | High |
| TC-4398 | All editorial API routes return correct HTTP status codes | 1. Test each editorial API endpoint for error cases. 2. Verify status codes for: not found, unauthorized, validation error, server error. | 404 for not found, 401 for unauthenticated, 403 for unauthorized (wrong realtor), 400 for validation errors, 500 for server errors. No endpoint returns 200 for error conditions. | High |
| TC-4399 | Editorial routes not in sitemap | 1. Fetch /sitemap.xml or inspect Next.js sitemap generation config. | No /newsletters/editorial/* routes appear in the sitemap. Internal CRM routes are excluded from sitemap to prevent indexing. | Low |
| TC-4400 | robots.txt disallows editorial API routes | 1. Fetch /robots.txt. 2. Check for Disallow rules covering /api/editorial/*. | robots.txt includes "Disallow: /api/editorial/" or equivalent. Editorial API endpoints are not crawlable by search engines. | Low |
| TC-4401 | No NEXT_PUBLIC_ prefix on server-only secrets | 1. Search all editorial source files for NEXT_PUBLIC_ environment variable access. 2. Check that no server secrets use this prefix. | NEXT_PUBLIC_SUPABASE_URL is the only NEXT_PUBLIC_ variable used in editorial code (it is intended to be public). MLS API keys, Anthropic keys, and Resend keys are accessed without NEXT_PUBLIC_ prefix. | High |
| TC-4402 | No hardcoded API keys or secrets in editorial source files | 1. Search editorial files for strings matching key patterns (sk-, re_, Bearer, apikey). | Zero hardcoded API keys found. All secrets accessed via process.env.VARIABLE_NAME. No development/test keys committed to source. | High |
| TC-4403 | No hardcoded realtor_id or user_id in editorial queries | 1. Search editorial action and API files for hardcoded UUID strings. | No UUID literals present in DB queries. All queries use realtor_id = session.user.id or equivalent dynamic value. Hardcoded IDs would create a security vulnerability. | High |
| TC-4404 | Editorial editor does not import browser-only APIs at module level | 1. Import src/components/editorial/EditionEditor.tsx in a Node.js environment (SSR simulation). 2. Check for window/document references at module scope. | No ReferenceError: window is not defined or similar SSR errors. Browser-only APIs (window, document, navigator) are only accessed inside useEffect hooks or event handlers, not at module level. | High |
| TC-4405 | Editorial preview component safe for server-side rendering | 1. Render <EditorialPreview> component using ReactDOMServer.renderToString() in a Node.js context. | Component renders to HTML string without errors. No client-only APIs called during SSR. Output is valid HTML. | High |
| TC-4406 | BullMQ worker code not bundled into Next.js app | 1. Run Next.js build. 2. Inspect .next/server/ output for BullMQ worker files. | BullMQ worker code (editorial-worker.ts or similar) is not present in the Next.js build output. Worker runs as a separate process (standalone Node.js). No "bullmq is not a Next.js-compatible module" error. | High |
| TC-4407 | react-email render() not called in browser context | 1. Search editorial frontend components for calls to render() from @react-email/components. | render() calls are only present in: API routes, server actions, or the BullMQ worker. No component in src/components/editorial/ calls react-email render() directly. Preview uses a safe preview API endpoint instead. | High |
| TC-4408 | All editorial DB queries use tenant-scoped client (not admin client) | 1. Review all editorial action functions (getEditions, saveEdition, getBlocks, etc.). 2. Check which Supabase client is used. | All read/write operations use the authenticated Supabase server client (createServerClient with session-based auth). Admin client (createAdminClient) is not used for editorial queries. RLS enforces tenant isolation. | High |
| TC-4409 | External data cache fetch uses server-side fetch | 1. Review external_data_cache fetch code in editorial actions. | Fetch calls to Repliers, Bank of Canada, etc. use Node.js fetch or node-fetch (server-side). No browser fetch API is used. Requests happen server-side and are not visible to the browser. | High |
| TC-4410 | Editorial cron endpoints require Bearer CRON_SECRET header | 1. Call GET /api/editorial/cron/winner-selection without Authorization header. 2. Call with Authorization: Bearer wrong_secret. 3. Call with correct secret. | Requests 1 and 2 return HTTP 401 Unauthorized. Request 3 returns 200 and executes the cron job. CRON_SECRET is read from environment, not hardcoded. | High |
| TC-4411 | Editorial cron unauthorized request returns 401 | 1. Send POST to /api/editorial/cron/generate-scheduled with no Authorization header. | HTTP 401 returned immediately. Cron job does not execute. No DB changes made. Response body: {"error": "Unauthorized"}. | High |
| TC-4412 | No duplicate Next.js route conflicts with editorial routes | 1. Run npm run build or next build. 2. Check for any "conflicting pages" or "duplicate route" build warnings. | Build completes without route conflict warnings. No /newsletters/[id] and /newsletters/editorial/[id] collision. No catch-all routes accidentally capture editorial routes. | High |
| TC-4413 | Editorial feature branch does not break existing passing tests | 1. Run all existing automated tests (node scripts/test-email-marketing-ui.mjs, node scripts/qa-test-email-engine.mjs, node scripts/test-workflow-emails.mjs). | All previously passing tests continue to pass. Test counts: 1833 UI tests pass, 27/28 QA pipeline tests pass (known 1 skip). No new failures introduced by editorial code. | High |
| TC-4414 | Editorial code passes security review (no OWASP Top 10 violations) | 1. Run a security scan (OWASP ZAP or manual review) on editorial API routes. 2. Check for: SQL injection, XSS, IDOR, auth bypass, insecure direct object reference. | No critical or high-severity findings. All DB queries parameterized. All user input sanitized. All endpoints authenticated. resource IDs validated against realtor_id. No IDOR vulnerabilities in editorial edition/block access. | High |
