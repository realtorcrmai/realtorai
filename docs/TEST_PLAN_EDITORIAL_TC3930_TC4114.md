<!-- docs-audit: none --># QA Expansion — Section X, Y, Z, AA, AB: Component/System/Error/Onboarding Tests
## Test Range: TC-3930 to TC-4114
## Feature: Editorial Newsletter System

**Version:** 1.0
**Date:** 2026-04-15
**Scope:** EditionEditorClient, dnd-kit sorting, system integration, error boundaries, and first-use onboarding flows for the Editorial Newsletter module.

---

### Section X: Deep EditionEditorClient Tests (TC-3930 to TC-3969)

| TC# | Test Title | Test Steps | Expected Result | Priority |
|-----|-----------|------------|-----------------|----------|
| TC-3930 | EditionEditorClient initial render with edition prop | 1. Authenticate as a realtor with an existing draft edition. 2. Navigate to `/newsletters/editorial/[id]`. 3. Inspect the DOM for the `EditionEditorClient` component root. | EditionEditorClient mounts without error; edition title, status badge, and block list are all visible on screen. Edition `id` from the URL matches the prop passed to the component. | P0 |
| TC-3931 | Renders all blocks from blocks JSONB array | 1. Seed a test edition with 5 blocks of mixed types (hero, just_sold, quick_tip, cta, divider) in the `blocks` JSONB column. 2. Navigate to the edition editor. 3. Count rendered block editors in the DOM. | Exactly 5 block editors are rendered, one per entry in the `blocks` array, in the same order as the JSONB array. Each block editor shows the correct block type label. | P0 |
| TC-3932 | Block array empty shows empty state with "Add your first block" CTA | 1. Create an edition with an empty `blocks` array (`[]`) in the DB. 2. Navigate to the edition editor. | Empty state UI is displayed with the text "Add your first block" and a visible CTA button. No block editor rows are rendered. No JavaScript error in console. | P1 |
| TC-3933 | Block count badge in header shows correct number | 1. Seed edition with exactly 7 blocks. 2. Navigate to edition editor. 3. Locate the block count badge in the editor header. | The badge reads "7 blocks" (or equivalent label "7"). After adding one more block via the UI, the badge updates to "8 blocks" without page refresh. | P1 |
| TC-3934 | Hero block renders headline and subheadline text fields | 1. Seed edition with one hero block containing `{ type: "hero", headline: "Spring Market Is Here", subheadline: "Homes are moving fast in your neighbourhood" }`. 2. Open edition editor. 3. Locate the hero block editor. | The hero block editor shows a labelled "Headline" text input prefilled with "Spring Market Is Here" and a "Subheadline" text input prefilled with "Homes are moving fast in your neighbourhood". | P0 |
| TC-3935 | Just Sold block renders address, sale price, and days on market fields | 1. Seed edition with one just_sold block containing `{ type: "just_sold", address: "123 Main St", sale_price: 850000, days_on_market: 14 }`. 2. Open edition editor. | The just_sold block editor renders labeled fields for "Address" (prefilled "123 Main St"), "Sale Price" (prefilled "850000"), and "Days on Market" (prefilled "14"). | P0 |
| TC-3936 | Market commentary block renders rich textarea with word count | 1. Seed edition with one market_commentary block containing a 120-word commentary body. 2. Open edition editor and locate the market_commentary block. | A textarea is rendered with the 120-word body. A live word count indicator below the textarea reads "120 words". | P1 |
| TC-3937 | Rate watch block renders numeric rate input and label field | 1. Seed edition with a rate_watch block: `{ type: "rate_watch", rate_value: 5.19, rate_label: "5-Year Fixed" }`. 2. Open edition editor. | The rate_watch block editor shows a numeric input prefilled with "5.19" and a text input for rate label prefilled with "5-Year Fixed". | P1 |
| TC-3938 | Local intel block renders news_item text area and source URL field | 1. Seed edition with a local_intel block: `{ type: "local_intel", news_item: "New park opening on Cambie Street", source_url: "https://vancouver.ca/news/park" }`. 2. Open edition editor. | local_intel block editor renders a "News Item" textarea prefilled with the news text and a "Source URL" input prefilled with the URL. | P1 |
| TC-3939 | Neighborhood spotlight block renders description textarea and area name field | 1. Seed edition with a neighborhood_spotlight block: `{ type: "neighborhood_spotlight", area_name: "Kitsilano", description: "Tree-lined streets with ocean views..." }`. 2. Open edition editor. | The neighborhood_spotlight block editor shows an "Area Name" text input and a "Description" textarea, both prefilled correctly. | P1 |
| TC-3940 | Quick tip block renders tip_text textarea | 1. Seed edition with a quick_tip block: `{ type: "quick_tip", tip_text: "Stage your entryway to create a great first impression." }`. 2. Open edition editor. | A "Tip Text" textarea is rendered in the quick_tip block editor, prefilled with the provided tip. Character count is visible. | P2 |
| TC-3941 | Agent note block renders note textarea | 1. Seed edition with an agent_note block: `{ type: "agent_note", note: "Personal message from your realtor: rates may shift before summer." }`. 2. Open edition editor. | An "Agent Note" textarea is rendered in the agent_note block editor, prefilled with the note content. | P2 |
| TC-3942 | CTA block renders button_text and url fields | 1. Seed edition with a cta block: `{ type: "cta", button_text: "Book a Free Consultation", url: "https://calendly.com/agent" }`. 2. Open edition editor. | The cta block editor shows a "Button Text" input prefilled with "Book a Free Consultation" and a "URL" input prefilled with the Calendly link. | P1 |
| TC-3943 | Divider block shows minimal editor with no required fields | 1. Seed edition with a divider block: `{ type: "divider" }`. 2. Open edition editor. | The divider block renders a minimal UI (e.g., a preview of the horizontal rule and a label "Section Divider"). No required field inputs are shown. No validation errors appear. | P2 |
| TC-3944 | Hero block headline field enforces max 80 characters | 1. Open an edition editor with a hero block. 2. Click into the Headline field. 3. Type a string that is exactly 81 characters long. | The headline input stops accepting input at 80 characters. The 81st character is not entered. A character counter shows "80/80". No characters beyond the limit are stored. | P1 |
| TC-3945 | Hero block subheadline field enforces max 120 characters | 1. Open an edition editor with a hero block. 2. Click into the Subheadline field. 3. Attempt to paste a 125-character string. | Only the first 120 characters are accepted. The subheadline input value is truncated to 120 characters. Counter reads "120/120". | P1 |
| TC-3946 | Just sold block shows validation error when address is empty on save | 1. Open an edition editor with a just_sold block. 2. Clear the Address field entirely. 3. Click outside the field (blur) or press Tab to trigger validation. | A red inline error message appears below the Address field reading "Address is required". The block is not saved to the DB until a valid address is entered. | P1 |
| TC-3947 | Market commentary block shows validation error when body is under 80 words | 1. Open an edition editor with a market_commentary block. 2. Enter a 50-word body text in the commentary textarea. 3. Click outside the field (blur). | A validation message appears reading "Commentary must be at least 80 words (currently 50)." The block save is blocked until the word count reaches 80 or more. | P1 |
| TC-3948 | Rate watch block rejects non-numeric rate value | 1. Open an edition editor with a rate_watch block. 2. Clear the Rate Value field and type "five-point-two". 3. Blur the field. | An inline validation error appears: "Rate value must be a number (e.g. 5.19)." The field is highlighted in red. The invalid string is not persisted to the DB. | P1 |
| TC-3949 | Local intel block shows error when news_item is empty | 1. Open an edition editor with a local_intel block. 2. Clear the News Item textarea. 3. Blur the field or attempt to save. | Validation error "News item is required" appears below the textarea. Save is blocked until the field is non-empty. | P1 |
| TC-3950 | Neighborhood spotlight block shows error when description is empty | 1. Open an edition editor with a neighborhood_spotlight block. 2. Delete the Description textarea content. 3. Blur. | Inline error "Description is required" is shown. Block does not save until description is provided. | P1 |
| TC-3951 | Quick tip block enforces max 200 characters on tip_text | 1. Open an edition editor with a quick_tip block. 2. Paste a 210-character string into the Tip Text textarea. | Only the first 200 characters are accepted. Counter reads "200/200". Remaining 10 characters are discarded. | P1 |
| TC-3952 | Agent note block enforces max 300 characters on note | 1. Open an edition editor with an agent_note block. 2. Type exactly 301 characters into the Note textarea. | Input stops at 300 characters. Counter reads "300/300". The 301st character is rejected. | P1 |
| TC-3953 | CTA block requires button_text | 1. Open an edition editor with a cta block. 2. Clear the Button Text field. 3. Blur. | Validation error "Button text is required" appears. The block save is blocked. | P1 |
| TC-3954 | CTA block requires URL | 1. Open an edition editor with a cta block. 2. Clear the URL field. 3. Blur. | Validation error "URL is required" appears. Entering a non-URL string (e.g. "not a url") additionally shows "Must be a valid URL." | P1 |
| TC-3955 | Block save triggers updateBlock server action | 1. Open edition editor. 2. In a hero block, change the headline to "New Headline Text". 3. Wait 500ms after last keystroke. 4. Check server action call in network tab. | A call to the `updateBlock` server action is made with the correct `editionId`, `blockId`, and updated `{ headline: "New Headline Text" }` payload. | P0 |
| TC-3956 | Block save is debounced 500ms after last keystroke | 1. Open edition editor with a hero block. 2. Type rapidly into the headline field: type 5 characters within 300ms. 3. Monitor network requests for updateBlock. | Only one `updateBlock` call is made, triggered 500ms after the final keystroke, not after each individual character. | P1 |
| TC-3957 | Save shows "Saving..." indicator while request is in flight | 1. Open edition editor. 2. Change a field value. 3. Monitor the save indicator element in the header immediately after the debounce fires (before response). | The save indicator reads "Saving..." (with a spinner icon) during the period between request sent and response received. | P1 |
| TC-3958 | Save success shows "Saved" with checkmark | 1. Open edition editor. 2. Change a field value. 3. Wait for the updateBlock server action to respond with success. 4. Observe the save indicator. | The indicator transitions from "Saving..." to "Saved ✓" (green text with checkmark). The indicator fades out after approximately 2 seconds. | P1 |
| TC-3959 | Save error shows red indicator with retry button | 1. Open edition editor. 2. Simulate network failure (devtools → offline mode or intercept the updateBlock action to return an error). 3. Change a field value and wait for the debounce. | The save indicator shows a red error state with text "Save failed" and a "Retry" button. Clicking "Retry" re-sends the updateBlock action. | P0 |
| TC-3960 | All 10 block types can be added via the Add Block menu | 1. Open an edition editor with an empty blocks array. 2. Click the "Add Block" CTA. 3. In the block picker menu, verify all 10 block types are listed: hero, just_sold, market_commentary, rate_watch, local_intel, neighborhood_spotlight, quick_tip, agent_note, cta, divider. 4. Click each type and verify a new block appears. | All 10 block types are present in the picker. Each successfully adds a blank block of the correct type to the edition's block list. | P0 |
| TC-3961 | Block editor header shows correct block type label | 1. Seed edition with one block of each of the 10 types. 2. Open edition editor. | Each block editor row has a visible label identifying its type (e.g., "Hero Block", "Just Sold", "Rate Watch"). Labels match the `type` field in the JSONB. | P2 |
| TC-3962 | Block can be deleted via delete icon | 1. Open edition editor with 3 blocks. 2. Click the delete (trash) icon on the second block. 3. Confirm the deletion prompt if one appears. | The second block is removed from the list. The editor now shows 2 blocks. The block count badge updates to "2 blocks". The deletion is persisted via a removeBlock server action call. | P1 |
| TC-3963 | Block collapse/expand toggle works | 1. Open edition editor with a market_commentary block. 2. Click the collapse/toggle button on the block header. 3. Then click it again to expand. | Block body (textarea and fields) collapses out of view on first click. Re-clicking expands it. All field values are preserved across collapse/expand. | P2 |
| TC-3964 | Editing one block does not affect sibling block values | 1. Seed edition with two quick_tip blocks: tip A = "Tip One", tip B = "Tip Two". 2. Open edition editor. 3. Change tip A's text to "Tip One Modified". | Tip B's textarea still reads "Tip Two". The updateBlock call includes only the first block's ID, not the second block's. | P1 |
| TC-3965 | Block editor renders correctly for edition in "ready" status (read-only CTA fields) | 1. Seed an edition with `status = "ready"`. 2. Open the edition editor. | The block editors are visible but form fields are in read-only mode (inputs have `disabled` attribute or `readOnly` prop). A banner indicates "Edition is ready to send — editing is disabled." | P1 |
| TC-3966 | Block editor renders correctly for edition in "sent" status (all fields read-only) | 1. Seed an edition with `status = "sent"`. 2. Navigate to the edition editor URL. | All block fields are read-only/disabled. The "Save" indicator is hidden. A banner reads "This edition has been sent and cannot be edited." No updateBlock calls fire. | P0 |
| TC-3967 | Block editor renders correctly for edition in "generating" status (fields disabled with spinner) | 1. Seed an edition with `status = "generating"`. 2. Navigate to the edition editor URL. | All block fields are disabled. A full-width "Generating content..." loading overlay or banner is shown. Add Block and drag handles are hidden. | P1 |
| TC-3968 | Multiple simultaneous field changes queue correctly and save in order | 1. Open an edition with a hero block (headline and subheadline fields). 2. Rapidly update headline → wait 100ms → update subheadline within the same 500ms debounce window. | A single debounced updateBlock call is made with both the updated headline and subheadline values, not two separate calls. Both values appear in the request payload. | P1 |
| TC-3969 | EditionEditorClient does not re-mount on block list reorder | 1. Open edition editor with 3 blocks. 2. Reorder block 1 and block 3 via drag and drop. 3. Inspect React DevTools or add a mount counter via test utility. | The `EditionEditorClient` root component does not unmount and remount during the reorder. Only the internal block list order changes. Existing field values are preserved. | P1 |

---

### Section Y: dnd-kit/Sorting Deep Tests (TC-3970 to TC-4009)

| TC# | Test Title | Test Steps | Expected Result | Priority |
|-----|-----------|------------|-----------------|----------|
| TC-3970 | DndContext wraps BlockList component | 1. Open edition editor with 3+ blocks. 2. Inspect the React component tree (React DevTools or Playwright snapshot). | The `DndContext` provider from dnd-kit wraps the `BlockList` component. The `onDragStart`, `onDragEnd`, and `onDragCancel` callbacks are passed as props to `DndContext`. | P0 |
| TC-3971 | SortableContext receives correct items array | 1. Seed edition with 4 blocks having IDs: ["block-1", "block-2", "block-3", "block-4"]. 2. Open edition editor. 3. Inspect `SortableContext` props via React DevTools. | `SortableContext` receives an `items` array of ["block-1", "block-2", "block-3", "block-4"] in the same order as the DB blocks array. | P1 |
| TC-3972 | Each block has useSortable hook bound to unique ID | 1. Open edition editor with 3 blocks. 2. In React DevTools, inspect each block component for useSortable hook state. | Each block component has a `useSortable` hook instance with a unique `id` matching its block ID. No two blocks share the same sortable ID. | P1 |
| TC-3973 | Drag overlay renders dragged block at pointer position | 1. Open edition editor with 3 blocks. 2. Click and hold the drag handle of block 2 using Playwright `mouse.down`. 3. Move the mouse to a new position. 4. Take a screenshot. | A drag overlay is visible showing a preview of block 2's content following the cursor position. The original block 2 in the list shows reduced opacity (0.5) while dragging. | P1 |
| TC-3974 | dragStart fires setIsDragging(true) | 1. Open edition editor with 3+ blocks. 2. Begin dragging a block handle (mousedown + mousemove > 8px). 3. Check component state via React DevTools or data attribute on root element. | The `isDragging` state is `true` immediately after drag activation. A data attribute `data-dragging="true"` is applied to the editor root (or equivalent observable indicator). | P1 |
| TC-3975 | dragEnd fires reorderBlocks action with new order | 1. Open edition editor with blocks in order [A, B, C]. 2. Drag block A to position 3 (after C). 3. Release mouse (dragEnd). 4. Monitor server action calls. | The `reorderBlocks` server action is called with the new sorted order [B, C, A] and the correct `editionId`. The DB is updated to reflect the new block order. | P0 |
| TC-3976 | dragCancel restores original block order | 1. Open edition editor with blocks [A, B, C]. 2. Begin dragging block C toward position 1. 3. Trigger drag cancel (press Escape or programmatically fire `onDragCancel`). 4. Inspect the rendered block list. | The block list reverts to the original order [A, B, C]. No `reorderBlocks` server action call is made. The optimistic UI update is rolled back. | P1 |
| TC-3977 | Keyboard drag: Space to pick up block | 1. Open edition editor with 3+ blocks. 2. Tab to focus the drag handle of block 2. 3. Press Space. | Block 2 enters drag mode. An ARIA live region announces "Dragging: [block type]". The drag handle receives `aria-pressed="true"` or equivalent. | P1 |
| TC-3978 | Keyboard drag: arrow keys move block up and down | 1. With block 2 in drag mode (after Space to pick up), press the Down Arrow key once. 2. Then press the Up Arrow key twice. | Down Arrow moves block 2 to position 3. Each Up Arrow press moves it back up one position. The list renders the updated order in real time. ARIA live region announces "Moved to position X of Y". | P1 |
| TC-3979 | Keyboard drag: Space to drop block in new position | 1. Pick up block 1 with Space. 2. Press Down Arrow twice to move it to position 3. 3. Press Space to drop. 4. Monitor server actions. | The block is dropped at position 3. The `reorderBlocks` action fires with the new order. ARIA live region announces "Dropped at position 3." | P1 |
| TC-3980 | Keyboard drag: Escape cancels drag without reordering | 1. Pick up block 2 with Space on its drag handle. 2. Press Down Arrow to move it toward position 3. 3. Press Escape. | Block 2 returns to its original position (2). No `reorderBlocks` call is made. ARIA live region announces "Drag cancelled." | P1 |
| TC-3981 | Pointer sensor activationConstraint distance of 8px prevents accidental drag | 1. Open edition editor. 2. Simulate a mousedown and mousemove of only 4px (less than 8px threshold) on a block's drag handle. 3. Check if drag activates. | Drag does NOT activate. The block remains stationary. No `isDragging = true` state change fires. This is verified by checking that the drag overlay is not rendered. | P1 |
| TC-3982 | Block drag activation requires 8px movement (confirmed) | 1. Simulate mousedown and mousemove of exactly 8px on a drag handle. | Drag activates at exactly the 8px boundary. The drag overlay appears and `isDragging` transitions to `true`. | P1 |
| TC-3983 | Dragged block shows opacity 0.5 during drag | 1. Begin dragging block 2 using the drag handle (move more than 8px). 2. Take a DOM snapshot or CSS computed style check of the original block 2 element. | The original block 2 element has `opacity: 0.5` (or `opacity: 0.4`/similar) applied via CSS class or inline style while it is being dragged. | P2 |
| TC-3984 | Drop zone shows blue highlight when dragged item hovers | 1. Drag block 1's handle. 2. Move the cursor slowly over the position between block 2 and block 3. 3. Capture a screenshot at that hover position. | The drop zone between blocks 2 and 3 shows a blue highlight indicator (e.g., a 2px blue border or blue-tinted drop placeholder). The highlight appears only when the dragged item is directly over that zone. | P2 |
| TC-3985 | Sortable animation CSS transition is 200ms | 1. Open edition editor. 2. Drag and drop a block to reorder. 3. Inspect CSS transition on block elements using `getComputedStyle` or CSS DevTools. | Block elements have `transition: transform 200ms ease` applied during reorder animation. The animation duration is exactly 200ms (or within acceptable 180-220ms range). | P2 |
| TC-3986 | Blocks maintain edit state (unsaved field values) after reorder | 1. Open edition editor with blocks [hero, quick_tip, cta]. 2. Type "Unsaved Draft Tip" into the quick_tip textarea WITHOUT triggering a save (within debounce window). 3. Immediately drag the cta block above the quick_tip block. | After reorder, the quick_tip block still shows "Unsaved Draft Tip" in its textarea. The edit state is preserved through the reorder. | P1 |
| TC-3987 | Reorder persists to DB via updateEditionBlocks action | 1. Open edition editor with blocks [A, B, C]. 2. Drag A to position 2. 3. Wait for server action call. 4. Reload the page and inspect block order. | After reload, blocks appear in order [B, A, C], confirming the new order was persisted to the DB `blocks` JSONB column via `updateEditionBlocks`. | P0 |
| TC-3988 | Reorder optimistically updates UI before server confirms | 1. Intercept the `updateEditionBlocks` server action with a 2-second artificial delay. 2. Drag block 1 to position 3. | The UI immediately shows the new order [B, C, A] without waiting for the server response. A "Saving..." indicator appears but the list does not revert during the 2-second wait. | P1 |
| TC-3989 | Server error on reorder reverts to previous order | 1. Configure the `updateEditionBlocks` server action to return a 500 error. 2. Open edition editor with blocks [A, B, C]. 3. Drag block A to position 3. 4. Wait for the server response. | The UI reverts to the original order [A, B, C] after the error response. An error toast appears: "Failed to save new order. Please try again." | P0 |
| TC-3990 | Drag handle is on left side of block only | 1. Open edition editor with 3 blocks. 2. Inspect each block's DOM structure. 3. Attempt to initiate a drag from the right side of a block (content area). | Drag handles are rendered as a dedicated element on the left side of each block (e.g., `⠿` grip icon). Dragging from the main block content area does NOT initiate a drag. Only the left-side handle triggers dnd-kit. | P1 |
| TC-3991 | Drag disabled when edition status is "generating" | 1. Seed an edition with `status = "generating"`. 2. Open the edition editor. 3. Attempt to drag a block by its handle. | Drag handles are either hidden or have pointer-events disabled. The `useSortable` hook's `disabled` prop is `true`. No drag activation occurs. | P1 |
| TC-3992 | Drag disabled when edition status is "sent" | 1. Seed edition with `status = "sent"`. 2. Open edition editor. 3. Attempt to drag a block handle. | Drag handles are disabled/hidden. Dragging does not activate. A visual indicator (e.g., lock icon or greyed handles) shows drag is unavailable. | P1 |
| TC-3993 | DragOverlay unmounts when drag ends | 1. Drag a block from position 1 to position 3 and release. 2. Inspect the DOM after drop. | The `DragOverlay` component is no longer rendered in the DOM after `onDragEnd` fires. The drag overlay disappears cleanly. | P2 |
| TC-3994 | DragOverlay unmounts when drag is cancelled | 1. Begin dragging a block. 2. Press Escape to cancel. 3. Inspect DOM. | The `DragOverlay` is removed from DOM. No overlay ghost remains. | P2 |
| TC-3995 | Multiple rapid drags result in correct final order | 1. Open edition with blocks [A, B, C, D, E]. 2. Rapidly drag A to position 5, then D to position 1 (back-to-back within 500ms). 3. Wait for all server actions to complete. 4. Reload page. | The DB and UI both reflect the correct final order resulting from both drag operations in sequence. No race condition causes a stale order to overwrite the final state. | P1 |
| TC-3996 | Block drag works with keyboard when 5+ blocks are present | 1. Open edition editor with 6 blocks. 2. Tab to the drag handle of block 3. 3. Press Space to activate. 4. Press Down Arrow 3 times to move to position 6. 5. Press Space to drop. | Block 3 successfully moves to position 6. `reorderBlocks` fires with the updated order. ARIA announcements confirm each position change. | P1 |
| TC-3997 | Drag and drop works on touch devices (simulated) | 1. Set viewport to 375px wide (mobile). 2. Using Playwright `touchscreen.tap` followed by `touchscreen.move`, simulate a touch drag on a block handle. | Touch dragging activates the dnd-kit pointer sensor (touch events map to pointer events). The block can be reordered via touch. | P2 |
| TC-3998 | Block list re-renders efficiently (no full list unmount) | 1. Open edition editor with 5 blocks. 2. Drag block 5 to position 1. 3. Monitor React renders using React DevTools profiler or component mount counters. | Individual block components update their position but do not fully unmount and remount. Only blocks whose order changed re-render (not all 5). | P2 |
| TC-3999 | Drag handle has accessible aria-label | 1. Open edition editor. 2. Inspect the drag handle element of any block via DOM. | Each drag handle has `aria-label="Drag to reorder [block type]"` or equivalent. The role is `button` or `presentation` with appropriate ARIA attributes for keyboard drag support. | P1 |
| TC-4000 | Dropping block at same position fires no server action | 1. Pick up block 2 with keyboard Space. 2. Press Down Arrow once (move to position 3). 3. Press Up Arrow once (move back to position 2). 4. Press Space to drop. | Block returns to position 2 (original position). The `reorderBlocks` server action is NOT called since the order did not change. | P2 |
| TC-4001 | Blocks with very long content don't distort drag overlay | 1. Seed a market_commentary block with 500 words of text. 2. Drag that block's handle. 3. Observe the drag overlay. | The drag overlay for the market_commentary block renders with the same width as the editor column. Long content is truncated or scrollable within the overlay. The overlay does not overflow the viewport. | P2 |
| TC-4002 | Block reorder persists after page refresh | 1. Drag blocks into order [C, A, B]. 2. Wait for `updateEditionBlocks` to succeed. 3. Refresh the page (hard reload). | After refresh, blocks are rendered in the order [C, A, B], confirming persistence to the DB JSONB array. | P0 |
| TC-4003 | Drag doesn't trigger save debounce for field values | 1. Open edition editor. 2. Type in a hero headline (start debounce timer). 3. Within the 500ms debounce window, drag the hero block to a new position. | The reorder fires `updateEditionBlocks` immediately. The field change debounce timer continues independently and fires `updateBlock` after its 500ms completes. Both actions execute without interference. | P1 |
| TC-4004 | SortableContext strategy is verticalListSortingStrategy | 1. Inspect the `SortableContext` component props in React DevTools. | The `strategy` prop on `SortableContext` is set to `verticalListSortingStrategy` (imported from @dnd-kit/sortable). | P2 |
| TC-4005 | Block type label in drag overlay matches block type | 1. Drag a `rate_watch` block. 2. Inspect the drag overlay content. | The drag overlay shows the block type label "Rate Watch" (or equivalent human-readable label) so the user knows what they are dragging. | P2 |
| TC-4006 | Dragging last block does not create phantom gap | 1. Open edition with exactly 3 blocks. 2. Drag the last block (position 3) to position 1. 3. Inspect the block list DOM after drop. | No empty/ghost DOM element remains at the bottom of the list. The list contains exactly 3 block elements. | P2 |
| TC-4007 | Drag handle not rendered when edition has 0 blocks | 1. Open an edition with an empty blocks array. 2. Inspect the DOM for drag handle elements. | No drag handle elements are rendered. The empty state "Add your first block" CTA is shown instead. | P2 |
| TC-4008 | Drag handle not rendered when edition has exactly 1 block | 1. Seed an edition with exactly 1 block. 2. Open edition editor. 3. Inspect drag handle visibility. | The drag handle for the single block is either hidden (no reordering possible with one item) or present but non-functional. No drag activation is possible. UX decision should be consistent — document which behavior is expected. | P3 |
| TC-4009 | Reorder updates SortableContext items array synchronously | 1. Open edition with blocks [A, B, C]. 2. Drag A to position 3 (optimistic update). 3. Inspect `SortableContext` items prop before server response returns. | The `items` array in `SortableContext` is immediately updated to [B, C, A] during the optimistic update, not waiting for the server response. | P1 |

---

### Section Z: System Integration Tests (TC-4010 to TC-4049)

| TC# | Test Title | Test Steps | Expected Result | Priority |
|-----|-----------|------------|-----------------|----------|
| TC-4010 | Creating edition triggers BullMQ job enqueue | 1. Navigate to `/newsletters/editorial`. 2. Click "New Edition" and complete the creation form. 3. Monitor the BullMQ dashboard or check the `email_events` table for a new generation job row. | A BullMQ job of type `generate_editorial_edition` is enqueued within 1 second of edition creation. The job payload includes the correct `editionId` and `realtorId`. | P0 |
| TC-4011 | BullMQ job is picked up within 5 seconds of enqueue | 1. Create a new edition (triggers enqueue per TC-4010). 2. Watch the BullMQ job queue dashboard or poll the edition `status` column every 500ms. | The job transitions from `waiting` to `active` within 5 seconds. The edition `status` in the DB updates from `draft` to `generating` within 5 seconds of creation. | P0 |
| TC-4012 | Generation status polling correctly reads from DB | 1. While an edition is in `generating` status, navigate to the edition editor. 2. Observe the UI for status indicators. 3. Manually update the DB `status` to `ready` while the page is open. | The editor detects the status change (via polling or real-time subscription) and updates the UI from "Generating..." to "Ready to Send" without requiring a page refresh. Polling interval should be 3–5 seconds. | P1 |
| TC-4013 | Generation completes — edition status transitions to "ready" | 1. Create a new edition with valid voice profile. 2. Allow the BullMQ generation worker to run to completion. 3. Check the `editions` table `status` column after job completion. | The `status` column updates to `ready`. The `blocks` JSONB column is populated with AI-generated content for all standard block types. `generated_at` timestamp is set. | P0 |
| TC-4014 | Send triggers Resend API call with correct payload | 1. Open a `ready` edition. 2. Click "Send Edition". 3. Confirm the send dialog. 4. Monitor outbound API calls (via network tab or test spy on Resend SDK). | A POST call is made to `https://api.resend.com/emails` (or Resend SDK equivalent). The payload includes `from`, `to` (array of subscribed contact emails), `subject`, and `html` (rendered edition HTML). | P0 |
| TC-4015 | Resend API success increments send_count | 1. Send an edition with Resend configured and working. 2. Resend responds with 200. 3. Check the `editions` table row for the sent edition. | The `send_count` column increments by the number of recipients. `status` updates to `sent`. `sent_at` timestamp is set. `newsletter_analytics` row is created or updated. | P0 |
| TC-4016 | Resend API failure does not increment send_count | 1. Configure Resend to return a 4xx error (e.g., invalid `from` domain). 2. Attempt to send an edition. | `send_count` remains unchanged. `status` remains `ready` (not `sent`). An error is logged in the `email_events` table with the Resend error code. An error toast appears in the UI. | P0 |
| TC-4017 | Webhook open event inserts newsletter_events row | 1. Send an edition to a test contact. 2. Simulate an `email.opened` webhook POST to `/api/webhooks/resend` with the correct `message_id` and contact email. 3. Verify the `newsletter_events` table. | A new row is inserted into `newsletter_events` with `event_type = "open"`, the correct `newsletter_id`, `contact_id` (resolved from email), and `occurred_at` timestamp. | P1 |
| TC-4018 | Webhook click event inserts newsletter_events row and updates contact intelligence | 1. Simulate an `email.clicked` webhook POST with `message_id`, contact email, and clicked URL. 2. Check `newsletter_events` and `contacts.newsletter_intelligence`. | `newsletter_events` row inserted with `event_type = "click"` and the clicked URL. The contact's `newsletter_intelligence` JSONB is updated: `engagement_score` incremented, clicked URL appended to `click_history`. | P1 |
| TC-4019 | Webhook bounce event marks contact as bounced | 1. Simulate an `email.bounced` webhook POST for a known contact. 2. Check the `contacts` table row for that contact. | The contact's `email_status` (or equivalent field) is updated to `bounced`. The contact is excluded from future sends. A `newsletter_events` row is inserted with `event_type = "bounce"`. | P1 |
| TC-4020 | Webhook unsubscribe event revokes CASL consent | 1. Simulate an `email.unsubscribed` webhook POST for a known contact with CASL consent. 2. Check the `contacts` table. | `casl_consent_given` is set to `false`. `casl_consent_date` is cleared or a `casl_revoked_at` timestamp is set. Contact is removed from future newsletter sends. A `newsletter_events` row is inserted with `event_type = "unsubscribe"`. | P0 |
| TC-4021 | Voice profile update causes next generation to use new rules | 1. Update the `editorial_voice_profiles` table for the test realtor to add a new rule: "Always use a friendly first-name greeting." 2. Trigger a new edition generation. 3. Inspect the generated agent_note block content. | The generated content includes a friendly first-name greeting, reflecting the new voice rule. The old content (without the greeting) is not reproduced. | P1 |
| TC-4022 | External data cache miss triggers a live API call | 1. Clear any cached external data for the test realtor's market (e.g., delete rows from the data cache table or set `cached_at` to expire). 2. Trigger an edition generation. 3. Monitor outbound HTTP calls. | At least one outbound API call is made to the relevant external data provider (e.g., MLS stats API, rate feed API) to fetch fresh data. The new data is stored in the cache table. | P1 |
| TC-4023 | External data cache hit skips API call | 1. Ensure the external data cache has a fresh entry (less than TTL expiry) for the test realtor's market. 2. Trigger an edition generation. 3. Monitor outbound HTTP calls. | No outbound call is made to the external data provider. The generator reads directly from the cache table. Generation completes using cached data. | P1 |
| TC-4024 | Cache TTL expiry triggers fresh API call | 1. Manually set the external data cache row `cached_at` to a time older than the TTL (e.g., 25 hours ago for a 24-hour TTL). 2. Trigger edition generation. | The system detects the expired cache, makes a fresh external API call, updates the cache row with new `cached_at`, and uses the fresh data for generation. | P1 |
| TC-4025 | edition_number auto-increments per realtor (tenant-scoped) | 1. As Realtor A, create three editions in sequence. 2. As Realtor B (separate tenant), create two editions. 3. Query `edition_number` values for each realtor. | Realtor A's editions have `edition_number` = 1, 2, 3. Realtor B's editions have `edition_number` = 1, 2 (starting from 1 independently). Numbers do not cross-pollinate between tenants. | P0 |
| TC-4026 | newsletter_analytics row is created on first edition send | 1. Send a new edition for the first time. 2. Query the `newsletter_analytics` table for the edition's `id`. | A new `newsletter_analytics` row is created with `edition_id`, `realtor_id`, `sent_count` (matching number of recipients), `open_count = 0`, `click_count = 0`, `created_at`. | P1 |
| TC-4027 | newsletter_analytics open_count updates correctly after webhook | 1. Send an edition. 2. Simulate 5 distinct `email.opened` webhook events for 5 different contacts. 3. Query `newsletter_analytics`. | `newsletter_analytics.open_count` is 5. `open_rate` (if computed) is `5 / sent_count * 100`. Values reflect all 5 unique open events. | P1 |
| TC-4028 | newsletter_analytics click_count updates correctly after webhook | 1. Send an edition. 2. Simulate 3 `email.clicked` webhook events (2 from one contact, 1 from another). 3. Query `newsletter_analytics`. | `click_count` = 3 (total clicks including repeat clicks from same contact). Alternatively, if unique clicks: `click_count` = 2. Clarify implementation — test against documented behavior. | P1 |
| TC-4029 | Feature flag off causes /newsletters/editorial to return 404 | 1. Set the `editorial_newsletter` feature flag to `false` in the `users.enabled_features` JSONB for the test realtor (or global feature flag config). 2. Navigate to `/newsletters/editorial`. | HTTP 404 response is returned. The page renders a "404 Not Found" or "Feature not available" screen. No editorial newsletter UI is accessible. | P0 |
| TC-4030 | Feature flag on causes /newsletters/editorial to be accessible | 1. Ensure `editorial_newsletter` feature flag is `true`. 2. Navigate to `/newsletters/editorial`. | HTTP 200 response. The editorial newsletters dashboard renders correctly. All UI elements (edition list, New Edition button, etc.) are present. | P0 |
| TC-4031 | Plan check blocks send when realtor is over tier send limit | 1. Set the test realtor's plan to `free` which has a limit of 100 sends/month. 2. Seed the DB to show 100 sends this month. 3. Attempt to send another edition. | The send is blocked. A plan limit modal or toast appears: "You've reached your monthly send limit. Upgrade your plan to send more." The edition status remains `ready` and `send_count` is not incremented. | P0 |
| TC-4032 | Plan check allows send when realtor is under tier send limit | 1. Set test realtor plan to `professional` with 10,000 sends/month. 2. Current month sends = 50. 3. Send an edition to 200 contacts. | Send proceeds without a plan gate. Edition status transitions to `sent` and `send_count` = 200. | P1 |
| TC-4033 | Edition creation scoped to realtor tenant (multi-tenancy) | 1. Create editions as Realtor A and Realtor B using separate authenticated sessions. 2. As Realtor A, query the editions list API. | Only Realtor A's editions are returned. Realtor B's editions are not visible. RLS policy enforces `realtor_id` isolation. | P0 |
| TC-4034 | Generation job payload contains correct tenant context | 1. Create an edition as Realtor A. 2. Inspect the BullMQ job data payload. | The job payload includes `realtorId: <realtor_a_id>`, `editionId`, and any voice profile metadata. The worker uses `realtorId` to fetch tenant-specific contacts and voice profile. | P1 |
| TC-4035 | Edition send only targets subscribed contacts (CASL compliant) | 1. Realtor A has 10 contacts: 8 with `casl_consent_given = true`, 2 with `false`. 2. Send an edition to "All Contacts" segment. | Only 8 contacts receive the email. Resend API call `to` array contains exactly 8 email addresses. The 2 non-consented contacts are excluded. | P0 |
| TC-4036 | Generation worker retries on Claude API transient error | 1. Mock the Claude API to return a 503 on the first call and succeed on the second call. 2. Trigger edition generation. | BullMQ retries the job. On the second attempt, Claude API returns 200. Generation completes successfully. Edition status transitions to `ready`. Retry count is logged. | P1 |
| TC-4037 | Edition generation worker marks job as failed after max retries | 1. Mock Claude API to always return 503. 2. Set BullMQ max retries to 3. 3. Trigger edition generation. 4. Wait for all retries to exhaust. | After 3 retries, the BullMQ job status is `failed`. The edition `status` updates to `error`. An error message is saved to `editions.error_message`. Realtor is notified (in-app notification or email). | P1 |
| TC-4038 | Send confirmation dialog shows recipient count before sending | 1. Open a `ready` edition. 2. Click "Send Edition". 3. Inspect the send confirmation modal/dialog. | The confirmation dialog displays: "Send to [N] subscribers?" where N is the count of CASL-consented contacts in the target segment. The count is accurate. | P1 |
| TC-4039 | Resend from-email domain matches verified realtor domain | 1. Check the `RESEND_FROM_EMAIL` env var and verified domain config. 2. Send an edition and capture the outbound Resend API call. | The `from` field in the Resend request uses `RESEND_FROM_EMAIL` value (e.g. `team@realtors360.ai`). The domain is verified in Resend. Delivery succeeds without SPF/DKIM failure. | P0 |
| TC-4040 | Edition list shows correct status badges for each lifecycle state | 1. Seed editions in states: `draft`, `generating`, `ready`, `sent`, `error`. 2. Navigate to `/newsletters/editorial`. | Each edition row shows a status badge matching its state: "Draft" (grey), "Generating" (amber spinner), "Ready" (green), "Sent" (blue with date), "Error" (red). | P1 |
| TC-4041 | Sent edition shows send date and open rate in list view | 1. Seed a `sent` edition with `sent_at = 2026-04-10` and `newsletter_analytics` showing 40% open rate. 2. Navigate to the editorial editions list. | The sent edition row shows "Sent Apr 10, 2026" and "40% open rate" (or "40% opens"). | P2 |
| TC-4042 | Edition duplication creates new draft with same block structure | 1. Open a `sent` edition with 5 blocks. 2. Click "Duplicate Edition". | A new edition is created with `status = draft`, `edition_number` = next increment, and the same 5 blocks (cloned from the original). The new edition has a `created_at` of now. No `sent_at` or `send_count`. | P2 |
| TC-4043 | Generation progress visible via status bar during generation | 1. Trigger edition generation. 2. Navigate to the edition editor while status is `generating`. | A progress indicator or status bar is visible showing generation progress (e.g. "Generating block 3 of 6..." or a spinner with "AI is building your edition"). | P2 |
| TC-4044 | Edition send creates entry in communications table for each recipient | 1. Send an edition to 5 contacts. 2. Query the `communications` table for entries related to the edition. | 5 rows are inserted into `communications` with `channel = "email"`, `direction = "outbound"`, `body = edition subject line`, and `contact_id` matching each recipient. | P2 |
| TC-4045 | External data fetch errors are caught and generation continues with fallback | 1. Configure the external data API to return 500. 2. Trigger edition generation. | Generation continues using fallback copy (e.g. "Market data temporarily unavailable") for the affected blocks. The edition reaches `ready` status with a partial data warning, not a full failure. | P1 |
| TC-4046 | Concurrent edition generation requests for same realtor are queued | 1. Simultaneously trigger two edition generation requests for the same realtor (two rapid edition creates). | Both jobs are queued in BullMQ. They execute sequentially, not concurrently, for the same realtor queue. No data corruption occurs from parallel Claude API calls using the same voice profile. | P1 |
| TC-4047 | BullMQ job includes edition version to prevent stale writes | 1. Trigger generation, then immediately manually update a block in the edition. 2. Allow the generation job to complete. | The generation job checks the edition version/`updated_at` before writing blocks. If the edition was manually updated after the job was enqueued, the job does not overwrite manual changes (or it appends only AI-generated blocks, not overwriting manual ones). | P1 |
| TC-4048 | newsletter_events are scoped to correct realtor tenant | 1. As Realtor A, send edition and receive open webhooks. 2. As Realtor B, query `/newsletters/analytics`. | Realtor B's analytics page shows 0 events from Realtor A's sends. `newsletter_events` rows are scoped by `realtor_id`. | P0 |
| TC-4049 | High recipient count send (1000+) completes within 30 seconds | 1. Create a realtor with 1,000 CASL-consented contacts. 2. Send an editorial edition to "All Contacts". 3. Time the operation from click to `status = sent`. | The send operation completes within 30 seconds. Resend batch API or multiple API calls are handled efficiently. No timeout errors. `send_count = 1000` in `newsletter_analytics`. | P1 |

---

### Section AA: Error Boundary and Resilience Tests (TC-4050 to TC-4089)

| TC# | Test Title | Test Steps | Expected Result | Priority |
|-----|-----------|------------|-----------------|----------|
| TC-4050 | EditorErrorBoundary catches render error in BlockEditor | 1. Seed an edition block with a malformed `type` value (e.g., `type: "unknown_type_xyz"`) that causes the block editor component to throw during render. 2. Open the edition editor. | The `EditorErrorBoundary` catches the render error. The faulty block shows a contained error state (not a full page crash). Other blocks render normally. Console shows a React error boundary caught error. | P0 |
| TC-4051 | Error boundary shows fallback UI with "Something went wrong" message and retry button | 1. Trigger a block render error (as in TC-4050). 2. Inspect the fallback UI rendered by the error boundary. | The fallback UI shows "Something went wrong" with a description of the issue and a "Try Again" or "Retry" button. The rest of the editor (header, other blocks) remains functional. | P0 |
| TC-4052 | Retry button resets error boundary state | 1. Trigger error boundary for a block. 2. Fix the underlying cause (e.g., reset the block `type` via DB admin). 3. Click the "Retry" button on the fallback UI. | The error boundary state is reset. The block editor attempts to re-render. If the underlying issue is resolved, the block renders correctly. If still broken, the boundary catches it again (no infinite loop). | P1 |
| TC-4053 | Generation error shows specific error message, not generic | 1. Configure the generation worker to return a specific Claude API error: `{"type": "invalid_request_error", "message": "Prompt exceeds maximum token length"}`. 2. Trigger generation. 3. Inspect the edition error state UI. | The edition shows an error state with the specific message "Generation failed: Prompt exceeds maximum token length" (or condensed version). NOT a generic "Something went wrong." The `editions.error_message` column stores the raw error. | P1 |
| TC-4054 | Generation partial failure shows which blocks failed | 1. Seed a generation job that succeeds for 3 blocks but fails for 2 (market_commentary and rate_watch). 2. Allow generation to complete with partial failure. 3. Open edition editor. | The editor shows 3 blocks with AI content and 2 blocks with error states. Each error block displays "Failed to generate: [block type]" with a "Regenerate Block" action. The edition status is `partial` or `ready_with_errors`. | P1 |
| TC-4055 | Network timeout on save shows retry option (not silent failure) | 1. Open edition editor. 2. Intercept the `updateBlock` server action to simulate a network timeout (30 seconds with no response). 3. Change a field value and trigger debounce. | After the request times out, the save indicator shows a red error state: "Save timed out — changes may not be saved." A "Retry" button appears. Clicking "Retry" re-sends the `updateBlock` action. No silent failure. | P0 |
| TC-4056 | Network timeout on send shows error toast and edition stays in "ready" state | 1. Open a `ready` edition. 2. Intercept the send API call to simulate a 30-second timeout. 3. Click "Send Edition" and confirm. 4. Monitor edition status. | An error toast appears: "Send timed out. Your edition has not been sent. Please try again." The edition `status` remains `ready`. `send_count` is not incremented. | P0 |
| TC-4057 | Supabase connection error on edition load shows error page | 1. Simulate a Supabase connection failure (e.g., temporarily set `NEXT_PUBLIC_SUPABASE_URL` to an invalid value in a test environment, or mock the Supabase client to throw). 2. Navigate to an edition editor URL. | A full-page error state is shown: "Unable to load edition. Check your connection and try again." with a Reload button. No partial/broken UI is rendered. | P0 |
| TC-4058 | Claude API 429 (rate limit) during generation shows user-friendly message | 1. Mock the Claude API to return HTTP 429 with `Retry-After: 120` header. 2. Trigger edition generation. 3. Wait for the error state to propagate to the UI. | The edition error state shows: "AI is busy — generation will retry in 2 minutes." The BullMQ job is not marked as failed; it is delayed by `Retry-After` seconds and retried automatically. | P1 |
| TC-4059 | Claude API 500 shows "AI temporarily unavailable" message | 1. Mock the Claude API to return HTTP 500 for all requests. 2. Trigger generation. 3. Exhaust retries (3 attempts). | After 3 failed retries, the edition status is `error` and the UI shows "AI temporarily unavailable. Please try generating again later." The specific HTTP 500 code may be logged internally but not shown to the user. | P1 |
| TC-4060 | Resend API 422 shows detailed "Email sending failed" message | 1. Configure Resend to return HTTP 422 with body `{"name": "validation_error", "message": "Invalid 'from' address: domain not verified"}`. 2. Attempt to send an edition. | An error toast or modal displays: "Email sending failed: Invalid 'from' address: domain not verified." The edition `status` remains `ready`. Actionable guidance (e.g., "Verify your sending domain in Settings") is shown. | P1 |
| TC-4061 | Redis/BullMQ unavailable shows delayed generation warning | 1. Simulate Redis/BullMQ being unavailable (mock connection refusal to Redis). 2. Create a new edition (which triggers enqueue). | A non-blocking warning banner appears: "Content generation is temporarily delayed. Your edition will be generated shortly." The edition is created in `draft` status. A retry mechanism attempts re-enqueue. | P1 |
| TC-4062 | Invalid block JSONB in DB shows block-level error, not full editor crash | 1. Directly write a malformed block entry to the `blocks` JSONB column (e.g., `[{ "type": "hero", "headline": null, "_corrupt": true, ""}]`). 2. Open the edition editor. | The corrupt block renders a block-level error card: "Block data is unreadable." Other valid blocks render normally. The editor header and navigation are unaffected. No full-page crash. | P0 |
| TC-4063 | Missing RESEND_API_KEY detected at startup with clear error | 1. Start the newsletter service with `RESEND_API_KEY` removed from the environment. 2. Check server startup logs. | The service logs a clear startup error: "FATAL: Missing required env var RESEND_API_KEY. Email sending will not function." The service does not crash silently — it either refuses to start or marks the email feature as disabled. | P0 |
| TC-4064 | Missing ANTHROPIC_API_KEY detected at startup with clear error | 1. Start the newsletter service with `ANTHROPIC_API_KEY` unset. 2. Check server startup logs. | Server logs: "FATAL: Missing required env var ANTHROPIC_API_KEY. AI generation will not function." Generation jobs fail fast with a descriptive error rather than a cryptic API auth failure at runtime. | P0 |
| TC-4065 | Edition with null blocks array handled gracefully | 1. Seed an edition row with `blocks = NULL` (SQL NULL, not empty array). 2. Open the edition editor. | The editor renders the empty state ("Add your first block") rather than crashing. No `TypeError: Cannot read properties of null (reading 'map')` or similar error. Console shows no unhandled errors. | P1 |
| TC-4066 | Edition with corrupted voice_rules JSONB skips voice injection gracefully | 1. Seed the `editorial_voice_profiles` table with a row where `voice_rules` is a malformed JSON string or contains unexpected keys. 2. Trigger edition generation for this realtor. | Generation proceeds. The malformed voice_rules are ignored (skipped, not injected). A warning is logged: "Voice profile could not be parsed — generating with default style." The edition reaches `ready` status. | P1 |
| TC-4067 | Concurrent send attempt (double-click) is idempotent | 1. Open a `ready` edition. 2. Click "Send Edition" twice in rapid succession (within 200ms). 3. Check the number of Resend API calls made and the final `send_count`. | Only one Resend API call is made. `send_count` increments by N (recipients) exactly once, not 2N. The "Send" button is disabled immediately after the first click to prevent double-send. | P0 |
| TC-4068 | Session expiry mid-save shows "Session expired" modal | 1. Open edition editor and keep the tab open until the NextAuth session expires (or manually clear the session cookie via DevTools). 2. Attempt to edit a field and trigger the debounce save. | The `updateBlock` server action returns a 401 (unauthorized). The UI shows a modal: "Your session has expired. Please log in again to save your changes." The modal has a "Log In" button that redirects to `/login` while preserving the current URL as the redirect-back param. | P0 |
| TC-4069 | CORS error on external data API handled with Claude fallback | 1. Configure the external data fetch URL to a domain that returns a CORS error (or mock a network-level CORS failure). 2. Trigger edition generation. | The generation worker catches the CORS/network error, logs a warning, and instructs Claude to generate the affected block using only its training knowledge and any available CRM data. The block is generated with a disclaimer note (e.g., "Note: live market data unavailable"). | P1 |
| TC-4070 | Duplicate send attempt after status is "sent" is blocked | 1. Send an edition successfully (`status = sent`). 2. Attempt to call the send action again (e.g., via a direct API call or by manipulating the UI state). | The send action checks the current edition `status` and rejects the request: "This edition has already been sent." HTTP 409 or equivalent. `send_count` is not changed. | P0 |
| TC-4071 | Generation fails for edition with no subscribed contacts | 1. Create a realtor with 0 CASL-consented contacts. 2. Trigger edition generation. | Generation proceeds (content is AI-generated regardless of recipient count). Edition reaches `ready` status. On attempting to send, a warning appears: "You have 0 subscribed contacts — no emails will be sent." | P2 |
| TC-4072 | Long-running generation (>60 seconds) does not leave edition in "generating" forever | 1. Mock the Claude API to respond after a 70-second delay. 2. Trigger generation. 3. Monitor edition status. | BullMQ worker timeout (e.g., 120 seconds) fires before the job becomes permanently stuck. If the worker times out, the edition status is updated to `error` with message "Generation timed out." Not permanently stuck in `generating`. | P1 |
| TC-4073 | Error state in edition list allows retry from list view | 1. Seed an edition with `status = error`. 2. Navigate to the editorial editions list. | The error edition row has a "Retry Generation" button visible in the list. Clicking it re-enqueues the BullMQ generation job. The edition status updates to `generating`. | P1 |
| TC-4074 | Error boundary does not swallow errors silently | 1. Trigger a block render error. 2. Check the browser console. | Even though the error boundary handles the error gracefully in the UI, the error is still logged to the console with the full stack trace (React re-throws in development mode). In production, the error is logged to the monitoring/error tracking system (Sentry or equivalent). | P1 |
| TC-4075 | Block save failure does not lose field value in UI | 1. Open edition editor. 2. Type "Important Market Note" in a market_commentary block. 3. Simulate a save failure (intercept `updateBlock` to return error). | The field still shows "Important Market Note" in the textarea after the failed save. The value is not cleared. The user can correct the issue (e.g., reconnect) and retry without re-typing. | P1 |
| TC-4076 | Generation error message stored in DB for support diagnosis | 1. Trigger a generation that fails with a specific Claude API error. 2. Query the `editions` table `error_message` column. | The `error_message` column contains the raw error text and error type from Claude (or the internal error). The error is suitable for a support team to diagnose (not empty, not truncated to 1 char). | P2 |
| TC-4077 | Send action validates edition status before calling Resend | 1. Manually set an edition's `status` to `draft` in the DB. 2. Attempt to call the send server action directly (bypass UI guard). | The send action performs a DB check: if `status != "ready"`, it returns an error: "Cannot send edition in current status: draft." No Resend API call is made. | P0 |
| TC-4078 | Unhandled promise rejection in generation worker is caught and logged | 1. Mock an unhandled promise rejection in the generation worker (e.g., a database write that throws without try/catch). | BullMQ catches the unhandled rejection and marks the job as failed. The edition status updates to `error`. No node process crash. Worker continues processing the next job. | P1 |
| TC-4079 | Block editor shows stale data warning if edition was modified by another session | 1. Open edition editor in Tab A. 2. Open the same edition in Tab B and update a block. 3. Return to Tab A and attempt to save a change. | Tab A detects the optimistic concurrency conflict (e.g., version mismatch). A warning appears: "This edition was updated in another window. Reload to see the latest changes." Saving in Tab A is blocked until the user acknowledges. | P2 |
| TC-4080 | Generation worker respects IP warm-up governor limits | 1. Inspect the generation worker config for the IP warm-up governor (max sends per day for new sending domains). 2. Trigger sends that would exceed the daily warm-up cap. | Sends above the warm-up limit are queued for the next day or show a "Daily send limit reached for domain warm-up (Day 3 limit: 500)" message. The governor prevents deliverability damage. | P2 |
| TC-4081 | Failed send attempt creates error notification for realtor | 1. Trigger a send that fails (Resend returns 500). | An in-app notification is created for the realtor via `src/lib/notifications.ts`: "Edition send failed: [edition title]. Please try again." The notification appears in the notification dropdown. | P1 |
| TC-4082 | Resend webhook signature validation rejects unsigned requests | 1. Send a POST to `/api/webhooks/resend` without the `Resend-Signature` header. | HTTP 401 is returned. The webhook is not processed. No `newsletter_events` row is inserted. Console logs: "Webhook signature verification failed." | P0 |
| TC-4083 | Resend webhook signature validation accepts correctly signed requests | 1. Send a POST to `/api/webhooks/resend` with a correctly HMAC-signed `Resend-Signature` header using `RESEND_WEBHOOK_SECRET`. | HTTP 200 is returned. The webhook is processed. Correct `newsletter_events` row is inserted. | P0 |
| TC-4084 | Edition editor shows network status indicator when offline | 1. Open edition editor. 2. Disable network in browser DevTools (offline mode). 3. Attempt to edit a field. | A network status banner appears at the top of the editor: "You are offline — changes will not be saved." The save debounce still fires but fails visibly with the red error indicator. | P2 |
| TC-4085 | Deleting a block while a save is in flight does not cause duplicate save | 1. Edit a block (starts 500ms debounce). 2. Within the debounce window, click delete on that same block. 3. Confirm the delete. | The pending `updateBlock` debounce for the deleted block is cancelled. Only the `removeBlock` action fires. No orphaned `updateBlock` call is made for a block that no longer exists. | P1 |
| TC-4086 | Generation error for one block does not prevent sending the edition | 1. Seed an edition with 5 blocks, 4 with valid AI content and 1 with `status: "error"` in its block data. 2. Attempt to send the edition. | The send action either: (a) blocks with "1 block has generation errors — fix before sending" OR (b) sends with the failed block omitted and shows a warning. Document which behavior is expected and test accordingly. | P1 |
| TC-4087 | DB transaction rollback on partial send failure | 1. Mock the Resend API to succeed for the first 50 contacts but fail on the 51st (mid-batch failure). | The DB transaction rolls back: `send_count` is not partially incremented. `status` remains `ready`. The error is logged. On retry, the full send is attempted again from 0. | P1 |
| TC-4088 | Error boundary does not catch errors in server components (correct boundary scope) | 1. Seed an edition that causes a server component data fetch error (e.g., DB returns an unexpected null). 2. Navigate to the edition editor page. | The Next.js error page (or `error.tsx` route segment handler) catches server-side errors. The `EditorErrorBoundary` only catches client-side render errors. Both error paths are verified. | P2 |
| TC-4089 | CORS error on Resend webhook endpoint handled with 400 (not 500) | 1. Send a POST to `/api/webhooks/resend` with a `Content-Type` that is not `application/json`. | HTTP 400 is returned with a message: "Invalid content type." No 500 internal server error. Monitoring is not alerted for expected bad requests. | P2 |

---

### Section AB: Onboarding and First-Use Tests (TC-4090 to TC-4114)

| TC# | Test Title | Test Steps | Expected Result | Priority |
|-----|-----------|------------|-----------------|----------|
| TC-4090 | New realtor with no editions sees empty state with "Create Your First Edition" hero CTA | 1. Create a brand-new realtor account with zero editions. 2. Navigate to `/newsletters/editorial`. | A full-page empty state is rendered with a prominent "Create Your First Edition" hero CTA button. No empty table is shown. The CTA is visually distinct (coral brand button, large text). | P0 |
| TC-4091 | Empty state includes 60-second explainer video placeholder | 1. Navigate to `/newsletters/editorial` as a new realtor with no editions. 2. Inspect the empty state component. | The empty state includes a video player element or thumbnail placeholder labeled "See how Editorial Newsletters work (60 sec)". A play button is visible. The video URL or placeholder is wired up (even if the final video asset is pending). | P1 |
| TC-4092 | First edition creation opens voice profile wizard if no voice profile exists | 1. Ensure the test realtor has no row in `editorial_voice_profiles`. 2. Click "Create Your First Edition" (or "New Edition"). | Instead of immediately creating an edition, the voice profile wizard modal opens first. The edition creation is deferred until the wizard is completed or skipped. | P0 |
| TC-4093 | Voice profile wizard step 1 captures tone preference | 1. Open the voice profile wizard (first edition creation flow). 2. Inspect Step 1 of the wizard. | Step 1 displays a "Tone" selection with at least 3 options (e.g., Professional, Warm & Conversational, Bold & Direct). A radio group or card-select UI is used. Selecting an option enables the "Next" button. | P1 |
| TC-4094 | Voice profile wizard step 2 captures writing style preference | 1. Advance to Step 2 of the voice profile wizard. | Step 2 presents writing style options (e.g., "Short & punchy sentences", "Detailed and thorough", "Storytelling narrative"). Selecting a style records it for the voice profile. | P1 |
| TC-4095 | Voice profile wizard step 3 captures signature phrase | 1. Advance to Step 3 of the voice profile wizard. | Step 3 shows a text input labeled "Signature Phrase" with helper text: "A phrase you commonly use to close emails or greet clients (e.g. 'Let's make your move happen')." The field is optional with a Skip option. | P1 |
| TC-4096 | Voice profile wizard step 4 accepts sample email paste | 1. Advance to Step 4 of the voice profile wizard. | Step 4 shows a large textarea labeled "Paste a sample email you've written before" with character count. Helper text: "We'll analyze your writing style to match your voice." The field is optional. Min 50 words recommended (soft validation with advisory message). | P1 |
| TC-4097 | Voice profile wizard step 5 shows review and confirm | 1. Complete Steps 1-4 of the wizard. 2. Advance to Step 5. | Step 5 shows a summary of all selected preferences: Tone, Writing Style, Signature Phrase (if entered), and a snippet of the pasted sample (if provided). "Confirm & Create Edition" and "Edit" buttons are visible. | P1 |
| TC-4098 | Voice profile wizard shows step progress indicator | 1. Open the voice profile wizard. 2. Inspect the wizard header. | A step progress indicator is visible (e.g., "Step 2 of 5" text OR 5 step dots where completed steps are filled). The current step is highlighted. Navigation back to previous steps is possible. | P1 |
| TC-4099 | Voice profile save creates editorial_voice_profiles row in DB | 1. Complete all 5 steps of the voice profile wizard and click "Confirm & Create Edition." | A new row is inserted into `editorial_voice_profiles` with `realtor_id`, `tone`, `writing_style`, `signature_phrase`, `sample_email`, and `created_at`. The row is created before the edition is created. | P0 |
| TC-4100 | Voice profile skip sets profile with default values | 1. Open voice profile wizard on first edition creation. 2. Click the "Skip for now" or "Use defaults" button (present on the wizard). | A row is inserted into `editorial_voice_profiles` with default values (e.g., `tone: "professional"`, `writing_style: "balanced"`, `signature_phrase: null`). The edition creation proceeds immediately. | P1 |
| TC-4101 | After voice profile setup, realtor is redirected to new edition editor with profile applied | 1. Complete the voice profile wizard (all 5 steps, click Confirm). | The realtor is redirected to the new edition editor (`/newsletters/editorial/[new-id]`). The edition has `voice_profile_id` populated. The editor header shows "Voice profile applied." | P0 |
| TC-4102 | First generation starts automatically after voice profile is saved | 1. Complete the voice profile wizard and arrive at the new edition editor. 2. Observe the edition status without clicking anything. | Within 5 seconds of landing on the new edition editor, the BullMQ generation job is enqueued automatically and the edition status transitions to `generating`. No manual "Generate" click is required for the first edition. | P1 |
| TC-4103 | Generation success triggers confetti animation on first edition | 1. Complete voice profile wizard → auto-generation starts → generation succeeds (status → ready). 2. Observe the UI when status transitions to `ready` for the first time. | A confetti animation plays for ~2 seconds when the first edition reaches `ready` status. This is a one-time delight (does not repeat for subsequent editions). A `confetti_shown` flag is set in local storage or user preferences. | P3 |
| TC-4104 | First send triggers onboarding checklist item completion | 1. Successfully send the first editorial edition. 2. Navigate to the onboarding checklist (dashboard or `/onboarding`). | The checklist item "Send your first editorial newsletter" is marked as complete (checkmark, green state). The overall checklist progress bar advances. | P1 |
| TC-4105 | Tooltip on "Generate AI Content" button for first-time editors | 1. Open an edition editor as a realtor with no prior editorial sends (first-time user). 2. Locate the "Generate AI Content" button. 3. Hover over the button. | A tooltip appears: "Click to let AI write your edition based on your voice profile and current market data." The tooltip only appears for first-time users (dismissed after first use via localStorage or user_preferences). | P2 |
| TC-4106 | Tooltip on drag handle explains reorder for first-time users | 1. Open edition editor as a first-time user with multiple blocks. 2. Hover over a block's drag handle. | A tooltip appears: "Drag to reorder blocks." The tooltip is shown for first-time users and dismissed after the first drag interaction. | P2 |
| TC-4107 | Feature flag banner "Editorial Newsletters — Beta Feature" is visible | 1. Log in as a realtor with the `editorial_newsletter` feature enabled. 2. Navigate to `/newsletters/editorial`. | A dismissible banner is visible at the top of the page: "Editorial Newsletters — Beta Feature. Help us improve — share your feedback." The banner includes a "Dismiss" button and stores dismissal in localStorage. | P2 |
| TC-4108 | Feedback CTA appears after first send "How was your first editorial?" | 1. Send the first editorial edition. 2. After the send success state is shown, observe the UI for a feedback prompt. | A feedback CTA modal or banner appears: "How was your first editorial? ⭐ Rate your experience." Options: 1-5 star rating or thumbs up/down. Submitting records the feedback. Dismissing closes without requiring a rating. | P2 |
| TC-4109 | Onboarding checklist item "Send your first editorial newsletter" is present | 1. Complete the 5-step onboarding wizard as a new realtor. 2. Navigate to the onboarding checklist. | The checklist includes the item "Send your first editorial newsletter" (in addition to existing items). It links to `/newsletters/editorial`. It is unchecked by default for new users. | P1 |
| TC-4110 | Empty state for "Sent" tab shows "No editions sent yet — your first one is ready!" | 1. As a new realtor, generate a first edition (status = ready) but do not yet send it. 2. Navigate to the editorial editions list and click the "Sent" tab. | The Sent tab shows an empty state with: "No editions sent yet — your first one is ready!" with a "Send Now" CTA that links to the ready edition. | P2 |
| TC-4111 | Edition list default sort order is newest first | 1. Create 3 editions at different times (edition A oldest, edition C newest). 2. Navigate to `/newsletters/editorial`. | The edition list is sorted by `created_at` descending: edition C appears first, then B, then A. The sort order is "Newest first" by default with no user action required. | P1 |
| TC-4112 | Help sidebar link in editor header links to documentation for first-time users | 1. Open any edition editor for the first time. 2. Locate the "Help" or "?" link in the editor header. | A help link or icon is visible in the editor header (visible on first use). Clicking it opens a help sidebar or links to the Editorial Newsletter user guide documentation. The link is accessible (has `aria-label="Help"`). | P2 |
| TC-4113 | Keyboard shortcut hint for Ctrl+S displayed in editor | 1. Open an edition editor. 2. Inspect the editor UI for keyboard shortcut hints (e.g., in the header near the save indicator, or in a tooltip). | A hint text "Ctrl+S to save" (or "⌘S on Mac") is visible somewhere in the editor UI. Pressing Ctrl+S (Windows) or Cmd+S (Mac) triggers an immediate `updateBlock` save for the currently focused block, bypassing the 500ms debounce. | P2 |
| TC-4114 | Voice profile wizard accessible via Settings after initial setup | 1. Complete the voice profile wizard during first edition creation. 2. Navigate to `/settings` or `/newsletters/editorial/voice-profile`. | A "Voice Profile" settings section is accessible. The realtor can edit their tone, writing style, and signature phrase after initial setup. Changes are saved to `editorial_voice_profiles` and used on the next generation. | P1 |
