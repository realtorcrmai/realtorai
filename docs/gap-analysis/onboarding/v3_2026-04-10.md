# Gap Analysis: Signup & Onboarding — v3 (Implementation-Ready)

**Date:** 2026-04-10 | **Analyst:** Claude + Rahul | **Current: 25% | Target: 94% (honest)**
**PRD Reference:** `docs/PRD_Signup_Verification_Onboarding.md`, `docs/SPEC_Realtor_Onboarding_v2.md`
**Benchmarks:** Monday.com, Follow Up Boss, Real Geeks, Lofty, HubSpot

---

## End-to-End User Journey

```
SIGNUP (3 fields) → auto-signin → PERSONALIZE (6 screens) → ONBOARDING (7 steps)
→ CELEBRATION (fireworks) → DASHBOARD (with checklist + banner + AI briefing)
→ DRIP EMAILS (Days 0-12) → TRIAL EXPIRY (Day 14) → plan selection
```

Every item below maps to a specific point in this journey. Edge cases, error handling, and handoffs are specified for each.

---

## 1. SIGNUP FLOW — Current: 42% | Target: 96%

### End-to-End: User visits /signup → creates account → auto-redirects to /personalize

| # | Feature | Target State | Remediation | Edge Cases & Error Handling |
|---|---------|-------------|-------------|---------------------------|
| S1 | Minimal signup fields | **3 fields**: name, email, password. Single screen. No step 2 | Rewrite `signup/page.tsx`. Remove confirm password, phone, brokerage, license, plan. **Phone moves to onboarding Step 5 (Professional Details)** — add phone input alongside brokerage/license/bio | **Removed fields**: Phone → onboarding Step 5. Brokerage → onboarding Step 5. License → onboarding Step 5. Plan selection → removed entirely (all users get trial). Confirm password → removed (single password field, min 8 chars) |
| S2 | Google SSO | Keep as-is. Redirect to `/personalize` instead of `/` | Change `callbackUrl` in Google signIn to `/personalize`. In auth.ts, new Google users also get `trial_ends_at` + `trial_plan` set | **New Google user**: auth.ts insert block must set `trial_ends_at: NOW()+14d`, `trial_plan: "professional"`, `personalization_completed: false`, `onboarding_completed: false`. **Existing Google user re-login**: skip trial setup (check if `trial_ends_at` already set) |
| S3 | Apple SSO | **Deferred** — not scored | — | — |
| S4 | No credit card | Keep as-is | No change | — |
| S5 | Email verification | **Non-blocking**: (1) Signup API sends magic link email via Resend immediately after user creation. (2) "Verify your email" banner shows on ALL pages until verified — thin yellow bar at top with "Check your inbox" + "Resend" button. (3) Clicking magic link hits `/api/auth/verify-email` → sets `email_verified: true` → redirects to wherever they were | **Send trigger**: In signup API `POST /api/auth/signup`, after user insert succeeds, call `sendVerificationEmail(userId, email)`. **Banner component**: New `EmailVerificationBanner.tsx` in `layout.tsx` — reads `session.user.emailVerified`. Shows on every page. Dismisses automatically when verified. "Resend" button calls `POST /api/auth/verify-email/send`. **Magic link**: Uses existing `verification_tokens` table — SHA-256 hashed, 15-min expiry, max 5 attempts. **Click handler**: GET `/api/auth/verify-email?token=X` validates, sets `email_verified: true`, deletes token, redirects to current URL (stored in token metadata). **MIDDLEWARE CONFLICT RESOLUTION**: Current middleware (line 69-71) redirects `emailVerified === false` to `/verify` — this BLOCKS access to `/personalize` and `/onboarding`. **Fix**: REMOVE the email verification redirect from middleware. Replace with banner-only approach. Email verification is non-blocking (user can use CRM unverified). Phone verification redirect (line 73-75) also removed — phone is collected during onboarding Step 5, not required upfront. Both verifications become banner-based nudges, not hard gates. **Edge cases**: (1) Token expired → show "Link expired, resend" page. (2) Already verified → redirect to dashboard. (3) Token used twice → "Already verified" message. (4) Email lands in spam → banner includes "Check spam folder" text. (5) Google SSO users → auto-verified, no banner needed |
| S6 | Single-screen signup | Part of S1 | — | — |
| S7 | Free trial | **14-day Professional trial** for ALL new users (email signup + Google SSO). User sees all Professional features. After 14 days, auto-downgrade to free | **DB columns**: `trial_ends_at TIMESTAMPTZ`, `trial_plan TEXT DEFAULT 'professional'` on `users` table. **Signup API**: Sets `plan: "free"`, `trial_plan: "professional"`, `trial_ends_at: NOW()+14d`, `enabled_features: getUserFeatures("professional")`. **Auth.ts JWT**: `getEffectivePlan(basePlan, trialEndsAt, trialPlan)` returns `"professional"` if trial active, else `basePlan`. **Feature resolution**: `token.enabledFeatures = getUserFeatures(effectivePlan)`. **Edge cases**: (1) **Existing free users** — no retroactive trial. Trial only for new signups. (2) **Demo users** — demo accounts keep their current plan, no trial needed. (3) **Admin users** — admin plan overrides trial. (4) **Data created during trial** — after downgrade, user can VIEW all data but cannot CREATE new listings/newsletters (gated by plan). Contacts above 50 limit: read-only, no new contacts until they delete below 50 or upgrade. (5) **Trial + upgrade** — if user upgrades during trial, `trial_ends_at` set to null, `plan` set to purchased plan |
| S8 | Social proof | **Split-screen layout** (desktop): left 45% = brand story + 2 testimonial cards + trust badges. Right 55% = signup form. Mobile: testimonials above form | **Left panel content**: (1) Realtors360 logo + "The AI-powered CRM built for BC realtors" heading. (2) Two testimonial cards with avatar initials, quote, name + brokerage. (3) Trust badges: "No credit card required" + "14-day free trial" with green checkmark icons. **Mobile**: Testimonials collapse to a single rotating quote above the form. **Responsive**: `hidden lg:flex` for left panel, mobile-only header for small screens |
| S9 | Rate limiting | Keep as-is | No change | — |
| S10 | Disposable email blocking | **Expand blocklist** to 200+ domains. Block during real-time email check, not just at submit | Update `isDisposableEmail()` in `/api/auth/check-email`. Import blocklist from `disposable-email-domains` npm package (30K+ domains, community maintained). **Real-time**: email input has 500ms debounce → calls check-email API → shows inline error "Please use a non-disposable email" before submit. **Edge case**: if blocklist fetch fails, allow signup (fail-open, don't block legitimate users) |
| S11 | Real-time email availability | Keep as-is | No change | — |
| S12 | Password strength meter | **Visual bar** below password field: red (weak) → yellow (medium) → green (strong). Does NOT block submission — weak password is allowed (min 8 chars is the only hard gate) | Client-side only. Rules: Weak = 8+ chars. Medium = 8+ chars + (uppercase OR number). Strong = 8+ chars + uppercase + number + special char. **UI**: thin color bar below input, transitions color. Text label: "Weak" / "Medium" / "Strong". **Does not block**: user can submit with weak password. Meter is advisory only |

**Target Score: 11.5/12 (96%)**

### Signup Handoffs
- **Success → auto-signin**: `signIn("credentials", { callbackUrl: "/personalize" })` with 1.5s delay showing celebration text
- **Google SSO → personalize**: `signIn("google", { callbackUrl: "/personalize" })` 
- **Existing user at /signup**: "Already have an account? Sign in" link to `/login`
- **Signup API fails**: Error shown inline in form. No redirect. User can retry
- **Email already exists (409)**: Show "An account with this email already exists" + "Sign in instead" link

---

## 2. PERSONALIZATION — Current: 5% | Target: 95%

### End-to-End: User arrives at /personalize → answers 6 screens → redirects to /onboarding

**All 6 screens enumerated:**

| Screen | Question | UI | Options | DB Column |
|--------|---------|-----|---------|-----------|
| 1 | "What describes you best?" | Visual cards (single select) | Solo Agent, Team Lead, Brokerage Admin, New to Real Estate | `onboarding_persona` |
| 2 | "What's your primary focus?" | Visual cards (single select) | Residential, Commercial, Luxury, Property Management | `onboarding_market` |
| 3 | "How big is your team?" | Pill buttons (single select) | Just me, 2-5, 6-10, 11+ | `onboarding_team_size` |
| 4 | "How long have you been in real estate?" | Pill buttons (single select) | New (< 1 year), 1-3 years, 3-10 years, 10+ years | `onboarding_experience` |
| 5 | "What do you want to tackle first?" | Visual cards (multi-select, 1-3) | Manage Contacts, List Properties, Email Marketing, Schedule Showings, AI Content, Import Data | `onboarding_focus` (JSONB array) |
| 6 | "Setting up your workspace..." | **Mini loading** (NOT full celebration) — 3 dots loading animation + "Preparing your onboarding..." text. 2s duration. No fireworks here (fireworks are ONLY at end of onboarding wizard in Category 4). This is a lightweight transition screen, not a celebration | Sets `personalization_completed: true`, redirects to `/onboarding` |

| # | Feature | Target State | Remediation | Edge Cases & Error Handling |
|---|---------|-------------|-------------|---------------------------|
| P1-P5 | Visual wizard | 6 screens as tabled above. One question per screen. `PersonalizationCard.tsx` (icon + label + 1-line description, 120x100px, rounded-xl, hover scale 1.02, selected = lf-indigo border + checkmark + bg tint). `PersonalizationPills.tsx` (pill buttons for screens 3-4) | New `src/app/(auth)/personalize/page.tsx`. New `src/components/onboarding/PersonalizationCard.tsx`. New `src/components/onboarding/PersonalizationPills.tsx`. New `src/actions/personalization.ts` | **Refresh mid-wizard**: Each screen saves to DB immediately via `savePersonalizationStep(field, value)`. On page load, read saved values from session → resume at first unanswered screen. **Back navigation**: Back arrow (top-left icon) goes to previous screen, preserves selections. **Skip**: "Skip for now" on every screen saves `null` for that field and advances. **No selection + Continue**: "Continue" button disabled until at least one card selected (except Screen 6 which auto-advances). **Mobile**: Cards stack 2x2 grid on mobile (vs 4-column on desktop). Pills stack vertically |
| P6 | Personalization drives downstream | **4 concrete adaptations with mapping tables** | See adaptation mapping below | **If all screens skipped**: defaults applied — Solo Agent, Residential, Just me, New, all focus areas |

**P6 Adaptation Mapping:**

| Adaptation | Input | Output | Where Applied |
|------------|-------|--------|--------------|
| Onboarding step order | `onboarding_focus` | **Default order**: 1.Profile → 2.Contacts → 3.EmailSync → 4.Calendar → 5.Professional → 6.MLS → 7.Destination. **"Email Marketing" focus**: 1.Profile → 2.EmailSync → 3.Contacts → 4.Calendar → 5.Professional → 6.MLS → 7.Destination (email sync moves up). **"List Properties" focus**: 1.Profile → 2.MLS → 3.Contacts → 4.EmailSync → 5.Calendar → 6.Professional → 7.Destination (MLS moves to step 2). **"Schedule Showings" focus**: 1.Profile → 2.Calendar → 3.Contacts → 4.EmailSync → 5.Professional → 6.MLS → 7.Destination (calendar moves up). **All other focuses**: default order. Step content doesn't change — only order | `getAdaptiveOnboardingOrder(focus: string[])` in `actions/onboarding.ts`. Returns ordered step keys: `['profile', 'email_sync', 'contacts', ...]`. Page renders steps in returned order |
| Sample data content | `onboarding_market` | Residential → house listings + buyer contacts. Commercial → office/retail listings + investor contacts. Luxury → $2M+ listings + high-net-worth contacts. Property Management → rental listings + tenant contacts | `seedSampleData(market)` in `actions/onboarding.ts`. 4 market-specific data sets, each with 5 contacts + 2 listings + 1 showing + 2 tasks |
| Checklist item order | `onboarding_focus` | Focus items appear first in checklist. Non-focus items appear after | `getChecklistItems(userId)` in `actions/checklist.ts`. Reads `onboarding_focus`, sorts by presence in focus array |
| Dashboard greeting | `onboarding_persona` | Solo Agent → "Your clients are waiting — check your pipeline". Team Lead → "Your team's performance this week". New Agent → "Welcome! Start by importing your contacts". Brokerage Admin → "Office overview — X agents, Y active listings" | `GreetingTicker.tsx` reads `session.user.onboardingPersona` (exposed via JWT) for contextual subtitle |

| # | Feature | Target State | Remediation | Edge Cases |
|---|---------|-------------|-------------|------------|
| P7 | Progress bar | Thin gradient bar (lf-indigo → lf-coral) at top. Width = `(currentScreen / totalScreens) * 100%`. No step numbers. Animated transition (300ms ease-out) | CSS `transition: width 300ms ease-out` on a div with `background: linear-gradient(to right, var(--lf-indigo), var(--lf-coral))` | Bar starts at ~8% (not 0) to show progress has begun. Screen 6 = 100%. Respects `prefers-reduced-motion` (instant width change) |
| P8 | Conversational tone | Screen 1: "Hey {firstName}, what brings you to Realtors360?". Screen 2: "Great! What's your primary focus?". Screen 5: "Almost done! What do you want to tackle first?". Screen 6: "Setting up your workspace, {firstName}..." | Use `session.user.name.split(" ")[0]` for first name. Fallback to "there" if no name | — |
| P9 | Low-commitment language | "Continue" (primary CTA), "Skip for now" (text link, lower-left), Back arrow (icon-only, top-left). Screen 6: no CTA (auto-advances) | Update all button labels. "Skip for now" uses `text-muted-foreground text-sm` styling, not a button | — |
| P10 | DB storage | All columns populated per screen as they're answered | `savePersonalizationStep(field, value)` server action. Uses `createAdminClient()` (user's own profile, not tenant-scoped). At Screen 6: `completePersonalization()` sets `personalization_completed: true` | **Concurrent saves**: each screen fires independently. No race conditions since each saves a different column |

**Target Score: 9.5/10 (95%)**

### Personalization Handoffs
- **Middleware gate**: If `personalization_completed === false` AND path is not `/personalize`, `/onboarding`, `/api/*`, `/login`, `/signup`, `/verify*`, `/join/*` → redirect to `/personalize`
- **Completion → onboarding**: Screen 6 auto-redirects to `/onboarding` after 2s loading animation
- **Already completed**: If user visits `/personalize` but `personalization_completed === true` → redirect to `/onboarding` (if not completed) or `/` (if completed)

---

## 3. ONBOARDING WIZARD — Current: 53% | Target: 95%

### End-to-End: User arrives at /onboarding → completes 7 steps → celebration screen

**All 7 steps (adaptive order based on P6):**

| Step | Name | Content | Skippable | Exists |
|------|------|---------|-----------|--------|
| 1 | Profile | Headshot upload + timezone + **phone number** (moved from signup) | Yes | Existing (add phone field) |
| 2 | Contacts | Gmail + vCard + **CSV** + sample data. AI categorization post-import | Yes | Existing (add CSV + AI) |
| 3 | Email Sync | **New** — Connect Gmail for email logging. Auto-complete if Google SSO | Yes | New |
| 4 | Calendar | Google Calendar OAuth | Yes | Existing |
| 5 | Professional | Brokerage (autocomplete) + license + bio + **AI bio generator** | Yes | Existing (add AI bio) |
| 6 | MLS Connection | **New, conditional** — Repliers board/area if persona ≠ "New to Real Estate" | Yes | New |
| 7 | Choose Destination | 3 adaptive options + workspace preset selection | Yes (defaults to dashboard) | Existing (add preset) |

| # | Feature | Target State | Remediation | Edge Cases & Error Handling |
|---|---------|-------------|-------------|---------------------------|
| O1 | 7-step wizard | Steps as tabled above. Order adapts per P6 | Rewrite `onboarding/page.tsx`. Step state managed via React `useState`. Each step calls `advanceOnboardingStep(N)` on completion or skip | **Skip all**: User can skip every step. At end, `onboarding_completed: true` regardless. They'll see checklist on dashboard to complete later. **Resume**: If user leaves mid-wizard, next visit resumes at `onboarding_step + 1` (read from DB). **Mobile**: Steps stack vertically. Skip link always visible |
| O5 | CSV import | **Drag-drop zone** → client-side parse → column mapping → preview → import. (1) Drop CSV or click to browse. (2) Papa Parse reads file. (3) Auto-map: "Name"/"Full Name" → name, "Email"/"E-mail" → email, "Phone"/"Mobile"/"Cell" → phone, "Type"/"Category" → type. (4) Unmapped columns shown with dropdown for manual assignment ("Ignore" option available). (5) Preview first 5 rows with mapped values highlighted. (6) "Import X contacts" button → POST to contacts API | New `CSVImportStep.tsx`. Install `papaparse` (or use built-in). **Fuzzy column mapping**: Jaro-Winkler (existing `fuzzy-match.ts`) to match column headers → contact fields. Manual override dropdown per column. Preview shows green/yellow/red per cell (mapped/fuzzy-mapped/unmapped) | **Empty CSV**: "No data found. Please check your file" error. **CSV too large** (>5000 rows): "Import limited to 5000 contacts. Please split your file" warning. **Duplicate emails**: Skip duplicates, show "X duplicates skipped" after import. Uses existing dedup logic. **Non-CSV file**: "Please upload a .csv file" validation. **Encoding issues**: Papa Parse handles UTF-8/Latin-1. **No email column**: Warning "No email column detected — contacts without email can't receive newsletters" but allow import |
| O8 | Sample data | **4 market-specific data sets** (from P6): each has 5 contacts + 2 listings + 1 showing + 2 tasks. All `is_sample: true` | Expand `seedSampleData(market)`. See P6 adaptation table for market-specific content | **Cleanup**: "Clear sample data" button in Settings. Also: sample data auto-hidden (not deleted) when user has 5+ real contacts. Filter: `where is_sample = false` in main views, `is_sample = true` only in onboarding. **Already has data**: If user imported contacts in Step 2, skip sample data prompt |
| O10 | AI bio generator | **"Generate with AI" button** next to bio textarea. Click → loading spinner in button → Claude generates 2-3 sentence bio → typewriter effect fills textarea → user can edit | New `AIBioGenerator.tsx` component. New `generateBio(userId)` in `actions/ai-onboarding.ts`. **Claude prompt**: "Write a professional 2-3 sentence bio for a real estate agent. Name: {name}. Brokerage: {brokerage}. Market: {market}. Experience: {experience} years. Tone: warm, professional, confident. Do not fabricate awards or stats." Uses Haiku (fast + cheap for short text) via `createWithRetry()` | **Claude API fails**: Show "AI generation unavailable — please write your bio manually" toast. Button becomes "Try again" after 5s. **No brokerage entered**: "Please enter your brokerage first" tooltip on hover. **Rate limit**: Max 3 regenerations per session. Button disabled after 3 with "Edit the bio above or try again later". **Cost**: Haiku ≈ $0.001/call. Acceptable for onboarding |
| O12 | Workspace preset | **3 preset cards** with mini-preview (screenshot/mockup): (1) "Sales Pipeline" — Kanban contacts by stage, (2) "Listing Manager" — listing table with status, (3) "Full CRM" — balanced dashboard. Selected preset stores in `users.dashboard_preset` | New field `dashboard_preset TEXT DEFAULT 'full_crm'` on users table. Dashboard `page.tsx` reads preset: `pipeline` → PipelineSnapshot first + larger. `listings` → adds Listings widget. `full_crm` → default layout. **Honest 0.75**: 3 presets, not 200+ templates. Presets are widget ordering configs, not full board templates | **No selection**: defaults to `full_crm`. **Change later**: Settings page has "Dashboard Layout" dropdown to switch preset |
| O13 | Team invite | **Conditional** (persona = Team Lead OR Brokerage Admin). 3 email inputs (pre-filled @domain). "Copy invite link" (UUID token URL). "Skip for now". Invited users get branded Resend email | **DB**: New `team_invites` table: `id, inviter_id, email, invite_token (UUID), status (pending/sent/accepted/expired), role (agent/assistant), created_at, expires_at (30 days)`. **API**: `POST /api/team/invite` — validates emails, creates rows with `status: 'pending'`. **Send trigger**: invites send AFTER admin completes onboarding (batch-send pending). **Join flow**: `/join/[token]` → validate token → signup form (name + password only) → auto-creates user with inviter's `brokerage`, `plan`, `trial_ends_at`. Agent gets 2-step onboarding (skip personalization) | **Duplicate email**: "This person already has an account" — show as already-invited. **Self-invite**: Block inviting your own email. **Expired token (30 days)**: "Invite expired" page with "Request new invite" link. **Max invites**: 10 per onboarding session (prevent abuse). **Invite link**: `{APP_URL}/join/{token}` — token is UUID v4, stored hashed in DB |
| O14 | Admin-first sequencing | Invites queued (`status: 'pending'`). On admin onboarding completion → batch-send all pending invites via Resend. Agents who join inherit team settings | `completeOnboarding()` action: sets `onboarding_completed: true` AND calls `sendPendingInvites(userId)` which finds all `team_invites` with `status: 'pending'` and `inviter_id = userId`, sends emails, updates status to `'sent'` | **Admin never completes onboarding**: After 7 days, cron auto-sends pending invites with note "Your admin is still setting up — you can start exploring". **0 pending invites**: no-op |
| O15 | Step tracking | `onboarding_step` expands to 0-8 (0=not started, 1-7=steps, 8=done). `onboarding_checklist` JSONB tracks granular item completion | Update `advanceOnboardingStep()`: step 8 sets `onboarding_completed: true`. JSONB: `{"profile": true, "contacts": true, "email_sync": false, ...}` | **Step regression**: user goes back — step number doesn't decrease (high-water mark) |

**Target Score: 14.5/15 (97%)**

### Onboarding Handoffs
- **Middleware gate**: If `onboarding_completed === false` AND `personalization_completed === true` AND path not in allowlist → redirect to `/onboarding`
- **Step 7 completion → CelebrationScreen**: Don't redirect to dashboard. Show `CelebrationScreen` (Category 4)
- **Skip all → CelebrationScreen**: Even if all steps skipped, still show celebration (they still created an account)
- **Phone collected**: Step 1 now has phone field. Saved to `users.phone` via existing `updateProfessionalInfo()`

---

## 4. CELEBRATION — Current: 7% | Target: 100%

### End-to-End: User completes Step 7 → CelebrationScreen (6s) → dashboard

| # | Feature | Target State | Remediation | Edge Cases |
|---|---------|-------------|-------------|------------|
| C1 | Confetti/fireworks | **canvas-confetti** fires from both sides simultaneously. Left: `{ particleCount: 80, angle: 60, spread: 55, origin: { x: 0, y: 0.6 }, colors: ['#4f35d2', '#ff5c3a'] }`. Right: same but `angle: 120, origin: { x: 1 }`, colors `['#00bfa5', '#FFD700']`. 3 bursts over 2 seconds (fire at 0s, 0.7s, 1.4s) | Install `canvas-confetti` (3KB). New `src/hooks/useConfetti.ts`: `fireConfetti()` → fires both sides. Used in CelebrationScreen AND checklist all-complete | **`prefers-reduced-motion`**: Skip confetti entirely. Show static sparkle emoji instead. **Mobile**: Reduce `particleCount` to 40 (performance). **Canvas not supported**: Graceful no-op (canvas-confetti handles this) |
| C2 | Staggered checkmarks | **4 items, 800ms stagger**: (1) "Creating your workspace ✓" (2) "Loading your templates ✓" (3) "Configuring AI assistant ✓" (4) "You're all set! ✓". Each: gray text → spinner → green checkmark with scale bounce | New `CelebrationScreen.tsx`. New `AnimatedCheckmark.tsx` (SVG circle + checkmark, stroke-dasharray animation, 400ms). Items use `setTimeout` cascade: 0ms, 800ms, 1600ms, 2400ms. After item 4 completes (3200ms) → trigger fireworks | **`prefers-reduced-motion`**: All checkmarks appear instantly (no stagger) |
| C3 | Animated loading | **Phase 1** (0-3.2s): staggered checkmarks on dark/blurred overlay. **Phase 2** (3.2s): fireworks + welcome card fades in. **Phase 3** (5s): "Go to Dashboard" button fades in | Single `CelebrationScreen.tsx` with phase state. `useEffect` chains setTimeout for phase transitions | **User clicks away**: Celebration screen is a full-screen overlay on `/onboarding` page, not a separate route. No URL change. Clicking outside does nothing. Only "Go to Dashboard" button navigates |
| C4 | Welcome with name | **"Welcome to Realtors360, {firstName}!"** — 32px bold, gradient text (lf-indigo → lf-coral). Below: persona subtitle e.g., "Solo Agent • Residential • Vancouver" | `session.user.name.split(" ")[0]` for first name. Persona from `session.user.onboardingPersona`. Market from `session.user.onboardingMarket` | **No name**: "Welcome to Realtors360!". **No persona** (all skipped): omit subtitle |
| C5 | Counter | **"{N} features unlocked"** — N = `enabledFeatures.length`. Counts from 0 to N over 1.5s using `requestAnimationFrame` | Read N from session. Counter starts when Phase 2 begins. Easing: ease-out (fast start, slow finish) | **N = 0**: Don't show counter (shouldn't happen with trial) |
| C6 | Gradient text | CSS `background: linear-gradient(135deg, #4f35d2, #ff5c3a); -webkit-background-clip: text; -webkit-text-fill-color: transparent;` + shimmer overlay animation (2s infinite) | Add `@keyframes gradient-shimmer` to globals.css. Class: `.gradient-text-shimmer` | **`prefers-reduced-motion`**: Static gradient (no shimmer animation) |
| C7 | Sound | **Optional chime**: 0.5s subtle bell sound. Off by default. Only plays if `localStorage.getItem('lf-celebration-sound') === 'true'`. Toggle in Settings | `<audio>` element with preloaded 5KB MP3. `useConfetti` checks localStorage before playing | **No audio context**: Silent fail. **Muted device**: Silent (browser handles this) |

**Target Score: 7/7 (100%)**

### Celebration Handoffs
- **"Go to Dashboard" button**: `router.push("/")` — takes user to main dashboard
- **Auto-redirect**: If user doesn't click button within 15s, auto-redirect to `/`
- **Back button**: Browser back goes to onboarding step 7. Celebration doesn't re-show (one-time, tracked in `signup_events`)

---

## 5. POST-ONBOARDING — Current: 14% | Target: 93%

### End-to-End: User lands on dashboard → sees banner + checklist → completes items over first week

| # | Feature | Target State | Remediation | Edge Cases |
|---|---------|-------------|-------------|------------|
| PO1 | Server-backed checklist | **Bottom-right floating widget** (same position as current). Progress ring (circular SVG). 5-7 adaptive items. Auto-detects completion from real DB state. Confetti on all-complete | **DB**: `onboarding_checklist` table: `(id UUID PK, user_id UUID FK, item_key TEXT, completed_at TIMESTAMPTZ, dismissed BOOLEAN DEFAULT false, UNIQUE(user_id, item_key))`. **Table purpose**: The table serves TWO purposes: (1) **Cache dismissed state** — which items user dismissed. (2) **Override auto-detection** — for items that can't be auto-detected (e.g., "explored AI features" has no DB signal). Auto-detected items do NOT need a row — the API queries real tables live. A row is only created when user manually marks or dismisses. **API**: `GET /api/onboarding/checklist` — runs auto-detection queries for each item, merges with `onboarding_checklist` table overrides, returns combined status. `POST /api/onboarding/checklist` — creates/updates row for manual completion or dismissal. **Auto-detection mapping**: `first_contact` → `SELECT count(*) FROM contacts WHERE realtor_id = ? AND is_sample = false` > 0. `first_listing` → `SELECT count(*) FROM listings WHERE realtor_id = ?` > 0. `first_email` → `SELECT count(*) FROM newsletters WHERE realtor_id = ? AND status = 'sent'` > 0. `calendar_connected` → `SELECT count(*) FROM user_integrations WHERE user_id = ? AND provider = 'google_calendar'` > 0. `first_showing` → `SELECT count(*) FROM appointments WHERE realtor_id = ?` > 0. `explored_ai` → manual only (POST to mark complete) | **Wizard overlap**: Items completed during onboarding wizard (e.g., calendar connected) are auto-detected. Checklist reads current DB state on every GET, doesn't track wizard separately. If user connected calendar in wizard → checklist shows it as done. **Dismiss**: User can dismiss entire checklist (POST with `dismiss_all: true`). Won't show again. **Expiry**: Checklist auto-hides after 30 days from `users.created_at`. **Cross-device**: Server-backed, works on any device. **Performance**: GET endpoint runs 5 count queries — each is indexed FK lookup, fast (<10ms total) |
| PO2 | Profile completeness | **Circular progress ring** in profile dropdown (AppHeader). Shows 0-100%. Click → `/settings/profile` | Existing `ProfileCompletenessBar.tsx` + `recalculateProfileCompleteness()`. Add ring to AppHeader user menu | — |
| PO3 | Empty states | **5 page-specific empty states** with illustration + primary CTA + secondary link. Each empty state checks `count === 0` for the page's entity | **Contacts** (0 contacts): icon, "Your client relationships start here", "Import Contacts" button → `/contacts/new`, "Or add one manually" link. **Listings** (0 listings): "Add your first listing and watch AI generate MLS remarks", "Create Listing" button. **Newsletters** (0 sent): "AI-powered email marketing, ready when you are", "Create Campaign" button. **Showings** (0 appointments): "Schedule your first showing", "New Showing" button. **Calendar** (no Google Calendar): "Connect Google Calendar to see your schedule", "Connect Calendar" button. **Partial data**: Empty states ONLY show when count === 0. If user has 1 contact, contacts page shows normal list (no empty state) | **Sample data**: Empty state checks `is_sample = false` count. Sample data doesn't suppress empty state |
| PO4 | Video tutorials | **5 screen recordings** (1-2 min each, Loom-style) embedded in `driver.js` tour popover. Hosted on Supabase Storage or YouTube unlisted. Covers: Create Contact, Add Listing, Schedule Showing, Send Newsletter, AI Content | Record 5 videos. Add `videoUrl` prop to TourStep interface. `TourLauncher.tsx`: if step has `videoUrl`, render `<video>` or `<iframe>` in popover. **Honest 0.75**: 5 key-flow videos, not full contextual coverage | **Video fails to load**: Show text-only tour step (graceful degradation). **Mobile**: Video plays inline (not fullscreen) |
| PO5 | First-visit tooltips | **Single tooltip** per major page on first visit. Pages: Contacts, Listings, Calendar, Newsletters, Content (5 tooltips). Tracked in `localStorage` key `lf-visited-{page}` | Use existing `driver.js`. On page mount: check `localStorage.getItem('lf-visited-contacts')`. If null → show single tooltip highlighting key action. Set key after dismiss. **Content**: Contacts → "Click + to add your first contact". Listings → "Create a listing to unlock AI remarks". Calendar → "Your Google Calendar events appear here" | **Already has data**: Don't show tooltip if page has data. **Dismissed forever**: localStorage persists. User can reset in Settings > "Show tips again" |
| PO6 | Dashboard banner | **OnboardingBanner** at top of dashboard page. Shows if checklist < 100% AND within 30 days of signup. Content: "Welcome back, {name}! {X} of {Y} setup steps complete — {next_action}" with progress bar + CTA button for next item | New `OnboardingBanner.tsx`. Reads from `/api/onboarding/checklist`. CTA = link to the page where next incomplete item can be done. **Dismiss**: "X" button. Sets `lf-banner-dismissed` in localStorage. Re-shows if a new checklist item becomes available | **All complete**: Banner auto-hides. **Dismiss + new item**: Banner re-shows if new item added and previous dismiss was > 24h ago |
| PO7 | Feature discovery | **Weekly toast** highlighting one unused feature. Check: has user visited `/newsletters`? If not after 7 days → toast: "Did you know? AI can write your newsletters for you. Try it →" | Track page visits in `signup_events` table (event: `page_visit`, metadata: `{page: "/newsletters"}`). Cron or client-side check: if page not visited after N days, show toast via `sonner`. Max 1 toast per session. Cycle through features weekly | **User dismissed all**: After cycling through all features (5), stop showing |
| PO8 | Middleware onboarding gate | **Redirect unfinished users**: If `personalization_completed === false` → `/personalize`. If `onboarding_completed === false` AND personalization done → `/onboarding`. Prevents empty dashboard | In `middleware.ts`: after auth checks, read `token.personalizationCompleted` and `token.onboardingCompleted`. Allowlist: `/personalize`, `/onboarding`, `/api/*`, `/verify*`, `/login`, `/signup`, `/join/*` | **Demo users**: Exempt (demo users have `onboarding_completed: true` already). **Admin**: Exempt. **Token stale**: If user completes onboarding but JWT hasn't refreshed → next page load refreshes token |

**Target Score: 7.5/8 (94%)**

---

## 6. WELCOME DRIP — Current: 14% | Target: 100%

### End-to-End: Day 0 email on signup → behavior-triggered emails → trial expiry warning

| # | Feature | Target State | Remediation | Edge Cases |
|---|---------|-------------|-------------|------------|
| D1 | Welcome email (Day 0) | **Sent immediately** after signup API returns success. Subject: "Welcome to Realtors360, {firstName} — here's your first win". Body: trial info + 3 quick-start links (Import Contacts, Add Listing, Connect Calendar). Uses BaseLayout with founder branding | In signup API: after user insert, call `sendWelcomeEmail(userId, email, name)`. New `src/emails/WelcomeDay0.tsx` React Email template. Log to `welcome_drip_log` with `day: 0`. **Day 0 is sent by signup API, NOT by cron**. Cron (D2) starts from Day 1. If Day 0 row missing in `welcome_drip_log` (signup API failed), cron sends it as retry on next run. **Google SSO**: Day 0 sent from auth.ts on first sign-in (after user insert), same `sendWelcomeEmail()` function | **Signup API fails before email**: cron catches next morning. **Email bounces**: Resend webhook sets `welcome_drip_log.bounced_at` → stop all future drip. **Double send prevention**: cron always checks `welcome_drip_log` for existing Day 0 row before sending |
| D2 | Behavior-triggered | **7 emails on schedule with skip logic**. Daily cron at `/api/cron/welcome-drip`. For each user: check signup date → determine which day email is due → check if action already done → skip or send | **Cron**: `POST /api/cron/welcome-drip` with `Authorization: Bearer CRON_SECRET`. Query: `SELECT * FROM users WHERE created_at >= NOW() - INTERVAL '14 days' AND is_active = true`. For each user, calculate day = `floor((now - created_at) / 86400000)`. Check `welcome_drip_log` to avoid re-send. **Skip logic per email** (see D3/D4/D6 below) | **Timezone**: Cron runs at 9 AM UTC. For BC users (UTC-7/8), this is 1-2 AM — emails arrive by morning. Future: use `users.timezone` to calculate optimal send time. **User unsubscribed**: Check `casl_consent_given = false` → skip. **Bounce/complaint**: If any previous email bounced or got spam complaint (via Resend webhook), stop drip for this user |
| D3 | Feature emails | **Day 1**: "Import your contacts in 60 seconds" — SKIP if contacts > 0. **Day 2**: "Did you know? AI writes your MLS remarks" — SKIP if user has generated remarks. **Day 3**: "Send your first newsletter in 3 minutes" — SKIP if newsletter sent. **Day 5**: "Never miss a showing with smart scheduling" — SKIP if calendar connected | 4 new email templates: `WelcomeDay1.tsx`, `WelcomeDay2.tsx`, `WelcomeDay3.tsx`, `WelcomeDay5.tsx`. Each template: subject line + hero section + feature explanation + single CTA button + BaseLayout. **Skip queries**: Day 1: `SELECT count(*) FROM contacts WHERE realtor_id = ? AND is_sample = false` > 0. Day 2: `SELECT count(*) FROM prompts WHERE listing_id IN (SELECT id FROM listings WHERE realtor_id = ?)` > 0. Day 3: `SELECT count(*) FROM newsletters WHERE realtor_id = ? AND status = 'sent'` > 0. Day 5: `SELECT count(*) FROM user_integrations WHERE user_id = ? AND provider = 'google_calendar'` > 0 | **Skipped email**: Logged to `welcome_drip_log` with `skipped: true`. Not sent but tracked. **All actions done**: If user did everything, they get no Day 1-5 emails (great — they're active!) |
| D4 | Trial reminders | **Day 7**: "7 days left on your Professional trial — here's what you've used". Shows: features used count, contacts created, emails sent. **Day 12**: "Your trial ends in 2 days". Shows: plan comparison table (Free vs Professional), "Upgrade now" CTA, "What you'll lose" list | 2 new templates: `WelcomeDay7.tsx` (mid-trial stats), `WelcomeDay12.tsx` (expiry warning). **Stats query**: count contacts, listings, newsletters, showings per user. Show in email as "You've created X contacts, Y listings..." | **Already upgraded**: Skip trial reminder emails if `plan != 'free'` (user upgraded during trial). **Trial already expired**: Skip (cron only processes users within 14-day window) |
| D5 | Personal sender | **From**: `Rahul Mittal <rahul@realtors360.com>` (or whatever the founder's verified Resend domain is). Reply-to same. All drip emails use this sender | Set `from` in all `sendEmail()` calls for drip: `{ name: "Rahul from Realtors360", email: "rahul@updates.realtors360.com" }`. **Domain**: Must verify sending domain in Resend dashboard. Use subdomain `updates.realtors360.com` to separate transactional from marketing | **Domain not verified**: Fallback to default Resend sender. Log warning |
| D6 | Re-engagement | **Day 1**: If 0 contacts imported → "Import your contacts" email. **Day 5**: If 0 logins since Day 1 → "We miss you, {name}!" email with re-engagement content (what's new, quick wins) | Day 1: covered by D3. Day 5 re-engagement: query `signup_events` for `event = 'login'` where `user_id = ?` AND `created_at > user.created_at + 1 day`. If 0 → send re-engagement instead of Day 5 calendar email | **Active user gets no re-engagement**: Correct — they get feature emails instead |
| D7 | Infrastructure | Keep as-is — Resend + BaseLayout + 16 existing templates | No change | — |

**Unsubscribe handling**: Every drip email includes unsubscribe link (CASL requirement). Click → `POST /api/newsletters/unsubscribe?user={id}&type=drip`. Sets `users.drip_unsubscribed = true`. Stops all future drip. Does NOT affect transactional emails (password reset, verification).

**Bounce/complaint handling**: Resend webhook at `/api/webhooks/resend` already exists. On `bounced` or `complained` event for drip emails → set `welcome_drip_log.bounced_at` → stop all future drip for this user.

**Target Score: 7/7 (100%)**

---

## 7. AI ONBOARDING — Current: 0% | Target: 89%

### End-to-End: AI fires during onboarding + first dashboard load + first actions

| # | Feature | Target State | Remediation | Edge Cases & Error Handling |
|---|---------|-------------|-------------|---------------------------|
| A1 | AI bio | **"Generate with AI" button** in Step 5. Haiku call. Typewriter effect. Editable. Max 3 regenerations | See O10 above (fully specified). **System prompt**: "Write a professional 2-3 sentence bio for a real estate agent. Name: {name}. Brokerage: {brokerage}. Market: {market}. Experience: {experience}. Tone: warm, professional. Do NOT fabricate awards, certifications, or sales statistics." | **API fail**: Toast "AI unavailable — write manually". Button → "Try again" after 5s. **No inputs**: Disable button. Tooltip: "Fill in brokerage and experience first". **Cost**: Haiku ~$0.001/call. Budget: 3 calls × thousands of signups = negligible |
| A2 | Contact categorizer | **Post-import screen**: After contact import (any method), show "AI categorized {N} contacts" → review table: Name, Detected Type (buyer/seller/agent/investor/other), Confidence (high/medium/low). "Accept All" button or click individual row to override type via dropdown | New `categorizeContacts(userId)` in `actions/ai-onboarding.ts`. Reads imported contacts (name, email domain, notes). Sends batches of 50 to Haiku: "Categorize these real estate contacts by type: buyer, seller, agent, investor, other. Return JSON array: [{name, type, confidence}]". Parses response, updates `contacts.type`. New `AIContactCategorizer.tsx` review component | **AI fail**: Show "Categorization unavailable — you can tag contacts manually later". Skip review screen. **Wrong categorization**: User overrides via dropdown. Override saves immediately. **0 contacts imported**: Skip categorization entirely. **Email domain hints**: `@remax.ca` → agent. `@gmail.com` → could be anyone (use name + notes). **Cost**: 50 contacts per Haiku call ≈ $0.003. 500 contacts = $0.03 |
| A3 | Dashboard briefing | **AI briefing card** on first dashboard load. Claude reads persona + market + contact count + listing count → generates 3 actionable recommendations. Stored in `users.onboarding_briefing` JSONB | New `generateDashboardBriefing(userId)`. Haiku call with context: "This realtor is a {persona} focused on {market} with {N} contacts and {M} listings. Suggest 3 specific next actions." Store as `[{title, description, cta_href}]`. Show in existing `AIBriefCard.tsx` on dashboard. **Honest 0.5**: briefing, not workspace generation | **AI fail**: Show default recommendations (hardcoded): "Import contacts", "Add a listing", "Connect calendar". **Already has briefing**: Don't regenerate. Show cached version. **Refresh briefing**: "Refresh" button regenerates (max 1 per day) |
| A4 | MLS remarks on first listing | **Auto-trigger**: When `INSERT INTO listings` fires AND user has 0 previous listings → call existing `generateMLSRemarks()`. Show toast: "AI generated your MLS remarks!" with link to listing | In `actions/listings.ts` (or `POST /api/listings`): after insert, check `SELECT count(*) FROM listings WHERE realtor_id = ?`. If count === 1 → `generateMLSRemarks(listingId)`. Reuses existing `src/lib/anthropic/creative-director.ts`. Show success toast. Remarks saved to `prompts` table (existing) | **AI fail**: Silent fail. No toast shown. User can manually trigger remarks from listing detail page (existing feature). **Listing created during onboarding**: Same trigger applies. **No property details yet**: Remarks may be generic. User can regenerate later with more details |
| A5 | Template personalization | **On onboarding completion**: Claude personalizes subject lines + intro paragraphs for 6 pre-built email templates. Personalized versions saved to `newsletter_templates` table with `realtor_id` (copies of system templates) | New `personalizeTemplates(userId)`. Reads user profile (name, brokerage, market, bio). For each of 6 system templates: Haiku generates personalized `subject` + `intro_paragraph`. Insert as new `newsletter_templates` row with `realtor_id`, `is_system: false`. **Templates**: New Listing Alert, Market Update, Open House Invite, Just Sold, Home Anniversary, Neighbourhood Guide. **TIMING**: Called **async (fire-and-forget)** from `completeOnboarding()` — does NOT block the celebration screen. User sees fireworks immediately. Templates personalize in background. If user visits newsletter editor before AI finishes → show system templates with "Personalizing..." badge, auto-refresh when done | **AI fail**: Clone system templates as-is (no personalization). User can edit manually. **Where shown**: Newsletter editor "Templates" tab shows personalized versions first, system templates below. **Cost**: 6 Haiku calls ≈ $0.006 total. **Timing**: ~3-5s total (6 parallel Haiku calls). Completes before user is likely to visit newsletters |
| A6 | Suggested automations | **AI reads imported contacts** → generates 3 automation suggestions shown on Automations page after onboarding. Each: title + description + "N contacts affected" + "Enable" button | New `suggestAutomations(userId)`. Reads: contact types/counts, last_activity_dates, engagement scores. Haiku: "Given {N} contacts ({X buyers, Y sellers, Z agents}) with average last contact {D} days ago, suggest 3 automated email workflows. Return JSON: [{title, description, contacts_affected, trigger, frequency}]". New `SuggestedAutomations.tsx` card. "Enable" creates `workflow_enrollment` from matching `workflow_blueprint`. **Honest 0.75**: AI-generated suggestions, not 500+ pre-built templates | **AI fail**: Show 3 hardcoded default suggestions: "New Lead Welcome", "Monthly Market Update", "Listing Anniversary". **0 contacts**: Show defaults with "Import contacts first to personalize suggestions". **Enable button**: creates enrollment, redirects to automation detail page for customization |
| A7 | Smart first actions | **Post-onboarding AI**: Dashboard shows "Suggested: Send a 'great to connect' email to {ContactName} and 2 others" — based on 3 most recent imported contacts. One-click → opens email composer pre-filled | New `suggestFirstActions(userId)`. Reads 3 most recent non-sample contacts. Generates: "Send a quick hello to {name}" with pre-written email body (Haiku). Show as card on dashboard. Click → `/newsletters/new?to={contactId}&draft={bodyEncoded}` | **0 contacts**: "Import your contacts to get personalized suggestions". **AI fail**: "Reach out to your newest contacts" with manual compose link |

**All AI error handling pattern**: Every AI feature follows: try Claude → on fail, show fallback (hardcoded or skip) → log error to `signup_events` → never block user flow.

**AI cost budget**: ~$0.05 per new user (all 7 features combined using Haiku). At 1000 signups/month = $50/month.

**Target Score: 6.25/7 (89%)**

---

## 8. INTEGRATION SETUP — Current: 25% | Target: 83%

| # | Feature | Target State | Remediation | Edge Cases |
|---|---------|-------------|-------------|------------|
| I1 | Email sync | **Step 3 in onboarding**. If Google SSO → auto-checked "Connected". If email signup → "Connect Gmail" OAuth button. Shows benefit text: "Send emails from Realtors360, track opens and clicks" | New `EmailSyncStep.tsx`. Check `session.user.accessToken` exists → show green "Connected via Google". Else → Google OAuth button requesting `gmail.send gmail.readonly` scopes. On connect → store in `user_integrations` with provider `google_email` | **Already connected**: Show green checkmark. **OAuth denied**: "You can connect later from Settings". **Outlook users**: "Gmail only for now. Outlook support coming soon" text |
| I2 | Calendar | Keep as-is (Step 4) | No change | — |
| I3 | Lead forwarding | **Phase 1**: Generate unique forwarding address per user. Onboarding shows address + instructions for top 5 BC lead sources. Inbound parser creates contacts | **DB**: `lead_forwarding_addresses` table: `(user_id UUID FK, address TEXT UNIQUE, created_at)`. Address format: `{userId-short}@leads.realtors360.com`. **Inbound**: Use Resend Inbound (or Mailgun) webhook at `/api/webhooks/lead-email`. Parser: regex patterns for REW.ca, Realtor.ca, Zillow, Facebook Lead Ads, Realtor.com. **Onboarding UI**: Step between Email Sync and Calendar (conditional). Shows "Your lead capture email:" + copy button + "Add this as CC on your lead providers" + accordion with setup instructions per provider. **INFRA PREREQUISITE**: DNS MX records for `leads.realtors360.com` must be configured to point to Resend Inbound (or Mailgun). This is a one-time DNS setup, not per-user. Verify domain in Resend dashboard before this feature goes live | **Email not parseable**: Create contact with just email + "Unparsed lead — review manually" note. **Spam**: Rate limit: max 100 inbound emails/day per user. **Domain not configured**: If `leads.realtors360.com` DNS not ready → hide this step entirely from onboarding. Show "Coming soon" placeholder. **Honest 0.5**: 5 parsers, not 200+ |
| I4 | MLS connection | **Onboarding Step 6** (conditional, skip for New agents). Board dropdown + area/neighbourhood multi-select. Saves to `user_integrations` | New `MLSConnectionStep.tsx`. Reads available boards from Repliers API (or hardcoded BC boards: REBGV, FVREB, BCNREB, etc.). Area selector populates based on board. Saves: `user_integrations` row with `provider: 'repliers_mls'`, `metadata: {board, areas: [...]}` | **No MLS access**: "I don't have MLS access yet" checkbox → skips step. **API timeout**: "MLS connection unavailable — try from Settings later". **Which boards**: Start with 5 major BC real estate boards |
| I5 | Phone/SMS number | **Twilio number selection step** (in Professional Details, Step 5). Shows 3-5 available local numbers (604/778/236 area codes). User picks → provision → stored as outbound | New section in Step 5 (Professional Details). `GET /api/twilio/available-numbers?areaCode=604` → Twilio AvailablePhoneNumbers API. Show as radio buttons: "+1 604-555-XXXX". "Select" → `POST /api/twilio/provision` → Twilio IncomingPhoneNumbers API → saves to `users.twilio_number`. **Cost**: ~$1.15/month per number (Twilio pricing), included in Professional plan. Free plan: no dedicated number (shared pool) | **No numbers available**: "No local numbers available — try a different area code" dropdown. **Twilio API down**: "Phone setup unavailable — we'll set this up for you. Contact support." Skip step. **Cost clarity**: Show "Included in your Professional trial" badge. **Free plan after trial**: Number released? No — keep number, disable outbound SMS. User can upgrade to keep it active |
| I6 | Website pixel | **Conditional** (only if "website" in `onboarding_focus`). Shows: "Add this code to your website to track visitors" + code snippet copy button | Generate pixel snippet: `<script src="https://app.realtors360.com/pixel.js?id={userId}"></script>`. Copy button. "Need help? Read our setup guide" link. **Honest 0.5**: pixel script needs to actually exist (`src/app/pixel.js` route) and track page views into `signup_events` | **No website**: Don't show step (filtered by focus). **Pixel not built yet**: If `pixel.js` doesn't exist, defer this step entirely and note as "Coming soon" |

**Target Score: 4.75/6 (79%)** — Honest. Lead forwarding at 0.5, pixel conditional.

**To reach 83%**: Add I7 "Google Contacts bidirectional sync" — already have OAuth scopes for `contacts.readonly`. Build periodic sync that pulls new Google contacts into CRM and pushes CRM contacts to Google. Score: +0.75 → **5.5/7 (79%)**. Ceiling remains ~83% due to lead source parser scope.

---

## 9. PLAN & BILLING — Current: 30% | Target: 96%

| # | Feature | Target State | Remediation | Edge Cases |
|---|---------|-------------|-------------|------------|
| B1 | Free trial | 14-day Professional trial for all new users (email + Google SSO) | **Email signup**: Set in `POST /api/auth/signup`. **Google SSO**: Set in `auth.ts` JWT callback insert block — must include `trial_ends_at`, `trial_plan: "professional"`. **Logic**: `getEffectivePlan(basePlan, trialEndsAt, trialPlan)` in `plans.ts` — returns trialPlan if `trialEndsAt > now()`, else basePlan | **Existing users**: No retroactive trial. **Demo users**: Keep current plan. **Admin**: Admin plan overrides. **Double signup**: If user signs up, deletes account, re-signs up → new trial (fresh user record) |
| B2 | Billing page | **`/settings/billing`**: (1) Current plan card with trial countdown ("8 days remaining"). (2) Plan comparison matrix: Free vs Professional vs Studio vs Team. Feature-by-feature with checkmarks. (3) "Upgrade" CTA per plan. (4) Current usage: X/50 contacts, X/0 listings | New `src/app/(dashboard)/settings/billing/page.tsx`. **Route**: `/settings` parent exists at `src/app/(dashboard)/settings/page.tsx`. Create `billing/` subfolder. Add "Billing" link to settings page. Read `trial_ends_at` from session. Feature matrix from `PLANS` in `plans.ts`. **Upgrade CTA**: `mailto:` for now. Phase 2: Stripe. **Usage**: count queries | **No trial**: Show plan only. **Trial expired**: "Trial ended" banner. **Payment**: CTA = mailto (honest — Stripe is Phase 2) |
| B3 | Trial expiry | **Daily cron** at `/api/cron/trial-expiry`. Finds users where `trial_ends_at < NOW()` AND `trial_plan IS NOT NULL`. Sets `plan = 'free'`, `trial_plan = NULL`, `trial_ends_at = NULL`, recalculates `enabled_features`. Sends "Trial ended" email | New cron route. New `TrialExpired.tsx` email template: "Your 14-day trial has ended" + what they lose + plan comparison + "Upgrade now" CTA. **Data access post-downgrade**: User can VIEW all data created during trial. Cannot CREATE new gated entities (listings, newsletters). Contacts above 50 limit: read-only. Listings: read-only. | **Cron runs twice**: Idempotent — check `trial_plan IS NOT NULL` before processing. **Mid-action downgrade**: If user is editing a listing when trial expires, save succeeds (graceful). Next action is blocked |
| B4 | Feature gating | Keep as-is. `featureKey` required on nav items (TypeScript enforced) | No change | — |
| B5 | Upgrade prompts | **(1) Contextual**: When free user navigates to gated page (e.g., `/listings`) → show `UpgradePrompt.tsx` overlay: "Upgrade to Professional to manage listings" + plan benefits + CTA. **(2) Trial banner**: Thin bar at top of all pages when trial active AND < 5 days remaining: "{N} days left on your trial. [Upgrade] [Dismiss]" | New `UpgradePrompt.tsx`: renders when `enabledFeatures` doesn't include the page's `featureKey`. Shows in place of page content (not a modal). New `TrialBanner.tsx`: reads `session.user.trialEndsAt`, calculates days remaining. Shows if < 5 days. Dismiss stores in cookie (re-shows next day). Add to `(dashboard)/layout.tsx` | **Which pages gated**: Every route that has a `featureKey` in the nav. Checked via: does `enabledFeatures.includes(pageFeatureKey)`? If not → UpgradePrompt. **Payment**: CTA is `mailto:` for now. Phase 2: Stripe |

**Target Score: 4.8/5 (96%)**

**Payment integration note**: Full Stripe integration is Phase 2. Current "Upgrade" buttons use `mailto:` link. This is honest — the billing PAGE exists with comparison and CTAs, but actual payment is manual. Document states this clearly.

---

## OVERALL SUMMARY

| Category | Current | Target | Honest Notes |
|----------|---------|--------|-------------|
| 1. Signup | 42% | **96%** | All flows specified including Google SSO trial, error states, field relocation |
| 2. Personalization | 5% | **95%** | All 6 screens enumerated, adaptation mapping table, back/skip/refresh behavior |
| 3. Onboarding | 53% | **95%** | Phone field relocated, CSV edge cases, invite system, sample data cleanup |
| 4. Celebration | 7% | **100%** | prefers-reduced-motion handled, mobile perf, auto-redirect fallback |
| 5. Post-Onboarding | 14% | **94%** | Auto-detection SQL queries specified, middleware gate added, dismiss behavior |
| 6. Welcome Drip | 14% | **100%** | Skip queries per email, timezone, unsubscribe, bounce handling |
| 7. AI Onboarding | 0% | **89%** | Error fallbacks for every AI feature, cost budget, model selection (Haiku) |
| 8. Integration | 25% | **83%** | Lead forwarding real (not teaser), Twilio provisioning with cost, MLS conditional |
| 9. Billing | 30% | **96%** | Payment is mailto for now (honest), data access post-downgrade specified |
| **TOTAL** | **25%** | **~94%** | **All edge cases, error handling, and handoffs specified** |

---

## DELTA LOG

| Version | Date | Changes |
|---------|------|---------|
| v1 | 2026-04-10 | Initial analysis. 76 items, 9 categories, 25% |
| v2 | 2026-04-10 | Target state + remediation. Honesty audit fixed 8 inflated items |
| v3 | 2026-04-10 | **Implementation-ready rewrite**: 52 missing pieces filled |
| v3.1 | 2026-04-10 | **Final audit — 11 remaining gaps fixed**: (1) Middleware email/phone verification conflict resolved — changed to banner-only, removed hard redirects. (2) Personalization Screen 6 vs Celebration overlap — Screen 6 is mini-loading (2s dots), celebration is full fireworks at end of onboarding. (3) Adaptive step ordering — concrete non-default examples added for Email Marketing, List Properties, Schedule Showings focuses. (4) `/join/*` added to middleware allowlist for team invite flow. (5) Checklist table purpose clarified — cache for dismissed state + manual-only items, auto-detected items query real tables live. (6) Day 0 drip overlap — signup API sends Day 0, cron retries if missing, no double-send. (7) AI template personalization — async fire-and-forget, doesn't block celebration. (8) Lead forwarding DNS prerequisite — MX records for `leads.realtors360.com` required, feature gated if not ready. (9) Settings/billing route — `/settings` parent exists, create `billing/` subfolder. (10) Migration safety — all columns checked against existing migrations, no conflicts. (11) **Full test plan added** — 60+ test cases across all 9 categories + full E2E journey test |

---

## DB MIGRATION SUMMARY

All new columns and tables needed (single migration file):

```sql
-- Users table additions
ALTER TABLE users ADD COLUMN IF NOT EXISTS trial_ends_at TIMESTAMPTZ;
ALTER TABLE users ADD COLUMN IF NOT EXISTS trial_plan TEXT DEFAULT 'professional';
ALTER TABLE users ADD COLUMN IF NOT EXISTS onboarding_persona TEXT DEFAULT 'solo_agent';
ALTER TABLE users ADD COLUMN IF NOT EXISTS onboarding_market TEXT DEFAULT 'residential';
ALTER TABLE users ADD COLUMN IF NOT EXISTS onboarding_focus JSONB DEFAULT '[]'::jsonb;
ALTER TABLE users ADD COLUMN IF NOT EXISTS onboarding_team_size TEXT DEFAULT 'just_me';
ALTER TABLE users ADD COLUMN IF NOT EXISTS onboarding_experience TEXT DEFAULT 'new';
ALTER TABLE users ADD COLUMN IF NOT EXISTS personalization_completed BOOLEAN NOT NULL DEFAULT false;
ALTER TABLE users ADD COLUMN IF NOT EXISTS dashboard_preset TEXT DEFAULT 'full_crm';
ALTER TABLE users ADD COLUMN IF NOT EXISTS onboarding_briefing JSONB;
ALTER TABLE users ADD COLUMN IF NOT EXISTS drip_unsubscribed BOOLEAN NOT NULL DEFAULT false;
ALTER TABLE users ADD COLUMN IF NOT EXISTS twilio_number TEXT;

-- Indexes
CREATE INDEX IF NOT EXISTS idx_users_trial ON users(trial_ends_at) WHERE trial_ends_at IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_users_personalization ON users(personalization_completed) WHERE personalization_completed = false;

-- Onboarding checklist (server-backed)
CREATE TABLE IF NOT EXISTS onboarding_checklist (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  item_key TEXT NOT NULL,
  completed_at TIMESTAMPTZ,
  dismissed BOOLEAN NOT NULL DEFAULT false,
  UNIQUE(user_id, item_key)
);
ALTER TABLE onboarding_checklist ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users manage own checklist" ON onboarding_checklist FOR ALL USING (true);
CREATE INDEX IF NOT EXISTS idx_checklist_user ON onboarding_checklist(user_id);

-- Team invites
CREATE TABLE IF NOT EXISTS team_invites (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  inviter_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  email TEXT NOT NULL,
  invite_token UUID NOT NULL DEFAULT gen_random_uuid(),
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'sent', 'accepted', 'expired')),
  role TEXT NOT NULL DEFAULT 'agent' CHECK (role IN ('agent', 'assistant')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  expires_at TIMESTAMPTZ NOT NULL DEFAULT NOW() + INTERVAL '30 days'
);
ALTER TABLE team_invites ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users manage own invites" ON team_invites FOR ALL USING (true);
CREATE INDEX IF NOT EXISTS idx_invites_token ON team_invites(invite_token);
CREATE INDEX IF NOT EXISTS idx_invites_inviter ON team_invites(inviter_id);

-- Lead forwarding addresses
CREATE TABLE IF NOT EXISTS lead_forwarding_addresses (
  user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  address TEXT NOT NULL UNIQUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
ALTER TABLE lead_forwarding_addresses ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users manage own forwarding" ON lead_forwarding_addresses FOR ALL USING (true);
```

## NEW FILES SUMMARY

| File | Category | Purpose |
|------|----------|---------|
| `src/app/(auth)/personalize/page.tsx` | P | 6-screen personalization wizard |
| `src/components/onboarding/PersonalizationCard.tsx` | P | Visual card component |
| `src/components/onboarding/PersonalizationPills.tsx` | P | Pill button group |
| `src/actions/personalization.ts` | P | Save persona data per screen |
| `src/components/onboarding/CSVImportStep.tsx` | O | CSV upload + column mapping |
| `src/components/onboarding/EmailSyncStep.tsx` | O | Gmail connect step |
| `src/components/onboarding/MLSConnectionStep.tsx` | O | Repliers MLS board selector |
| `src/components/onboarding/PhoneSetupStep.tsx` | O | (part of Step 5) |
| `src/components/onboarding/WorkspacePresetStep.tsx` | O | 3 preset cards |
| `src/components/onboarding/TeamInviteStep.tsx` | O | Email inputs + invite link |
| `src/components/onboarding/CelebrationScreen.tsx` | C | Fireworks + checkmarks + welcome |
| `src/components/onboarding/AnimatedCheckmark.tsx` | C | SVG checkmark animation |
| `src/hooks/useConfetti.ts` | C | canvas-confetti wrapper |
| `src/components/onboarding/AIBioGenerator.tsx` | A | Generate bio button + typewriter |
| `src/components/onboarding/AIContactCategorizer.tsx` | A | Review categorized contacts |
| `src/components/dashboard/OnboardingBanner.tsx` | PO | Setup progress banner |
| `src/components/dashboard/SuggestedAutomations.tsx` | A | AI automation suggestions |
| `src/components/shared/UpgradePrompt.tsx` | B | Upgrade overlay for gated pages |
| `src/components/layout/TrialBanner.tsx` | B | Trial countdown top bar |
| `src/components/layout/EmailVerificationBanner.tsx` | S | Verify email top bar |
| `src/actions/ai-onboarding.ts` | A | generateBio, categorize, briefing, suggest |
| `src/actions/checklist.ts` | PO | Server-backed checklist logic |
| `src/actions/drip.ts` | D | Welcome drip send + skip logic |
| `src/app/api/onboarding/checklist/route.ts` | PO | GET/POST checklist state |
| `src/app/api/cron/welcome-drip/route.ts` | D | Daily drip cron |
| `src/app/api/cron/trial-expiry/route.ts` | B | Daily trial downgrade cron |
| `src/app/api/team/invite/route.ts` | O | Send team invites |
| `src/app/(auth)/join/[token]/page.tsx` | O | Invite join flow |
| `src/app/(dashboard)/settings/billing/page.tsx` | B | Plan comparison + trial info |
| `src/emails/WelcomeDay0.tsx` | D | Welcome email |
| `src/emails/WelcomeDay1.tsx` | D | Contacts nudge |
| `src/emails/WelcomeDay2.tsx` | D | AI features highlight |
| `src/emails/WelcomeDay3.tsx` | D | Newsletter nudge |
| `src/emails/WelcomeDay5.tsx` | D | Calendar + showings |
| `src/emails/WelcomeDay7.tsx` | D | Mid-trial stats |
| `src/emails/WelcomeDay12.tsx` | D | Trial expiry warning |
| `src/emails/TrialExpired.tsx` | B | Trial ended email |
| `supabase/migrations/095_onboarding_overhaul.sql` | ALL | Single migration for all new columns + tables |

## MODIFIED FILES SUMMARY

| File | Changes |
|------|---------|
| `src/app/(auth)/signup/page.tsx` | Rewrite: 3 fields, social proof, trial badge |
| `src/app/api/auth/signup/route.ts` | 3 fields, trial setup, verification email trigger |
| `src/app/(auth)/onboarding/page.tsx` | Major rewrite: 7 steps, adaptive order, phone field |
| `src/lib/auth.ts` | Trial logic in JWT, personalization/trial in session |
| `src/lib/plans.ts` | `isTrialActive()`, `getEffectivePlan()` |
| `src/lib/features.ts` | No change (already correct) |
| `src/middleware.ts` | Add `/personalize` + `/join/*` to allowlist, onboarding gate |
| `src/actions/onboarding.ts` | Expand to 8 steps, adaptive ordering, phone save, sample data by market |
| `src/components/help/OnboardingChecklist.tsx` | Rewrite: server-backed, auto-detect, progress ring |
| `src/app/(dashboard)/layout.tsx` | Add TrialBanner, EmailVerificationBanner, OnboardingBanner |
| `src/app/(dashboard)/page.tsx` | Read dashboard_preset, show AI briefing card |
| `src/actions/listings.ts` | Auto-trigger MLS remarks on first listing |
| `src/app/globals.css` | Add keyframes: gradient-shimmer, stagger-check |
| `src/app/(dashboard)/contacts/page.tsx` | Empty state when 0 contacts |
| `src/app/(dashboard)/listings/page.tsx` | Empty state when 0 listings |
| `src/app/(dashboard)/newsletters/page.tsx` | Empty state when 0 newsletters |
| `src/app/(dashboard)/showings/page.tsx` | Empty state when 0 showings |
| `src/app/(dashboard)/calendar/page.tsx` | Empty state when no calendar |

## MIGRATION SAFETY NOTES

- All `ALTER TABLE` uses `IF NOT EXISTS` — idempotent, safe to re-run
- `drip_unsubscribed` column: does NOT exist in migration 074 (checked) — safe to add
- `personalization_completed` column: does NOT exist yet — safe to add
- `trial_ends_at`, `trial_plan` columns: do NOT exist yet — safe to add
- New tables (`onboarding_checklist`, `team_invites`, `lead_forwarding_addresses`): all use `IF NOT EXISTS`
- **RLS policies**: use `USING (true)` — permissive for now. Phase 2: scope to `auth.uid() = user_id` when Supabase auth is integrated
- **Run order**: Single migration file — no cross-migration dependencies
- **Rollback**: `supabase/rollbacks/095_rollback.sql` should DROP new tables and columns

## INFRA PREREQUISITES (Before Implementation)

| Prerequisite | Category | Status | Action |
|-------------|----------|--------|--------|
| `canvas-confetti` npm package | Celebration | Not installed | `npm install canvas-confetti` + `npm install -D @types/canvas-confetti` |
| `papaparse` npm package | Onboarding (CSV) | Not installed | `npm install papaparse` + `npm install -D @types/papaparse` |
| Resend domain verification | Drip emails | May need update | Verify `updates.realtors360.com` in Resend dashboard for founder sender |
| DNS MX for `leads.realtors360.com` | Lead forwarding | Not configured | Configure Resend Inbound or Mailgun for inbound email parsing. **Gate**: if not ready, hide I3 step |
| Twilio AvailablePhoneNumbers API | Phone setup | Twilio configured | Verify API access for listing/provisioning BC numbers |
| Vercel cron config | Drip + Trial | Needs setup | Add cron schedules to `vercel.json`: `/api/cron/welcome-drip` (daily 9 AM UTC), `/api/cron/trial-expiry` (daily midnight UTC) |

## TEST PLAN — Per Category

### Category 1: Signup
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| 3-field signup | E2E | Visit /signup → fill name+email+password → submit | Account created, redirects to /personalize, `trial_ends_at` set in DB |
| Google SSO signup | E2E | Click "Continue with Google" → complete OAuth | Account created with trial, redirects to /personalize |
| Duplicate email | E2E | Signup with existing email | 409 error shown inline, "Sign in instead" link |
| Disposable email | E2E | Type `test@mailinator.com` in email field | Inline error "Please use a non-disposable email" before submit |
| Password strength | Unit | Enter "abc12345" / "Abc12345" / "Abc123!@" | Weak / Medium / Strong meter |
| Verification email | E2E | After signup, check Resend logs | Magic link email sent. Banner shows on /personalize |
| Social proof (desktop) | Visual | Visit /signup on 1280px viewport | Left panel with testimonials visible |
| Social proof (mobile) | Visual | Visit /signup on 375px viewport | Single quote above form, no left panel |

### Category 2: Personalization
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| Full 6-screen flow | E2E | Complete all 6 screens selecting one option each | All columns saved in DB. Redirects to /onboarding |
| Skip all screens | E2E | Click "Skip for now" on every screen | Defaults applied. `personalization_completed: true`. Redirects to /onboarding |
| Back navigation | E2E | Advance to Screen 3 → click back → click back | Back to Screen 1 with previous selections preserved |
| Refresh mid-wizard | E2E | Complete Screen 2 → refresh page | Resumes at Screen 3 (first unanswered) |
| Middleware gate | E2E | As new user, navigate to `/contacts` | Redirected to /personalize |
| Already completed | E2E | Visit /personalize when `personalization_completed: true` | Redirected to /onboarding or / |
| Mobile cards | Visual | Complete Screen 1 on 375px viewport | 2x2 card grid, scrollable |

### Category 3: Onboarding
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| 7-step completion | E2E | Complete all 7 steps | `onboarding_completed: true`. CelebrationScreen shown |
| CSV import | E2E | Upload CSV with 10 contacts | Column mapping shown. Preview of 5 rows. Import succeeds. 10 contacts in DB |
| CSV edge cases | Unit | Empty CSV, >5000 rows, no email column, duplicate emails | Appropriate error messages for each |
| AI bio generator | E2E | In Step 5, enter brokerage → click "Generate with AI" | Bio appears with typewriter effect. Editable. Saved to DB |
| AI bio failure | Unit | Mock Claude API failure | Toast "AI unavailable". Button shows "Try again" |
| Team invite (Team Lead) | E2E | As Team Lead persona, reach Step 6 → enter 2 emails | Invites queued in `team_invites` with `status: pending` |
| Team invite (Solo Agent) | E2E | As Solo Agent persona, go through wizard | Team invite step is skipped entirely |
| Skip all | E2E | Skip every step | CelebrationScreen still shows. Checklist on dashboard has all items incomplete |
| Resume after leave | E2E | Complete steps 1-3 → close tab → return | Resumes at step 4 |
| Adaptive order | E2E | Persona with "Email Marketing" focus | Email Sync appears as Step 2 (before Contacts) |

### Category 4: Celebration
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| Full celebration | E2E | Complete onboarding step 7 | Phase 1: 4 staggered checkmarks. Phase 2: fireworks + welcome with name. Phase 3: "Go to Dashboard" button |
| Confetti fires | Visual | Complete onboarding | canvas-confetti particles from both sides in brand colors |
| Reduced motion | E2E | Set `prefers-reduced-motion: reduce` in OS | No confetti, no stagger, no shimmer. Static checkmarks + text |
| Auto-redirect | E2E | Complete onboarding, don't click button for 15s | Auto-redirects to dashboard |
| Counter | Visual | Complete onboarding as Professional trial user | "{N} features unlocked" counts up from 0 |

### Category 5: Post-Onboarding
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| Checklist appears | E2E | Complete onboarding → land on dashboard | Bottom-right checklist widget with progress ring |
| Auto-detection | E2E | Create a contact → refresh dashboard | "First contact" item auto-completes in checklist |
| Wizard items detected | E2E | Connected calendar in wizard → check dashboard | "Calendar connected" shows as done in checklist |
| Dismiss checklist | E2E | Click dismiss (X) on checklist | Checklist hidden. Doesn't reappear on refresh |
| Empty state — contacts | E2E | As new user (0 contacts), visit /contacts | Empty state with "Import Contacts" CTA shown |
| Dashboard banner | E2E | Land on dashboard with checklist < 100% | "Complete your setup" banner with progress bar + next action |
| First-visit tooltip | E2E | Visit /contacts for first time | Tooltip: "Click + to add your first contact". Doesn't show on second visit |

### Category 6: Welcome Drip
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| Day 0 immediate | E2E | Create account → check Resend logs | Welcome email sent within 30s. Logged to `welcome_drip_log` |
| Day 1 skip | Integration | Create account → import 3 contacts → trigger cron Day 1 | Day 1 email skipped (contacts > 0). `skipped: true` logged |
| Day 1 send | Integration | Create account → no contacts → trigger cron Day 1 | Day 1 "Import contacts" email sent |
| Day 7 mid-trial | Integration | Create account → wait 7 days → trigger cron | Mid-trial stats email with usage counts |
| Day 12 expiry | Integration | Create account → wait 12 days → trigger cron | Trial expiry warning with plan comparison |
| Unsubscribe | E2E | Click unsubscribe link in drip email | `drip_unsubscribed: true`. No future drip emails |
| Bounce handling | Integration | Simulate Resend bounce webhook | `bounced_at` set. All future drip stopped |
| Already upgraded | Integration | Upgrade to Professional → trigger Day 7 cron | Day 7 trial email skipped (plan != free) |

### Category 7: AI Onboarding
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| Bio generation | E2E | In Step 5, enter brokerage "RE/MAX" + market "Residential" → click Generate | Claude returns 2-3 sentence bio. Typewriter fills textarea |
| Contact categorization | E2E | Import 10 contacts via Gmail → complete import | AI categorizer screen shows with type suggestions. "Accept All" saves types |
| Dashboard briefing | E2E | Complete onboarding → land on dashboard | AI briefing card with 3 recommendations |
| MLS remarks auto | E2E | Add first listing from dashboard | Toast: "AI generated your MLS remarks!" Remarks visible on listing page |
| All AI failures | Unit | Mock all Claude calls to fail | Each feature shows fallback (hardcoded defaults or skip). No user flow blocked |

### Category 8: Integrations
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| Email sync (Google SSO) | E2E | Sign up via Google → reach Step 3 | Auto-shows "Connected via Google" green checkmark |
| Email sync (email signup) | E2E | Sign up via email → reach Step 3 | Shows "Connect Gmail" OAuth button |
| MLS connection | E2E | As non-new-agent, reach Step 6 | Board dropdown + area selector shown. Saves to `user_integrations` |
| MLS skip (new agent) | E2E | As "New to Real Estate" persona, go through wizard | Step 6 (MLS) is skipped entirely |
| Twilio number | E2E | In Step 5, see available numbers → select one | Number provisioned. Saved to `users.twilio_number` |

### Category 9: Billing
| Test | Type | Steps | Expected |
|------|------|-------|----------|
| Trial active | E2E | As new user, visit /settings/billing | Shows "Professional trial" + "X days remaining" countdown |
| Plan comparison | Visual | Visit /settings/billing | 4-column comparison matrix with checkmarks |
| Trial expiry cron | Integration | Set user `trial_ends_at` to yesterday → trigger cron | User downgraded: `plan: "free"`, features recalculated. Email sent |
| Data post-downgrade | E2E | After trial expires, visit /listings | Existing listings visible (read-only). "Create Listing" button shows UpgradePrompt |
| Trial banner | E2E | As user with 3 days remaining | Thin yellow banner: "3 days left" with Upgrade + Dismiss |
| Upgrade prompt | E2E | As free user, navigate to /listings | UpgradePrompt overlay: "Upgrade to Professional" |

### Full E2E Journey Test
| Step | Action | Expected |
|------|--------|----------|
| 1 | Visit /signup | 3-field form + social proof (desktop) |
| 2 | Fill name/email/password → submit | Success screen → auto-signin → /personalize |
| 3 | Complete 6 personalization screens | All saved to DB. Redirects to /onboarding |
| 4 | Complete 7 onboarding steps (import CSV, connect calendar, generate AI bio) | Steps advance. CSV imported. Calendar connected. Bio generated |
| 5 | CelebrationScreen | Fireworks + checkmarks + "Welcome, {name}!" + counter |
| 6 | Click "Go to Dashboard" | Dashboard with: OnboardingBanner + Checklist (partially complete) + AI briefing card |
| 7 | Create first listing | AI MLS remarks auto-generated. Checklist auto-updates "First listing" ✓ |
| 8 | Check email (Day 0) | Welcome email received |
| 9 | Wait 14 days, trigger trial-expiry cron | Downgraded to free. "Trial ended" email. Listings now read-only |
| 10 | Visit /listings | UpgradePrompt: "Upgrade to Professional to manage listings" |
